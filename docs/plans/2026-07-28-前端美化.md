# 前端美化与体验优化报告 v2.1（2026-07-28）

> **v2.1 说明**：在 v2（浅色重构方向）基础上细化为**可直接实施的工程规格**——所有改动给出完整代码或逐行映射，实施时按批次照做即可。五项关键决策已由本人拍板（见第一节）。
>
> 范围：`frontend/` 整体视觉重构 + 体验优化

---

## 一、决策记录（2026-07-28 本人确认）

| # | 决策点 | 结论 | 影响章节 |
|---|--------|------|----------|
| D1 | 登录页联系方式 | **邮箱（mailto）+ 微信号**（已提供，见第十章） | L4 |
| D2 | 侧边栏定位 | **重构为功能导航**：快捷提问入口移入侧栏 + 状态精简 | R7 |
| D3 | 登录页能力简介 | **加**：登录卡下方三条能力简介 | L2 |
| D4 | 用户消息头像 | **保留**（不随参考系移除） | R2 |
| D5 | 轨迹流光签名动效 | **做**：流式期间实时轨迹 + 连接线流光 | R8 |

---

## 二、设计主张与参考系取舍（沿用 v2）

**一句话主张**：用 ChatGPT 的骨架装豆包的温度——浅色数据工作台，Lottie 角色是唯一吉祥物，品牌青绿从"主色"收敛为"小面积强调色"。

| 模式 | 取舍 |
|------|------|
| 主题 | 浅色（`#fff` 主区 + `#f9f9f9` 侧栏），灰阶取 ChatGPT 中性系 |
| 消息版式 | 取豆包：AI 无气泡全宽 + 保留 Lottie 头像；用户品牌色浅气泡右对齐 |
| 对话列 | 768px 居中，消息列与输入框同宽 |
| 输入框 | 28px 圆角 pill 容器 + 悬浮阴影 + 近黑圆形发送钮 |
| 欢迎屏 | 取豆包：居中 96px 大头像 + 问候 + 建议 chips |
| 消息操作 | hover 显现、消息下方行内（触屏常显） |

---

## 三、Token 系统（浅色目标态，完整定义）

### 3.1 CSS 变量（写入 `global.css` 的 `:root`）

```css
:root {
  /* 背景 */
  --bg-base: #ffffff;
  --bg-sider: #f9f9f9;
  --bg-bubble-user: #eafaf3;
  --bg-subtle: rgba(0, 0, 0, 0.03);        /* 卡片头/表头浅底 */
  /* 品牌 */
  --accent: #63e2b7;                        /* 仅小面积点缀：selection、头像光晕 */
  --accent-strong: #0b7a55;                 /* 工作色：链接/强调/焦点/流光（白底 4.6:1） */
  --accent-strong-hover: #0d9463;
  --ink: #1f2328;                           /* 主按钮（近黑，ChatGPT 风） */
  --ink-hover: #3a4048;
  /* 文本 */
  --text-1: #1f2328;                        /* 15.8:1 主文本 */
  --text-2: #5c6370;                        /* 7.0:1  次要文本 */
  --text-3: #6b7280;                        /* 4.7:1  辅助/时间戳（过 AA） */
  /* 结构 */
  --border: rgba(0, 0, 0, 0.08);
  --border-strong: rgba(0, 0, 0, 0.12);
  --shadow-pop: 0 2px 12px rgba(0, 0, 0, 0.06);
  --shadow-card: 0 8px 32px rgba(0, 0, 0, 0.08);
  --radius-card: 10px;
  --radius-pill: 999px;
  /* 工具轨迹色（浅底可读版，写入 tool-constants.ts） */
  --tool-query: #4f83e0;
  --tool-viz: #0b7a55;
  --tool-rag: #d97706;
  --tool-explain: #9333ea;
}
```

### 3.2 字号体系（T1~T6，收敛现有 15 种散值）

| 级别 | 值 | 字重 | 应用处 |
|------|-----|------|--------|
| T1 | 1.5rem | 700 | 欢迎屏标题、登录页品牌标题（1.8→1.5 收敛） |
| T2 | 1.0rem | 600 | 快捷入口名、侧栏标题 |
| T3 | 0.875rem | 400 | 消息正文（0.92 收敛） |
| T4 | 0.8125rem | 400 | 辅助说明、轨迹摘要、快捷入口描述 |
| T5 | 0.75rem | 400 | 时间戳、标签、hint |
| T6 | 0.6875rem | 600 | 全大写小标签（sb-label、tech-badge） |

### 3.3 字体与数字

- 不引入 webfont（小带宽 + 中文场景），统一系统栈：`'"Segoe UI", -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif'`
- 等宽：`ui-monospace, 'Cascadia Code', 'JetBrains Mono', Consolas, monospace`
- 数据场景 `font-variant-numeric: tabular-nums`（数据表、状态数字）

---

## 四、批次一：浅色地基工程（逐文件规格）

### 4.1 `src/App.vue`（完整替换 script + template 行）

template 第 2 行去掉 `:theme`：

```vue
<n-config-provider :theme-overrides="themeOverrides">
```

script 完整替换为：

```ts
import type { GlobalThemeOverrides } from 'naive-ui'

const themeOverrides: GlobalThemeOverrides = {
  common: {
    fontFamily: '"Segoe UI", -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif',
    borderRadius: '10px',
    primaryColor: '#0b7a55',
    primaryColorHover: '#0d9463',
    primaryColorPressed: '#096a48',
    primaryColorSuppl: '#0b7a55',
    textColor1: '#1f2328',
    textColor2: '#5c6370',
    textColor3: '#6b7280',
    borderColor: 'rgba(0,0,0,0.08)',
  },
  Layout: {
    siderColor: '#f9f9f9',
    color: '#ffffff',
  },
  Card: {
    color: '#ffffff',
    borderColor: 'rgba(0,0,0,0.08)',
  },
  Input: {
    color: '#ffffff',
    colorFocus: '#ffffff',
    border: '1px solid rgba(0,0,0,0.12)',
    borderHover: '1px solid rgba(0,0,0,0.2)',
    borderFocus: '1px solid #0b7a55',
    boxShadowFocus: '0 0 0 2px rgba(11,122,85,0.15)',
  },
  Button: {
    colorPrimary: '#1f2328',
    colorHoverPrimary: '#3a4048',
    colorPressedPrimary: '#111418',
    textColorPrimary: '#ffffff',
    textColorHoverPrimary: '#ffffff',
    textColorPressedPrimary: '#ffffff',
  },
  DataTable: {
    thColor: 'rgba(0,0,0,0.02)',
    thTextColor: '#5c6370',
    borderColor: 'rgba(0,0,0,0.06)',
  },
}
```

> 决策：Naive `primary`（accent-strong）承载链接/焦点/tag 语义；**主操作按钮统一近黑**（Button.colorPrimary = --ink），登录"进入工作台"与发送钮自动同风格，无需逐处改。

### 4.2 `src/utils/markdown.ts`

第 5 行：`import 'highlight.js/styles/github-dark.css'` → `import 'highlight.js/styles/github.css'`

### 4.3 `src/styles/global.css`（完整替换）

```css
/* ============================================================
   Xiehaoyu-Agent — Global Styles (Light)
   ============================================================ */

:root {
  /* 见 3.1 完整变量表，原样粘贴于此 */
}

html, body, #app {
  margin: 0;
  padding: 0;
  height: 100%;
  background: var(--bg-base);
  color: var(--text-1);
  font-family: 'Segoe UI', -apple-system, 'PingFang SC', 'Microsoft YaHei', sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* ── Scrollbar ── */
::-webkit-scrollbar { width: 5px; height: 5px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(0, 0, 0, 0.15); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: rgba(0, 0, 0, 0.25); }

/* ── Selection ── */
::selection { background: rgba(99, 226, 183, 0.35); color: var(--text-1); }

/* ── Focus ring（F5）── */
:focus-visible {
  outline: 2px solid var(--accent-strong);
  outline-offset: 2px;
  border-radius: 4px;
}

/* ── Markdown Code Blocks ── */
pre code.hljs {
  border-radius: 10px;
  padding: 1rem 1.1rem;
  font-size: 0.84rem;
  line-height: 1.55;
  font-family: ui-monospace, 'Cascadia Code', 'JetBrains Mono', Consolas, monospace;
}
:not(pre) > code {
  background: rgba(0, 0, 0, 0.05);
  padding: 0.12em 0.4em;
  border-radius: 4px;
  font-size: 0.88em;
}

/* ── Tabular numbers（F10）── */
.n-data-table, .si-val { font-variant-numeric: tabular-nums; }

/* ── Animations ── */
@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(10px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeIn {
  from { opacity: 0; } to { opacity: 1; }
}
@keyframes slideInRight {
  from { opacity: 0; transform: translateX(20px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes slideInLeft {              /* F7 移动端侧栏 */
  from { opacity: 0; transform: translateX(-20px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes shake {                    /* L3 登录错误 */
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-6px); }
  75% { transform: translateX(6px); }
}
@keyframes flowDown {                 /* R8 轨迹流光 */
  from { background-position: 0 -100%; }
  to   { background-position: 0 100%; }
}
@keyframes blink {
  0%, 100% { opacity: 1; } 50% { opacity: 0; }
}

.chat-message-enter { animation: fadeInUp 0.35s ease-out; }
.fade-in { animation: fadeIn 0.3s ease-out; }
.slide-in { animation: slideInRight 0.3s ease-out; }

/* ── Streaming Cursor ── */
.streaming-cursor::after {
  content: '▊';
  animation: blink 1s step-end infinite;
  color: var(--accent-strong);
}

/* ── Naive UI Overrides ── */
.n-card { --n-border-color: var(--border) !important; }
.n-tabs .n-tabs-tab { transition: color 0.2s, background 0.2s; }
.n-button { transition: all 0.2s ease; }
.n-code { border-radius: 8px; }

/* ── Reduced motion（F6）── */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* ── Utility ── */
.truncate { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
```

### 4.4 `src/utils/tool-constants.ts`（工具轨迹色换浅底可读版）

`STEP_COLORS` 替换：

```ts
export const STEP_COLORS: Record<string, string> = {
  query_data: '#4f83e0',
  visualize: '#0b7a55',
  introduce_me: '#d97706',
  explain_result: '#9333ea',
}
```

（`TOOL_LABELS` / `TAG_MAP` / `CHART_LABELS` 不变；顶部注释已在上轮修正。）

### 4.5 组件颜色替换映射总表（逐文件逐值）

**`ChatMessage.vue` scoped style**：

| 行（约） | 旧值 | 新值 |
|---|---|---|
| .msg-role | `color: #777; font-size: 0.76rem` | `color: var(--text-3); font-size: 0.75rem` |
| .msg-time | `color: #555; font-size: 0.7rem` | `color: var(--text-3); font-size: 0.75rem` |
| .chat-message.user .msg-bubble | `background: linear-gradient(...); border: 1px solid rgba(99,102,241,0.15)` | `background: var(--bg-bubble-user); border: none` |
| .msg-content | `color: #e0e0e0; font-size: 0.92rem` | `color: var(--text-1); font-size: 0.875rem` |
| .msg-content pre | `background: rgba(0,0,0,0.3) !important` | **删除整条**（交 github 浅色主题 `#f6f8fa`） |
| .msg-content blockquote | `border-left: 3px solid #63e2b7; color: #999` | `border-left: 3px solid var(--accent-strong); color: var(--text-2)` |
| .msg-content a | `color: #63e2b7` | `color: var(--accent-strong)` |
| .msg-content th/td | `border: 1px solid rgba(255,255,255,0.1)` | `border: 1px solid var(--border)` |
| .msg-content th | `background: rgba(255,255,255,0.05)` | `background: var(--bg-subtle)` |
| .msg-tool-status | `color: #f59e0b` | `color: var(--tool-rag)`（#d97706 浅底可读橙） |
| .inline-result | `border: 1px solid rgba(255,255,255,0.06)` | `border: 1px solid var(--border); background: #fff` |
| .ir-section | `border-bottom: 1px solid rgba(255,255,255,0.05)` | `border-bottom: 1px solid rgba(0,0,0,0.05)` |
| .ir-section-header | `color: #ccc; background: rgba(255,255,255,0.02)` | `color: var(--text-2); background: var(--bg-subtle)` |
| .irt-connector | `background: rgba(255,255,255,0.08)` | `background: rgba(0,0,0,0.1)` |
| .irt-num | `color: #555` | `color: var(--text-3)` |
| .irt-summary | `color: #aaa` | `color: var(--text-2)` |
| 模板内联 icon 色 | `color="#63e2b7"`（图表）、`color="#63a4ff"`（数据表）、`color="#f59e0b"`（轨迹） | 分别改 `#0b7a55`、`#4f83e0`、`#d97706` |

**`ChatSidebar.vue` scoped style**：

| 位置 | 旧值 | 新值 |
|---|---|---|
| .sh-title | `#e0e0e0` | `var(--text-1)` |
| .sh-subtitle | `#666` | `var(--text-3)` |
| .sidebar-header | `border-bottom: 1px solid rgba(255,255,255,0.06)` | `1px solid rgba(0,0,0,0.06)` |
| .sb-label | `#555` | `var(--text-3)` |
| .status-item | `background: rgba(255,255,255,0.04)` | `background: #fff; border: 1px solid var(--border)` |
| .si-val | `#e0e0e0` | `var(--text-1)` |
| .si-lbl | `#666` | `var(--text-3)` |
| 模板状态点内联色 | `'#f59e0b' : '#63e2b7'` | `'#d97706' : '#0b7a55'` |
| .tech-badge | `background: rgba(255,255,255,0.05); color: #666` | `background: rgba(0,0,0,0.04); color: var(--text-2)` |

**`ChatInput.vue` scoped style**：

| 位置 | 旧值 | 新值 |
|---|---|---|
| .input-container | `background: rgba(255,255,255,0.04); border: 1px solid rgba(255,255,255,0.1); border-radius: 20px` | `background: #fff; border: 1px solid var(--border); border-radius: 28px; box-shadow: var(--shadow-pop)` |
| .input-container:focus-within | `border-color: rgba(99,226,183,0.4); background: rgba(255,255,255,0.05)` | `border-color: var(--accent-strong); background: #fff` |
| .char-count / .input-hint | `#555` | `var(--text-3)` |

**`ChatMain.vue` / `ChatView.vue`**：

| 位置 | 旧值 | 新值 |
|---|---|---|
| ChatMain .messages-list | `max-width: 860px` | `max-width: 768px`（R3） |
| ChatView .mobile-bar | `border-bottom: 1px solid rgba(255,255,255,0.08); background: rgba(16,16,20,0.95)` | `1px solid var(--border); background: rgba(255,255,255,0.95)` |
| ChatView .mobile-title | `#e0e0e0` | `var(--text-1)` |

**`LoginView.vue`**（详见第六章 L1/L3/L4，此处仅底色）：`.login-wrapper background: #0a0a0f → #ffffff`；`.login-card background/border` 白化；`.brand-title` 去渐变改 `var(--text-1)`；`.brand-subtitle` `#777 → var(--text-2)`；`.login-footer #555 → var(--text-3)`。

**`WelcomeCard.vue`**：整体随 R5 重写（第五章），旧暗色值全部废弃。

**`AnimatedAvatar.vue`**：`.animated-avatar background: rgba(99,226,183,0.06)` 浅底保留可用，不改。

### 4.6 批次一验证

`vue-tsc -b && vite build` 通过；浏览器走查：全站浅色、代码块 `#f6f8fa`、Naive 组件（tag/collapse/data-table/button）无暗色残留、滚动条浅灰。

---

## 五、批次二：版式重构 R1~R8（含完整代码）

### R1 AI 消息去气泡化

`ChatMessage.vue`：
- assistant 侧结构不变（本就无气泡），`.chat-message` 间距 `padding: 1rem 0 → 1.5rem 0`
- 内联结果区浅色卡片化已在 4.5 完成（白底 + `--border` + 分隔细线），本节无额外改动

### R2 用户消息气泡（D4：保留头像）

- 头像保留（`ChatMessage.vue:5-7` 不动）
- 气泡样式按 4.5 映射改 `var(--bg-bubble-user)`，追加：`max-width: 70%; border-radius: 18px`
- `.msg-bubble` 加 `overflow-wrap: break-word`（长文本不撑破 70% 约束）

### R3 对话列与输入区同宽

- `ChatMain.vue:75` `.messages-list max-width: 860px → 768px`
- `ChatInput.vue:60` `.chat-input-wrapper max-width: 760px → 768px`

### R4 输入框 pill 化

`ChatInput.vue` 模板按钮区替换（样式随 4.5 映射）：

```vue
<n-button
  v-if="streaming"
  circle
  aria-label="停止生成"
  title="停止生成"
  class="send-btn"
  @click="emit('stop')"
>
  <template #icon>
    <n-icon size="16" color="#fff"><svg viewBox="0 0 24 24"><path fill="currentColor" d="M6 6h12v12H6z"/></svg></n-icon>
  </template>
</n-button>
<n-button
  v-else
  circle
  aria-label="发送"
  title="发送"
  class="send-btn"
  :disabled="!text.trim() || disabled"
  @click="handleSend"
>
  <template #icon>
    <n-icon size="18" color="#fff"><svg viewBox="0 0 24 24"><path fill="currentColor" d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/></svg></n-icon>
  </template>
</n-button>
```

新增样式（近黑圆钮，不用 n-button type，避免主题影响禁用态灰）：

```css
.send-btn {
  background: var(--ink) !important;
  transition: background 0.2s ease;
}
.send-btn:hover:not(:disabled) { background: var(--ink-hover) !important; }
.send-btn:disabled { background: #c9ccd1 !important; }
```

### R5 欢迎屏居中化（`WelcomeCard.vue` 完整重写）

数据先提取共享常量（R7 侧栏复用）——新建 `src/utils/quick-questions.ts`：

```ts
export interface QuickQuestion {
  label: string      // 短名（侧栏导航用）
  question: string   // 完整问题（chips 文案 + 实际发送内容）
  color: string      // 标识色
}

export const QUICK_QUESTIONS: QuickQuestion[] = [
  { label: '自我介绍', question: '介绍一下你自己', color: '#0b7a55' },
  { label: '数据分析', question: '2018 年每月订单数，帮我画个图', color: '#4f83e0' },
  { label: '项目经历', question: '你做过哪些和数据相关的项目？', color: '#d97706' },
]
```

`WelcomeCard.vue` 完整内容：

```vue
<template>
  <div class="welcome">
    <div class="welcome-icon">
      <AnimatedAvatar :state="welcomeState" :size="96" @animation-end="handleWelcomeEnd" />
    </div>
    <h2 class="welcome-title">你好，我是 Xiehaoyu-Agent</h2>
    <p class="welcome-desc">可以问我个人经历与技术栈，或直接发起数据分析请求。</p>

    <div class="chips" :class="{ disabled: chat.isStreaming }">
      <button
        v-for="q in QUICK_QUESTIONS"
        :key="q.question"
        class="chip"
        :disabled="chat.isStreaming"
        @click="handleQuick(q.question)"
      >
        {{ q.question }}
      </button>
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { useChatStore } from '@/stores/chat'
import AnimatedAvatar from '@/components/shared/AnimatedAvatar.vue'
import type { AvatarState } from '@/components/shared/AnimatedAvatar.vue'
import { QUICK_QUESTIONS } from '@/utils/quick-questions'

const chat = useChatStore()
const welcomeState = ref<AvatarState>('welcome')

function handleWelcomeEnd() {
  welcomeState.value = 'idle'
}

function handleQuick(q: string) {
  if (chat.isStreaming) return
  chat.sendMessage(q)
}
</script>

<style scoped>
.welcome {
  max-width: 560px;
  margin: 4rem auto 0;
  padding: 0 1rem;
  text-align: center;
}
.welcome-icon { margin-bottom: 1.25rem; }
.welcome-title {
  font-size: 1.5rem;          /* T1 */
  font-weight: 700;
  color: var(--text-1);
  margin: 0 0 0.5rem;
  letter-spacing: -0.01em;
}
.welcome-desc {
  color: var(--text-2);
  font-size: 0.875rem;        /* T3 */
  margin: 0 0 2rem;
}
.chips {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 0.5rem;
}
.chip {
  border: 1px solid var(--border);
  border-radius: var(--radius-pill);
  background: #fff;
  color: var(--text-1);
  font-size: 0.8125rem;       /* T4 */
  padding: 0.45rem 0.95rem;
  cursor: pointer;
  transition: background 0.2s ease, border-color 0.2s ease;
}
.chip:hover:not(:disabled) {
  background: rgba(0, 0, 0, 0.03);
  border-color: var(--border-strong);
}
.chips.disabled .chip {
  opacity: 0.45;
  cursor: not-allowed;
}
</style>
```

> 注意 chips 改用原生 `<button>`（语义化 + 自带焦点环受益 F5），Naive auto-import 不受影响。

### R6 消息操作行内化（含重试）

`ChatMessage.vue`：删除气泡内 `.msg-actions`（25-29 行），在 `.msg-footer` 位置替换为操作行（仅 assistant 且非流式时渲染）：

```vue
<div v-if="message.role === 'assistant' && !isStreaming && message.content" class="msg-actions">
  <button class="action-btn" aria-label="复制内容" title="复制" @click="copyContent">
    <n-icon size="14"><svg viewBox="0 0 24 24"><path fill="currentColor" d="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z"/></svg></n-icon>
  </button>
  <button v-if="isError" class="action-btn" aria-label="重试" title="重试" @click="retry">
    <n-icon size="14"><svg viewBox="0 0 24 24"><path fill="currentColor" d="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z"/></svg></n-icon>
  </button>
  <template v-if="message.steps">
    <n-tag size="tiny" :bordered="false" type="info" round>{{ message.steps }} 步</n-tag>
    <n-tag v-for="t in message.tools" :key="t" size="tiny" :bordered="false" round>{{ toolLabel(t) }}</n-tag>
  </template>
</div>
```

script 新增：

```ts
const isError = computed(() => props.message.content.startsWith('执行失败'))

function retry() {
  const idx = chat.messages.findIndex(m => m === props.message)
  const prevUser = chat.messages.slice(0, idx).reverse().find(m => m.role === 'user')
  if (prevUser && !chat.isStreaming) chat.sendMessage(prevUser.content)
}
```

样式：

```css
.msg-actions {
  display: flex; align-items: center; gap: 0.4rem;
  margin-top: 0.5rem;
  opacity: 0;
  transition: opacity 0.2s;
}
.chat-message:hover .msg-actions { opacity: 1; }
@media (hover: none) { .msg-actions { opacity: 1; } }  /* 触屏常显 */
.action-btn {
  display: flex; align-items: center; justify-content: center;
  width: 26px; height: 26px;
  border: none; border-radius: 6px;
  background: transparent; color: var(--text-3);
  cursor: pointer; transition: background 0.2s, color 0.2s;
}
.action-btn:hover { background: rgba(0,0,0,0.05); color: var(--text-1); }
```

> 用户消息的操作行可只对 assistant 开放（参考系如此）；user 气泡内不再放复制钮。

### R7 侧边栏重构为功能导航（D2，`ChatSidebar.vue` 规格）

结构（template 主体替换 `.sidebar-body` 内容）：

```vue
<div class="sidebar-body">
  <!-- 快捷提问（功能导航） -->
  <div class="sb-section">
    <div class="sb-label">快捷提问</div>
    <div class="nav-list">
      <button
        v-for="q in QUICK_QUESTIONS"
        :key="q.question"
        class="nav-item"
        :disabled="chat.isStreaming"
        @click="handleNav(q.question)"
      >
        <span class="nav-dot" :style="{ background: q.color }" />
        <span class="nav-text">{{ q.label }}</span>
        <span class="nav-question truncate">{{ q.question }}</span>
      </button>
    </div>
  </div>

  <!-- 会话状态（精简为单行） -->
  <div class="sb-section">
    <div class="sb-label">会话状态</div>
    <div class="status-line">
      <span class="status-dot" :style="{ background: chat.isStreaming ? '#d97706' : '#0b7a55' }" />
      <span class="status-text">
        {{ chat.isStreaming ? '处理中' : '就绪' }} · {{ chat.messages.length }} 条消息 · {{ chat.currentTrace.length }} 步
      </span>
    </div>
  </div>

  <!-- 操作 -->
  <div class="sb-section">
    <div class="sb-label">操作</div>
    <n-button block secondary size="small" :disabled="chat.messages.length === 0" @click="chat.clearChat()">
      清空对话
    </n-button>
  </div>
</div>
```

script 新增：

```ts
import { QUICK_QUESTIONS } from '@/utils/quick-questions'

function handleNav(question: string) {
  if (chat.isStreaming) return
  chat.sendMessage(question)
  emit('close')   // 移动端点选后收起侧栏；桌面端 close 无人监听无副作用
}
```

（script 顶部 `defineEmits` 已有 `close`，复用。原 status-grid 整块删除。）

样式：

```css
.nav-list { display: flex; flex-direction: column; gap: 2px; }
.nav-item {
  display: flex; align-items: center; gap: 0.55rem;
  width: 100%;
  padding: 0.55rem 0.6rem;
  border: none; border-radius: 8px;
  background: transparent;
  cursor: pointer; text-align: left;
  transition: background 0.15s ease;
}
.nav-item:hover:not(:disabled) { background: rgba(0, 0, 0, 0.04); }
.nav-item:disabled { opacity: 0.5; cursor: not-allowed; }
.nav-dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }
.nav-text { font-size: 0.875rem; font-weight: 600; color: var(--text-1); flex-shrink: 0; }
.nav-question { font-size: 0.75rem; color: var(--text-3); }
.status-line { display: flex; align-items: center; gap: 0.5rem; padding: 0.4rem 0.2rem; }
.status-dot { width: 8px; height: 8px; border-radius: 50%; }
.status-text { font-size: 0.8125rem; color: var(--text-2); }
```

> 侧栏头部（头像 + 标题）与底部（tech-badge + 退出）结构不变，颜色按 4.5 映射。

### R8 轨迹"数据流"流光签名动效（D5）

问题：当前内联轨迹仅在**非流式**时渲染（`ChatMessage.vue:49` `!isStreaming`），流式期间看不到轨迹。本项让签名元素真正可见——**流式期间在 loading 区渲染实时紧凑轨迹**，当前步连接线流光；流式结束后该区域被常规 inline-result 替代（静态）。

`ChatMessage.vue` 模板 `.msg-loading` 区块扩展：

```vue
<div v-else class="msg-loading">
  <div class="loading-row">
    <n-spin :size="16" />
    <span v-if="isStreaming && chat.currentTool" class="msg-tool-status">
      正在调用「{{ toolLabel(chat.currentTool) }}」…
    </span>
  </div>
  <!-- 实时轨迹（流式中，含流光） -->
  <div v-if="isStreaming && chat.currentTrace.length > 0" class="live-trace">
    <div v-for="(step, i) in chat.currentTrace" :key="i" class="irt-step">
      <div class="irt-line">
        <div class="irt-dot" :style="{ background: stepColor(step.tool) }" />
        <div
          v-if="i < chat.currentTrace.length - 1 || chat.currentTool"
          class="irt-connector"
          :class="{ flow: i === chat.currentTrace.length - 1 }"
        />
      </div>
      <div class="irt-body">
        <div class="irt-header">
          <n-tag size="tiny" :bordered="false" :type="tagType(step.tool)" round>{{ toolLabel(step.tool) }}</n-tag>
          <span class="irt-num">Step {{ i + 1 }}</span>
        </div>
        <div class="irt-summary">{{ step.summary }}</div>
      </div>
    </div>
  </div>
</div>
```

样式（流光核心）：

```css
.msg-loading { padding: 0.5rem 0; }
.loading-row { display: flex; align-items: center; gap: 0.5rem; }
.live-trace { margin-top: 0.6rem; }
.irt-connector.flow {
  background: linear-gradient(180deg, var(--accent-strong) 0%, rgba(0,0,0,0.1) 50%, var(--accent-strong) 100%);
  background-size: 100% 250%;
  animation: flowDown 1.2s linear infinite;
}
```

> 逻辑：`chat.currentTool` 非空时末步视为"正在流入"，其下方连接线带 `.flow`；reduced-motion 下 F6 的全局降级自动将动画时长归零（静止）。loading 区原有 `.msg-tool-status` 文本保留，二者互补（文本说"在做什么"，轨迹说"走到哪了"）。

### 批次二验证

构建通过；走查：AI 无气泡 / 用户浅青绿气泡 ≤70% / 列宽 768 对齐 / 输入框阴影与近黑钮 / 欢迎屏居中 chips 及禁用态 / 消息 hover 出现操作行 / 侧栏导航可点且移动端点选收起 / 流式中实时轨迹可见且末步连接线流光、结束后静态。

---

## 六、批次五：登录页改造 L1~L4（`LoginView.vue` 规格）

### L1 浅色背景与品牌区

- `.login-wrapper background → #ffffff`
- orb 3 → 1：仅保留 `.orb-1`（`#63e2b7`，`opacity: 0.15 → 0.10`），`.orb-2/.orb-3` 及其 CSS 删除
- `.login-bg` 追加数据点阵（伪元素）：

```css
.login-bg::after {
  content: '';
  position: absolute;
  inset: 0;
  background-image: radial-gradient(circle, rgba(11, 122, 85, 0.08) 1px, transparent 1px);
  background-size: 28px 28px;
  mask-image: radial-gradient(ellipse at center, black 30%, transparent 75%);
  -webkit-mask-image: radial-gradient(ellipse at center, black 30%, transparent 75%);
}
```

- `.brand-title`：删除渐变三行（`background` / `-webkit-background-clip` / `-webkit-text-fill-color` / `background-clip`），改 `color: var(--text-1); font-size: 1.5rem`（T1）
- `.brand-subtitle` 文案改结构化：`个人智能体 · <span class="accent">数据问答</span>工作台`，`.accent { color: var(--accent-strong); font-weight: 600 }`

### L2 能力简介区（D3）

登录卡下方新增（template 中 `</n-card>` 之后）：

```vue
<div class="caps">
  <div class="cap" v-for="c in caps" :key="c.title">
    <n-icon size="18" :color="'#0b7a55'"><svg viewBox="0 0 24 24"><path fill="currentColor" :d="c.icon" /></svg></n-icon>
    <div class="cap-title">{{ c.title }}</div>
    <div class="cap-desc">{{ c.desc }}</div>
  </div>
</div>
```

script 数据：

```ts
const caps = [
  { title: '个人问答', desc: '基于个人知识库，第一人称回答经历与背景', icon: 'M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17.93c-3.95-.49-7-3.85-7-7.93 0-.62.08-1.21.21-1.79L9 15v1c0 1.1.9 2 2 2v1.93zm6.9-2.54c-.26-.81-1-1.39-1.9-1.39h-1v-3c0-.55-.45-1-1-1H8v-2h2c.55 0 1-.45 1-1V7h2c1.1 0 2-.9 2-2v-.41c2.93 1.19 5 4.06 5 7.41 0 2.08-.8 3.97-2.1 5.39z' },
  { title: '数据问答', desc: '自然语言查询 Olist 电商数据集，自动生成 SQL', icon: 'M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zM9 17H7v-7h2v7zm4 0h-2V7h2v10zm4 0h-2v-4h2v4z' },
  { title: '图表解读', desc: '自动选择图表类型，输出业务洞察', icon: 'M3.5 18.49l6-6.01 4 4L22 6.92l-1.41-1.41-7.09 7.97-4-4L2 16.99l1.5 1.5z' },
]
```

样式（移动端自动竖排）：

```css
.caps {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0.75rem;
  margin-top: 1.5rem;
}
.cap {
  background: #fff;
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 0.9rem 0.8rem;
  text-align: center;
}
.cap-title { font-size: 0.875rem; font-weight: 600; color: var(--text-1); margin-top: 0.4rem; }
.cap-desc { font-size: 0.75rem; color: var(--text-3); margin-top: 0.25rem; line-height: 1.5; }
@media (max-width: 640px) { .caps { grid-template-columns: 1fr; } }
```

### L3 登录错误态

script：

```ts
const loginError = ref(false)
// catch 中：loginError.value = true
// watch(code, () => { loginError.value = false })
```

template：`n-input` 加 `:status="loginError ? 'error' : undefined"`；`.login-card` 绑定 `:class="{ shake: loginError }"`。

样式：

```css
.login-card.shake { animation: shake 0.3s ease; }
```

（`shake` keyframes 已在 global.css 4.3 定义；reduced-motion 下自动被 F6 降级。）

### L4 联系方式（D1）

卡片 footer 文案替换：

```vue
<template #footer>
  <p class="login-footer">
    如需访问码：
    <a class="contact-link" href="mailto:xiehaoyu12138@163.com">xiehaoyu12138@163.com</a>
    · 微信 <span class="contact-text">xhy18711807395</span>
  </p>
</template>
```

```css
.contact-link { color: var(--accent-strong); text-decoration: none; }
.contact-link:hover { text-decoration: underline; }
.contact-text { color: var(--text-2); user-select: all; }
```

> 联系方式已填入真实值（2026-07-28，见第十章）。

---

## 七、批次三/四：正交修复项 F1~F7（完整代码）

### F1 图表适配 P0（`ChartRenderer.vue`，含 #11 modeBar）

`render()` 函数体替换：

```ts
async function render() {
  const el = rootRef.value
  if (!el || !props.figureJson) return
  if (el.offsetWidth === 0 || el.offsetHeight === 0) {
    rafId = requestAnimationFrame(render)
    return
  }
  const Plotly = await ensurePlotly()          // F3，见下
  if (chartInstance) {
    Plotly.purge(el)
    chartInstance = null
  }
  try {
    const fig = JSON.parse(props.figureJson)
    const layout = {
      paper_bgcolor: 'rgba(0,0,0,0)',
      plot_bgcolor: 'rgba(0,0,0,0)',
      font: { color: '#5c6370', size: 11 },
      margin: { t: 40, r: 20, b: 50, l: 50 },
      ...fig.layout,                            // 后端显式设置优先（目前未设置）
    }
    Plotly.newPlot(el, fig.data, layout, {
      responsive: true,
      displaylogo: false,
      displayModeBar: false,                    // F1 顺带：modeBar 不常显
    }).then((plot: any) => { chartInstance = plot })
  } catch {
    // Malformed figure JSON; leave the chart area empty.
  }
}
```

> 注意 `render` 改 async 后，`requestAnimationFrame(render)` 与 `watch` 中的调用无需改动（fire-and-forget）；`ResizeObserver` 回调里的 `Plotly.Plots.resize` 需改为从已加载实例取——见 F3 完整方案。

### F2 Lottie 按状态懒加载（`AnimatedAvatar.vue` script 替换要点）

```ts
const animationDataMap: Record<AvatarState, () => Promise<{ default: any }>> = {
  idle: () => import('@/assets/lottie/idle.json'),
  thinking: () => import('@/assets/lottie/thinking.json'),
  answering: () => import('@/assets/lottie/answering.json'),
  welcome: () => import('@/assets/lottie/welcome.json'),
  presenting: () => import('@/assets/lottie/presenting.json'),
  error: () => import('@/assets/lottie/error.json'),
}

let loadToken = 0   // 竞态守卫：状态快速切换时丢弃过期加载

async function loadAnimation(state: AvatarState) {
  if (!containerRef.value) return
  const token = ++loadToken
  const { default: animationData } = await animationDataMap[state]()
  if (token !== loadToken || !containerRef.value) return   // 过期或已卸载

  if (animInstance) {
    animInstance.destroy()
    animInstance = null
  }
  const isLooping = loopingStates.has(state)
  const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches   // F6

  animInstance = lottie.loadAnimation({
    container: containerRef.value,
    renderer: 'svg',
    loop: isLooping,
    autoplay: !reduced,
    animationData,
  })
  if (reduced) {
    animInstance.goToAndStop(0, true)            // 静态首帧
    return
  }
  if (!isLooping) {
    animInstance.addEventListener('complete', () => {
      emit('animation-end')
    })
  }
}
```

（`onMounted` 与 `watch` 调用处不变；删除原 6 条静态 import。）

### F3 Plotly 按需加载（`ChartRenderer.vue`）

```ts
// 删除顶层 import Plotly from 'plotly.js-dist'
let plotlyPromise: Promise<any> | null = null
let PlotlyRef: any = null

function ensurePlotly() {
  if (!plotlyPromise) {
    plotlyPromise = import('plotly.js-dist').then(m => {
      PlotlyRef = m.default || m
      return PlotlyRef
    })
  }
  return plotlyPromise
}
```

`ResizeObserver` 回调改为：

```ts
resizeObserver = new ResizeObserver(() => {
  if (chartInstance && rootRef.value && PlotlyRef) {
    PlotlyRef.Plots.resize(rootRef.value)
  }
})
```

`onUnmounted` 中的 `Plotly.purge` 同样改用 `PlotlyRef?.purge(...)`。

### F4 aria-label 补全（4 处）

| 文件 | 元素 | 改动 |
|---|---|---|
| `ChatInput.vue` | 发送钮 | R4 新模板已含 `aria-label="发送"` |
| `ChatInput.vue` | 停止钮 | R4 新模板已含 `aria-label="停止生成"` |
| `ChatView.vue:14` | 汉堡钮 | `<n-button text size="small" aria-label="打开侧栏菜单" @click=...>` |
| `ChatMessage.vue` | 复制/重试 | R6 新模板已含 `aria-label` |

### F5 焦点环

已在 global.css 4.3 落地（`:focus-visible` 全局规则）。

### F6 reduced-motion 降级

- CSS 侧：已在 global.css 4.3 落地（全局 media query）
- Lottie 侧：已在 F2 落地（`matchMedia` 判定 + `goToAndStop(0, true)`）

### F7 移动端侧栏遮罩与 Esc（`ChatView.vue`）

template（`ChatSidebar` 之后加遮罩）：

```vue
<div
  v-if="isMobile && showSidebar"
  class="sidebar-mask"
  @click="showSidebar = false"
/>
```

script 增加 Esc：

```ts
function handleKeydown(e: KeyboardEvent) {
  if (isMobile.value && e.key === 'Escape') showSidebar.value = false
}
// onMounted 中 window.addEventListener('keydown', handleKeydown)
// onUnmounted 中 window.removeEventListener('keydown', handleKeydown)
```

样式：

```css
.sidebar-mask {
  position: fixed;
  inset: 0;
  z-index: 99;
  background: rgba(0, 0, 0, 0.4);
  animation: fadeIn 0.2s ease-out;
}
.mobile-overlay {
  /* 现有属性保留，追加： */
  animation: slideInLeft 0.25s ease-out;
}
```

---

## 八、实施批次与文件清单（v2.1）

| 批次 | 章节 | 涉及文件 | 验证 |
|------|------|----------|------|
| 一 浅色地基 | 4.1~4.5 + tabular-nums | App.vue、global.css、markdown.ts、tool-constants.ts、ChatMessage/ChatSidebar/ChatInput/ChatMain/ChatView（仅颜色） | 构建 + 全页面浅色走查 |
| 二 版式重构 | R1~R8 | ChatMessage.vue、ChatInput.vue、ChatMain.vue、WelcomeCard.vue、ChatSidebar.vue、新增 utils/quick-questions.ts | 构建 + 五章末验证项 |
| 三 正确性+性能 | F1~F3 | ChartRenderer.vue、AnimatedAvatar.vue | 构建 + 出图/动画冒烟、chunk 拆分确认 |
| 四 可访问性+交互 | F4~F7（多数已随前置批次落地） | ChatView.vue | 构建 + Tab/Esc/遮罩走查 |
| 五 登录页 | L1~L4 | LoginView.vue | 构建 + 登录页视觉与错误态走查 |

每批执行 `vue-tsc -b && vite build`。批次一/二/五需浏览器走查；批次三需真实出图问题冒烟（消耗一次 LLM 调用）。

---

## 九、回归验证清单（v2.1，含测试方法）

- [ ] **浅色完整性**：逐页检查（登录/聊天/侧栏/移动端），无暗色残留；重点：hljs 代码块 `#f6f8fa`、n-data-table、n-collapse、n-tag、滚动条
- [ ] **版式**：AI 消息全宽无气泡；用户气泡 `var(--bg-bubble-user)` 右对齐、宽度 ≤70%；消息列与输入框均 768px 且左右对齐
- [ ] **输入框**：28px 圆角 + `var(--shadow-pop)`；发送钮近黑圆钮白箭头；流式中变方块停止钮；聚焦边框 `--accent-strong`
- [ ] **欢迎屏**：96px 头像居中 + T1 标题 + 3 个 pill chips；点击 chips 发送对应问题；流式中 chips `opacity 0.45` 且点击无效
- [ ] **侧栏导航**：3 个入口可点发送；状态行实时（处理中/就绪 + 消息数 + 步数）；移动端点选后侧栏自动收起
- [ ] **操作行**：assistant 消息 hover 出现复制（+失败时重试）按钮；触屏设备常显；重试后原失败消息保留、新消息正常流式
- [ ] **轨迹流光**：流式中 loading 区下方实时轨迹逐步增长，末步连接线青绿流光扫动；结束后流光消失、轨迹进入常规折叠区；reduced-motion 下全程静止
- [ ] **图表**：透明底融入白卡、文字 `#5c6370` 可读、modeBar 不常显；多次查询时标签与图表一致
- [ ] **性能**：`vite build` 产物中 6 个 lottie JSON 与 plotly 为独立 chunk；登录首屏 Network 无 plotly、仅 welcome.json
- [ ] **可访问性**：Tab 遍历全程焦点环可见；4 个图标按钮有 aria-label；系统开启减少动态后 Lottie 静态首帧、光标不闪
- [ ] **移动端**：遮罩点击/Esc 关闭侧栏、slideInLeft 动画；caps 简介竖排；chips 换行不溢出
- [ ] **登录页**：单雾光 + 点阵背景；错误访问码 → 输入框红框 + 卡片 shake + toast；footer 邮箱可点开邮件客户端、微信号可选中复制

---

## 十、待定事项

| 事项 | 状态 |
|------|------|
| 登录页邮箱地址（mailto 链接） | 已提供：`xiehaoyu12138@163.com`（2026-07-28 填入 L4） |
| 登录页微信号（文本） | 已提供：`xhy18711807395`（2026-07-28 填入 L4） |

全部决策与素材已就绪，可按批次一开始实施。

---

## 十一、实施记录（2026-07-28）

批次一~五全部实施完毕，每批 `vue-tsc -b && vite build` 均通过。与规格的两处偏差：

1. **实施前修复了本文档的一处损坏**：L4 联系方式曾被误拼接到 F7 代码块中间（编辑事故），已复原 F7 并将联系方式填入 L4。
2. **R7 落地微调**：`global.css` 的等宽数字选择器从 `.si-val`（已随 status-grid 删除）改为 `.status-text`；`ChatSidebar` 的 `defineEmits` 改为赋值给 `emit` 以支持 `handleNav` 中 `emit('close')`。

性能优化实测效果（构建产物对比）：

| 指标 | 实施前 | 实施后 |
|------|--------|--------|
| AnimatedAvatar chunk | 2,788 kB | 414 kB（6 个动画 JSON 拆为独立 chunk 按需加载） |
| ChatView chunk | 5,986 kB | 1,334 kB（plotly 拆为独立 4,651 kB chunk，仅出图时加载） |
| 登录首屏 | 加载全部 lottie + plotly | 仅 welcome.json，无 plotly |

浏览器走查（第九章清单）需在真实环境验证，重点：浅色完整性、轨迹流光、图表透明底、移动端遮罩、登录错误 shake。

**追加变更（2026-07-28）**：
1. **登录 401 拦截修复（存量 bug）**：`api/client.ts` 全局 401 重定向误伤登录请求本身（错误访问码 → 整页刷新、提示丢失），新增 `skipAuthRedirect` 选项，`loginApi` 跳过全局重定向，错误由登录页内联展示（红框 + shake + toast）。
2. **快捷提问扩充（本人决策：分组展示 / 侧栏同步 / 不含敏感问题 / 含多步炫技题）**：`quick-questions.ts` 重构为 `QUICK_QUESTION_GROUPS` 两组 ×4 题——「了解我」（自我介绍/技术栈/挑战项目/数据项目）+「试试数据问答」（每月订单画图/各州分布分析/评分分布/品类订单分析，含多步全链路题）；WelcomeCard 分组 chips、ChatSidebar 分组导航同步渲染。
