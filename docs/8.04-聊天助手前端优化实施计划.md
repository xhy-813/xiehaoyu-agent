# AI聊天助手前端优化 — 实施计划

> **For agentic workers:** 按任务顺序执行，每个 Task 的 checkbox (`- [ ]`) 用于跟踪进度。每完成一个 Task 提交一次。

**目标：** 在保持现有 Dark Geek 风格的前提下，优化聊天助手前端的视觉层次、动效流畅度、交互细节和代码结构。

**架构：** 纯前端优化，不涉及后端 API 变更。修改集中在 `frontend/src/components/chat/`、`frontend/src/views/ChatView.vue`、`frontend/src/styles/global.css`、`frontend/src/utils/markdown.ts`。拆分 `ChatMessage.vue` 为 `MessageBubble.vue` + `InlineResult.vue`，提取 `WelcomeScreen.vue`。

**技术栈：** Vue 3 + TypeScript + Naive UI + Lottie Web + MarkdownIt + highlight.js

## 全局约束

- 保持现有 Dark Geek 风格（深蓝底 `#0a192f` + 薄荷绿 `#64ffda`）
- 深色/浅色双主题均需验证
- 不新增功能，不修改后端 API
- 移动端适配（`@media (max-width: 640px)` 和 `(max-width: 768px)`）
- 所有 CSS 变量在 `:root` 和 `[data-theme="light"]` 中同步定义
- 每完成一个 Task 提交：`git add` + `git commit -m "feat: <task description>"`

---

## 文件结构

```
frontend/src/
├── components/chat/
│   ├── ChatMessage.vue          # 重构：消息容器，组合子组件
│   ├── ChatWidget.vue           # 修改：滚动按钮 + stop 状态
│   ├── ChatInput.vue            # 修改：Escape 清空 + 移动端
│   ├── MessageBubble.vue        # 新增：气泡组件（头像+内容+操作）
│   ├── InlineResult.vue         # 新增：内联结果（图表+数据表+轨迹）
│   └── WelcomeScreen.vue        # 新增：欢迎页空状态
├── views/
│   └── ChatView.vue             # 修改：使用 WelcomeScreen
├── styles/
│   └── global.css               # 修改：新增 CSS 变量 + 动画
├── utils/
│   └── markdown.ts              # 修改：代码块注入复制按钮
```

---

### Task 1: CSS 变量补充 + 全局样式优化

**文件：**
- 修改：`frontend/src/styles/global.css`

**说明：** 新增设计文档中定义的 CSS 变量，优化滚动条、动画、链接样式。为后续所有 Task 提供样式基础。

- [ ] **Step 1: 在 `:root` 块中新增 CSS 变量**

在 `--back-btn-hover-border` 之后，`}` 之前插入：

```css
  /* 新增：消息气泡 */
  --msg-assistant-bg: linear-gradient(135deg, rgba(100,255,218,0.03) 0%, transparent 100%);
  --msg-user-shadow: 0 2px 12px rgba(100,255,218,0.06);
  /* 新增：结果卡片 */
  --result-card-shadow: 0 2px 12px rgba(0,0,0,0.15);
  /* 新增：回到底部按钮 */
  --scroll-to-bottom-bg: rgba(17, 34, 64, 0.9);
  --scroll-to-bottom-border: rgba(100, 255, 218, 0.2);
  --scroll-to-bottom-hover-border: rgba(100, 255, 218, 0.4);
  /* 新增：代码复制按钮 */
  --code-copy-btn-bg: rgba(100, 255, 218, 0.08);
  --code-copy-btn-hover-bg: rgba(100, 255, 218, 0.16);
```

- [ ] **Step 2: 在 `[data-theme="light"]` 块中新增浅色版变量**

在 `--back-btn-hover-border` 之后，`}` 之前插入：

```css
  /* 新增：消息气泡 */
  --msg-assistant-bg: linear-gradient(135deg, rgba(9,105,218,0.03) 0%, transparent 100%);
  --msg-user-shadow: 0 2px 12px rgba(9,105,218,0.06);
  /* 新增：结果卡片 */
  --result-card-shadow: 0 1px 3px rgba(31,35,40,0.12);
  /* 新增：回到底部按钮 */
  --scroll-to-bottom-bg: rgba(255, 255, 255, 0.9);
  --scroll-to-bottom-border: rgba(9, 105, 218, 0.2);
  --scroll-to-bottom-hover-border: rgba(9, 105, 218, 0.4);
  /* 新增：代码复制按钮 */
  --code-copy-btn-bg: rgba(9, 105, 218, 0.07);
  --code-copy-btn-hover-bg: rgba(9, 105, 218, 0.14);
```

- [ ] **Step 3: 优化滚动条样式**

将现有的 `::-webkit-scrollbar` 规则替换为：

```css
::-webkit-scrollbar { width: 4px; height: 4px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(136, 146, 176, 0.2); border-radius: 2px; }
::-webkit-scrollbar-thumb:hover { background: rgba(136, 146, 176, 0.45); }
```

- [ ] **Step 4: 新增链接 hover 动画**

在 `:focus-visible` 块之后添加：

```css
a { text-decoration: none; transition: text-decoration 0.2s ease; }
a:hover { text-decoration: underline; }
```

- [ ] **Step 5: 新增 slideDown 动画**

在 `@keyframes float-blob` 之后添加：

```css
@keyframes slideDown {
  from { max-height: 0; opacity: 0; }
  to   { max-height: 600px; opacity: 1; }
}
```

- [ ] **Step 6: 修改流式光标样式**

将 `.streaming-cursor::after` 的 `content` 从 `'▊'` 改为 `'|'`，并增加平滑消失过渡：

```css
.streaming-cursor::after {
  content: '|';
  animation: blink 1s step-end infinite;
  color: var(--accent-strong);
  transition: opacity 0.3s ease;
}
.streaming-cursor.fade-out::after {
  opacity: 0;
}
```

- [ ] **Step 7: 验证并提交**

```bash
cd frontend && npm run build
# 确认无编译错误
git add frontend/src/styles/global.css
git commit -m "feat: 新增 CSS 变量，优化滚动条、链接、光标动画"
```

---

### Task 2: 消息气泡视觉优化 (ChatMessage.vue)

**文件：**
- 修改：`frontend/src/components/chat/ChatMessage.vue`

**说明：** 在现有 `ChatMessage.vue` 上直接优化气泡样式、消息分组间距、时间戳显示逻辑。不拆分组件（拆分在 Task 7-9）。

- [ ] **Step 1: 添加消息分组判断逻辑**

在 `<script setup>` 中新增：

```typescript
const isConsecutive = computed(() => {
  const idx = chat.messages.indexOf(props.message as ChatMessage)
  if (idx <= 0) return false
  return chat.messages[idx - 1].role === props.message.role
})
```

- [ ] **Step 2: 修改消息容器 class 绑定**

将 `<div class="chat-message" :class="[message.role, { 'chat-message-enter': animate }]">` 改为：

```html
<div
  class="chat-message"
  :class="[
    message.role,
    { 'chat-message-enter': animate, 'chat-message-consecutive': isConsecutive }
  ]"
>
```

- [ ] **Step 3: 时间戳条件显示**

将 `<span class="msg-time">{{ formatTime(message.timestamp) }}</span>` 改为：

```html
<span v-if="!isConsecutive" class="msg-time">{{ formatTime(message.timestamp) }}</span>
```

- [ ] **Step 4: 优化助手气泡背景**

在 `.chat-message.assistant .msg-bubble` 样式中增加背景：

```css
.chat-message.assistant .msg-bubble {
  border-left: 2px solid rgba(100, 255, 218, 0.25);
  padding-left: 0.75rem;
  background: var(--msg-assistant-bg);
  border-radius: 0 8px 8px 0;
}
```

- [ ] **Step 5: 优化用户气泡阴影**

在 `.chat-message.user .msg-bubble` 样式中增加阴影：

```css
.chat-message.user .msg-bubble {
  /* ... 现有样式保持不变 ... */
  box-shadow: 0 2px 12px rgba(100, 255, 218, 0.06), var(--msg-user-shadow);
}
```

- [ ] **Step 6: 新增连续消息间距样式**

```css
.chat-message-consecutive {
  padding-top: 0.15rem;
}
.chat-message-consecutive .msg-avatar {
  visibility: hidden;
}
.chat-message-consecutive .msg-role {
  display: none;
}
```

- [ ] **Step 7: 调整行高**

将 `.msg-content` 的 `line-height` 从 `1.75` 改为 `1.7`。

- [ ] **Step 8: 验证并提交**

```bash
cd frontend && npm run build
git add frontend/src/components/chat/ChatMessage.vue
git commit -m "feat: 消息气泡层次优化 — 分组间距、助手底衬、用户阴影、时间戳合并"
```

---

### Task 3: 欢迎页空状态氛围感 + WelcomeScreen 组件

**文件：**
- 创建：`frontend/src/components/chat/WelcomeScreen.vue`
- 修改：`frontend/src/views/ChatView.vue`

**说明：** 将欢迎页提取为独立组件，增加径向光晕背景、stagger 入场动画、Chip 微交互。`ChatView.vue` 改为引用 `WelcomeScreen`。

- [ ] **Step 1: 创建 WelcomeScreen.vue**

```vue
<template>
  <div class="welcome-screen">
    <div class="ws-glow" />
    <div class="ws-content">
      <AnimatedAvatar :state="avatarState" :size="88" />
      <h2 class="ws-title">你好，我是谢浩宇的数字分身</h2>
      <p class="ws-subtitle">
        可以问我个人经历与技术栈，或让我查数据、画图表
      </p>
      <div class="ws-chips">
        <button
          v-for="(q, i) in questions"
          :key="q.question"
          class="ws-chip"
          :style="{ animationDelay: `${0.08 * i}s` }"
          :disabled="disabled"
          @click="$emit('ask', q.question)"
        >
          {{ q.question }}
        </button>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import AnimatedAvatar from '@/components/shared/AnimatedAvatar.vue'
import { WELCOME_QUESTIONS } from '@/utils/quick-questions'
import type { AvatarState } from '@/components/shared/AnimatedAvatar.vue'

defineProps<{
  avatarState?: AvatarState
  disabled?: boolean
}>()

defineEmits<{ ask: [question: string] }>()

const questions = WELCOME_QUESTIONS
</script>

<style scoped>
.welcome-screen {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}
.ws-glow {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 300px;
  height: 300px;
  border-radius: 50%;
  background: radial-gradient(
    circle,
    rgba(100, 255, 218, 0.08) 0%,
    rgba(100, 255, 218, 0.03) 40%,
    transparent 70%
  );
  pointer-events: none;
}
.ws-content {
  position: relative;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  padding: 1rem;
  gap: 0.75rem;
}
.ws-title {
  font-size: 1.25rem;
  font-weight: 700;
  color: var(--text-bright);
  margin: 0;
  letter-spacing: 0.02em;
}
.ws-subtitle {
  font-size: 0.9rem;
  color: var(--text-2);
  line-height: 1.6;
  max-width: 420px;
  margin: 0;
}
.ws-chips {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 0.5rem;
  max-width: 520px;
  margin-top: 0.5rem;
}
.ws-chip {
  font-family: var(--font-mono);
  border: 1px solid var(--chip-border);
  border-radius: var(--radius-pill);
  background: var(--chip-bg);
  color: var(--accent-strong);
  font-size: 0.75rem;
  padding: 0.45rem 0.95rem;
  cursor: pointer;
  opacity: 0;
  animation: fadeInUp 0.4s ease-out forwards;
  transition: color 0.2s, border-color 0.2s, background 0.2s, transform 0.2s, box-shadow 0.2s;
}
.ws-chip:hover:not(:disabled) {
  border-color: var(--chip-border-hover);
  background: var(--chip-bg-hover);
  transform: translateY(-2px);
  box-shadow: 0 4px 16px rgba(100, 255, 218, 0.1);
}
.ws-chip:disabled {
  opacity: 0.45;
  cursor: not-allowed;
}
</style>
```

- [ ] **Step 2: 修改 ChatView.vue 使用 WelcomeScreen**

将 `<template #empty>` 块替换为：

```html
<template #empty>
  <WelcomeScreen
    :disabled="chat.isStreaming"
    @ask="ask"
  />
</template>
```

在 `<script setup>` 中新增 import：

```typescript
import WelcomeScreen from '@/components/chat/WelcomeScreen.vue'
```

删除不再需要的 import：
```typescript
// 删除这行
import AnimatedAvatar from '@/components/shared/AnimatedAvatar.vue'
// 删除这行
import { WELCOME_QUESTIONS } from '@/utils/quick-questions'
```

删除 `<style scoped>` 中的 `.cf-empty`、`.cf-welcome`、`.chips`、`.chip` 样式块。

- [ ] **Step 3: 验证并提交**

```bash
cd frontend && npm run build
git add frontend/src/components/chat/WelcomeScreen.vue frontend/src/views/ChatView.vue
git commit -m "feat: 欢迎页空状态提取为 WelcomeScreen，增加径向光晕和 Chip 入场动画"
```

---

### Task 4: 内联结果卡片视觉优化

**文件：**
- 修改：`frontend/src/components/chat/ChatMessage.vue`

**说明：** 优化内联结果卡片的阴影、分区头部、图表占位、折叠面板过渡。在现有代码上改，不拆分（拆分在 Task 8）。

- [ ] **Step 1: 卡片阴影变量**

在 `.inline-result` 样式中增加 `box-shadow`：

```css
.inline-result {
  margin-top: 0.75rem;
  border: 1px solid var(--border);
  background: var(--bg-card);
  border-radius: 10px;
  overflow: hidden;
  box-shadow: var(--result-card-shadow);
}
```

- [ ] **Step 2: 图表容器最小高度**

在 `.ir-chart-wrap` 中增加：

```css
.ir-chart-wrap {
  padding: 0.5rem;
  min-height: 300px;
}
```

- [ ] **Step 3: 折叠面板过渡动画**

在 `.ir-section` 的 `n-collapse` 相关样式后增加：

```css
.ir-section :deep(.n-collapse-item__content-wrapper) {
  transition: max-height 0.3s ease, opacity 0.3s ease;
}
```

- [ ] **Step 4: 验证并提交**

```bash
cd frontend && npm run build
git add frontend/src/components/chat/ChatMessage.vue
git commit -m "feat: 内联结果卡片阴影、图表占位、折叠面板过渡"
```

---

### Task 5: 动效优化 — 消息入场 + 流式光标 + Tool 切换

**文件：**
- 修改：`frontend/src/components/chat/ChatMessage.vue`

**说明：** 优化消息入场动画（首条 vs 后续区分）、流式光标平滑消失、Tool 状态切换微动效。

- [ ] **Step 1: 区分首条和后续消息入场动画**

在 `<script setup>` 中新增：

```typescript
const isFirstAssistantMessage = computed(() => {
  const idx = chat.messages.indexOf(props.message as ChatMessage)
  return idx === 0 || (props.message.role === 'assistant' &&
    chat.messages.slice(0, idx).every(m => m.role !== 'assistant'))
})
```

修改模板中的 `chat-message-enter` class 绑定：

```html
:class="[message.role, { 'chat-message-enter': animate && !isFirstAssistantMessage, 'chat-message-enter-first': animate && isFirstAssistantMessage }]"
```

- [ ] **Step 2: 新增入场动画样式**

```css
.chat-message-enter-first {
  animation: fadeInUp 0.4s ease-out;
}
.chat-message-enter {
  animation: fadeIn 0.2s ease-out;
}
```

- [ ] **Step 3: 流式光标平滑消失**

在 `ChatMessage.vue` 中，监听 `isStreaming` 变化，在结束时添加 `fade-out` class：

```typescript
const cursorFading = ref(false)

watch(() => props.isStreaming, (now, prev) => {
  if (prev && !now) {
    // streaming 刚结束，光标闪烁一次后消失
    cursorFading.value = true
    setTimeout(() => { cursorFading.value = false }, 300)
  }
})
```

修改模板中 `streaming-cursor` class：

```html
:class="{ 'streaming-cursor': isStreaming || cursorFading, 'fade-out': cursorFading }"
```

- [ ] **Step 4: Tool 标签入场动画**

在 `.irt-step` 样式中增加入场动画：

```css
.irt-step {
  display: flex;
  gap: 0.5rem;
  animation: fadeIn 0.25s ease-out;
}
```

- [ ] **Step 5: 验证并提交**

```bash
cd frontend && npm run build
git add frontend/src/components/chat/ChatMessage.vue
git commit -m "feat: 消息入场分级、流式光标平滑消失、Tool 标签入场"
```

---

### Task 6: 自动滚动 — "回到底部"浮动按钮

**文件：**
- 修改：`frontend/src/components/chat/ChatWidget.vue`

**说明：** 当用户手动上翻超过 200px 时显示浮动按钮，点击后平滑滚回底部。

- [ ] **Step 1: 添加滚动状态检测**

在 `<script setup>` 中新增：

```typescript
import { ref, onMounted, onBeforeUnmount } from 'vue'

const showScrollButton = ref(false)

function onScroll() {
  const el = messagesRef.value
  if (!el) return
  showScrollButton.value = el.scrollHeight - el.scrollTop - el.clientHeight > 200
}

function scrollToBottom() {
  const el = messagesRef.value
  if (!el) return
  el.scrollTo({ top: el.scrollHeight, behavior: 'smooth' })
}

onMounted(() => {
  messagesRef.value?.addEventListener('scroll', onScroll, { passive: true })
})
onBeforeUnmount(() => {
  messagesRef.value?.removeEventListener('scroll', onScroll)
})
```

- [ ] **Step 2: 在模板中新增浮动按钮**

在 `<div class="messages-area" ref="messagesRef">` 内部末尾（`</div>` 之前）添加：

```html
<button
  v-if="showScrollButton"
  class="scroll-to-bottom"
  aria-label="回到底部"
  @click="scrollToBottom"
>
  <svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
    <polyline points="6 9 12 15 18 9"/>
  </svg>
</button>
```

- [ ] **Step 3: 新增按钮样式**

```css
.scroll-to-bottom {
  position: sticky;
  bottom: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 36px;
  height: 36px;
  margin: 0 auto;
  border: 1px solid var(--scroll-to-bottom-border);
  border-radius: 50%;
  background: var(--scroll-to-bottom-bg);
  color: var(--accent-strong);
  cursor: pointer;
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  animation: fadeIn 0.2s ease-out;
  transition: border-color 0.2s, transform 0.2s;
  z-index: 10;
}
.scroll-to-bottom:hover {
  border-color: var(--scroll-to-bottom-hover-border);
  transform: translateY(-2px);
}
```

- [ ] **Step 4: 验证并提交**

```bash
cd frontend && npm run build
git add frontend/src/components/chat/ChatWidget.vue
git commit -m "feat: 新增回到底部浮动按钮，上翻超过200px时显示"
```

---

### Task 7: 代码块一键复制

**文件：**
- 修改：`frontend/src/utils/markdown.ts`

**说明：** 在 MarkdownIt 渲染的 `<pre>` 代码块中注入复制按钮，通过 `highlight` 回调包装。

- [ ] **Step 1: 修改 markdown.ts 注入复制按钮**

```typescript
/** Shared MarkdownIt instance.  Created once at module level so every
 * ChatMessage component reuses the same parser (including highlight.js). */
import MarkdownIt from 'markdown-it'
import hljs from 'highlight.js'
import 'highlight.js/styles/github-dark.css'

const md = new MarkdownIt({
  html: false,
  linkify: true,
  breaks: true,
  highlight(str: string, lang: string) {
    const langLabel = lang || ''
    if (lang && hljs.getLanguage(lang)) {
      try {
        const highlighted = hljs.highlight(str, { language: lang }).value
        return wrapCodeBlock(highlighted, langLabel)
      } catch {
        /* ignore */
      }
    }
    // 无语言或无高亮时，用 escapeHtml 包裹
    return wrapCodeBlock(md.utils.escapeHtml(str), langLabel)
  },
})

function wrapCodeBlock(code: string, lang: string): string {
  const langTag = lang ? `<span class="code-lang">${md.utils.escapeHtml(lang)}</span>` : ''
  const copyBtn = `<button class="code-copy-btn" onclick="
    const btn=this;
    const code=this.parentElement.querySelector('code').textContent;
    navigator.clipboard.writeText(code).then(()=>{
      btn.innerHTML='✓';
      btn.classList.add('copied');
      setTimeout(()=>{btn.innerHTML='<svg viewBox=\\'0 0 24 24\\' width=\\'14\\' height=\\'14\\'><path fill=\\'currentColor\\' d=\\'M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z\\'/></svg>';
      btn.classList.remove('copied');
    },1500)
  " title="复制代码">
    <svg viewBox="0 0 24 24" width="14" height="14">
      <path fill="currentColor" d="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z"/>
    </svg>
  </button>`
  return `<div class="code-block-wrapper">${langTag}${copyBtn}<pre><code>${code}</code></pre></div>`
}

// Only allow http: and https: links to prevent XSS via javascript: / data: URIs
const defaultLinkRender =
  md.renderer.rules.link_open ||
  function (tokens, idx, options, _env, self) {
    return self.renderToken(tokens, idx, options)
  }
md.renderer.rules.link_open = function (tokens, idx, options, env, self) {
  const href = tokens[idx].attrGet('href')
  if (href) {
    const lower = href.trim().toLowerCase()
    if (!lower.startsWith('http://') && !lower.startsWith('https://')) {
      tokens[idx].attrSet('href', '')
      tokens[idx].attrSet('title', `[blocked: ${lower.slice(0, 40)}]`)
    }
  }
  return defaultLinkRender(tokens, idx, options, env, self)
}

export function renderMarkdown(text: string): string {
  return md.render(text)
}
```

- [ ] **Step 2: 在 global.css 中新增代码块包装器样式**

在 `global.css` 末尾追加：

```css
/* 代码块包装器 */
.code-block-wrapper {
  position: relative;
  margin: 0.6em 0;
}
.code-block-wrapper .code-lang {
  position: absolute;
  top: 0;
  left: 0;
  padding: 0.2em 0.6em;
  font-size: 0.7rem;
  font-family: var(--font-mono);
  color: var(--text-3);
  background: rgba(100, 255, 218, 0.06);
  border-radius: 0 0 6px 0;
  pointer-events: none;
}
.code-block-wrapper .code-copy-btn {
  position: absolute;
  top: 6px;
  right: 6px;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 28px;
  height: 28px;
  border: 1px solid transparent;
  border-radius: 6px;
  background: var(--code-copy-btn-bg);
  color: var(--text-3);
  cursor: pointer;
  opacity: 0;
  transition: opacity 0.2s, background 0.2s, color 0.2s, border-color 0.2s;
  z-index: 2;
}
.code-block-wrapper:hover .code-copy-btn {
  opacity: 1;
}
.code-block-wrapper .code-copy-btn:hover {
  background: var(--code-copy-btn-hover-bg);
  color: var(--accent-strong);
  border-color: var(--accent-border);
}
.code-block-wrapper .code-copy-btn.copied {
  color: var(--accent-strong);
  font-weight: 700;
  font-size: 0.85rem;
}
```

- [ ] **Step 3: 验证并提交**

```bash
cd frontend && npm run build
git add frontend/src/utils/markdown.ts frontend/src/styles/global.css
git commit -m "feat: 代码块一键复制按钮 + 语言标签"
```

---

### Task 8: 停止生成体验优化

**文件：**
- 修改：`frontend/src/components/chat/ChatWidget.vue`
- 修改：`frontend/src/stores/chat.ts`

**说明：** 停止后保留已生成内容，消息底部显示"已停止生成"提示，可重试。

- [ ] **Step 1: 修改 chat store 的 stopStreaming**

在 `stopStreaming()` 函数中，标记停止状态：

```typescript
const wasStopped = ref(false)

function stopStreaming() {
  abortController.value?.abort()
  wasStopped.value = true
}
```

将 `wasStopped` 加入 return：

```typescript
return {
  messages,
  currentTrace,
  isStreaming,
  streamError,
  currentTool,
  avatarState,
  hasData,
  wasStopped,
  sendMessage,
  stopStreaming,
  clearChat,
}
```

- [ ] **Step 2: 修改 sendMessage 重置 wasStopped**

在 `sendMessage` 开始处添加：

```typescript
wasStopped.value = false
```

- [ ] **Step 3: 修改 ChatWidget.vue 传递 wasStopped**

在 `ChatMessage` 组件中增加 prop：

```html
<ChatMessage
  v-for="msg in chat.messages"
  :key="msg.id"
  :message="msg"
  :is-streaming="chat.isStreaming && msg === chat.messages[chat.messages.length - 1] && msg.role === 'assistant'"
  :was-stopped="chat.wasStopped && msg === chat.messages[chat.messages.length - 1] && msg.role === 'assistant'"
/>
```

- [ ] **Step 4: 修改 ChatMessage.vue 显示停止提示**

新增 prop：

```typescript
const props = defineProps<{
  message: ChatMessage
  isStreaming?: boolean
  wasStopped?: boolean
}>()
```

在气泡内容下方、操作按钮上方增加停止提示：

```html
<div v-if="wasStopped && message.content" class="msg-stopped-hint">
  已停止生成
  <button class="action-btn retry-btn" @click="retry">
    <n-icon size="14"><svg viewBox="0 0 24 24"><path fill="currentColor" d="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z"/></svg></n-icon>
    重试
  </button>
</div>
```

新增样式：

```css
.msg-stopped-hint {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-top: 0.5rem;
  font-size: 0.78rem;
  color: var(--text-3);
}
.retry-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.25rem;
  color: var(--accent-strong);
  font-size: 0.78rem;
}
```

- [ ] **Step 5: 验证并提交**

```bash
cd frontend && npm run build
git add frontend/src/components/chat/ChatWidget.vue frontend/src/stores/chat.ts frontend/src/components/chat/ChatMessage.vue
git commit -m "feat: 停止生成后保留内容 + 显示提示 + 可重试"
```

---

### Task 9: 移动端适配 + 键盘操作

**文件：**
- 修改：`frontend/src/components/chat/ChatInput.vue`
- 修改：`frontend/src/views/ChatView.vue`
- 修改：`frontend/src/components/chat/ChatMessage.vue`

**说明：** 移动端标题栏简化、输入区优化、消息列表字体调整、Escape 清空输入框。

- [ ] **Step 1: ChatInput.vue — Escape 清空输入框**

在 `handleEnter` 函数后新增：

```typescript
function onKeydown(e: KeyboardEvent) {
  if (e.key === 'Escape' && text.value) {
    text.value = ''
    inputRef.value?.focus()
  }
}
```

在模板的 `<n-input>` 上绑定：

```html
@keydown="onKeydown"
```

- [ ] **Step 2: ChatInput.vue — 移动端优化**

修改 `autosize` 属性，在移动端减少行数：

```html
:autosize="{ minRows: 1, maxRows: 5 }"
```

改为在移动端响应式处理，通过 CSS 控制。在 `<style scoped>` 中新增：

```css
@media (max-width: 640px) {
  .chat-input-wrapper {
    width: 100%;
  }
  .input-container {
    padding: 0.3rem 0.4rem 0.3rem 0.9rem;
    border-radius: 22px;
  }
  .input-hint {
    display: none;
  }
}
```

- [ ] **Step 3: ChatView.vue — 移动端标题栏简化**

现有的 `@media (max-width: 640px)` 已隐藏 `.cf-subtitle`。增加：

```css
@media (max-width: 640px) {
  .cf-bar { padding: 0.6rem 0.8rem; }
  .cf-subtitle { display: none; }
  .cf-title { font-size: 0.85rem; }
  .cf-back span { display: none; }
}
```

- [ ] **Step 4: ChatMessage.vue — 移动端消息列表**

在 `<style scoped>` 中新增：

```css
@media (max-width: 640px) {
  .chat-message { padding: 0.75rem 0; gap: 0.5rem; }
  .chat-message-consecutive { padding-top: 0.1rem; }
  .msg-content { font-size: 0.85rem; line-height: 1.65; }
  .chat-message.user .msg-bubble { max-width: 85%; }
  .msg-actions { opacity: 1; }
  .ir-table-wrap { max-height: 200px; overflow: auto; }
  .ir-chart-wrap { min-height: 220px; }
}
```

- [ ] **Step 5: 验证并提交**

```bash
cd frontend && npm run build
git add frontend/src/components/chat/ChatInput.vue frontend/src/views/ChatView.vue frontend/src/components/chat/ChatMessage.vue
git commit -m "feat: 移动端适配 + Escape 清空输入框"
```

---

### Task 10: 错误状态 + 消息操作按钮优化

**文件：**
- 修改：`frontend/src/components/chat/ChatMessage.vue`
- 修改：`frontend/src/stores/chat.ts`

**说明：** 错误消息区分类型、重试按钮更醒目、移动端操作按钮始终可见（已在 Task 9 的 CSS 中处理）。

- [ ] **Step 1: 修改 chat store 错误分类**

在 `sendMessage` 的 `catch` 块中，区分错误类型：

```typescript
} catch (err: any) {
  if (err.name === 'AbortError') {
    assistantMsg.content = assistantMsg.content || '请求已取消'
  } else if (err.message?.includes('timeout') || err.message?.includes('超时')) {
    streamError.value = '请求超时'
    assistantMsg.content = '请求超时，请稍后重试。'
  } else if (err.message?.includes('fetch') || err.message?.includes('network') || err.message?.includes('Network')) {
    streamError.value = '网络错误'
    assistantMsg.content = '网络连接失败，请检查网络后重试。'
  } else {
    streamError.value = err.message || 'AI 服务异常'
    assistantMsg.content = `AI 服务暂时不可用，请稍后重试。`
  }
  assistantMsg.error = true
}
```

- [ ] **Step 2: 优化重试按钮样式**

在 `.action-btn` 样式后新增：

```css
.action-btn.retry-btn {
  color: var(--accent-strong);
  border: 1px solid var(--accent-border);
  background: rgba(100, 255, 218, 0.06);
  gap: 0.3rem;
  padding: 0 0.6rem;
  width: auto;
  border-radius: 6px;
}
.action-btn.retry-btn:hover {
  background: rgba(100, 255, 218, 0.12);
  border-color: var(--accent-strong);
}
```

- [ ] **Step 3: 验证并提交**

```bash
cd frontend && npm run build
git add frontend/src/stores/chat.ts frontend/src/components/chat/ChatMessage.vue
git commit -m "feat: 错误消息区分类型 + 重试按钮醒目化"
```

---

### Task 11: 组件拆分 — MessageBubble + InlineResult

**文件：**
- 创建：`frontend/src/components/chat/MessageBubble.vue`
- 创建：`frontend/src/components/chat/InlineResult.vue`
- 修改：`frontend/src/components/chat/ChatMessage.vue`

**说明：** 将 `ChatMessage.vue` 中的气泡渲染逻辑和内联结果逻辑提取为独立组件，减少单文件复杂度。

- [ ] **Step 1: 创建 MessageBubble.vue**

```vue
<template>
  <div class="msg-bubble" :class="{ 'msg-bubble-error': isError }">
    <div
      class="msg-content"
      :class="{ 'streaming-cursor': isStreaming, 'fade-out': cursorFading }"
      v-html="renderedContent"
    />
  </div>
</template>

<script setup lang="ts">
import { computed, ref, watch } from 'vue'
import { renderMarkdown } from '@/utils/markdown'

const props = defineProps<{
  content: string
  isError?: boolean
  isStreaming?: boolean
}>()

const renderedContent = computed(() => renderMarkdown(props.content))

const cursorFading = ref(false)

watch(() => props.isStreaming, (now, prev) => {
  if (prev && !now) {
    cursorFading.value = true
    setTimeout(() => { cursorFading.value = false }, 300)
  }
})
</script>

<style scoped>
.msg-bubble {
  position: relative;
  border-radius: 12px;
  overflow: hidden;
  overflow-wrap: break-word;
}
.msg-bubble-error {
  background: rgba(255, 80, 80, 0.06);
  border: 1px solid rgba(255, 80, 80, 0.2);
  border-radius: 12px;
  padding: 0.6rem 0.9rem;
}
.msg-bubble-error .msg-content { color: #ff8080; }
.msg-content {
  font-size: 0.875rem;
  line-height: 1.7;
  color: var(--text-1);
  word-break: break-word;
}
.msg-content :deep(p) { margin: 0.5em 0; }
.msg-content :deep(p:first-child) { margin-top: 0; }
.msg-content :deep(pre) { border-radius: 10px; overflow-x: auto; margin: 0.6em 0; }
.msg-content :deep(code) { font-size: 0.84rem; }
.msg-content :deep(blockquote) { border-left: 3px solid var(--accent-strong); padding-left: 0.8rem; margin: 0.5em 0; color: var(--text-2); }
.msg-content :deep(a) { color: var(--accent-strong); text-decoration: none; }
.msg-content :deep(a:hover) { text-decoration: underline; }
.msg-content :deep(table) { border-collapse: collapse; margin: 0.5em 0; width: 100%; }
.msg-content :deep(th), .msg-content :deep(td) { border: 1px solid var(--border); padding: 0.4rem 0.7rem; font-size: 0.85rem; text-align: left; }
.msg-content :deep(th) { background: var(--bg-subtle); }
.msg-content :deep(ul), .msg-content :deep(ol) { padding-left: 1.5em; }
.msg-content :deep(li) { margin: 0.2em 0; }
</style>
```

- [ ] **Step 2: 创建 InlineResult.vue**

将 `ChatMessage.vue` 中内联结果部分（图表 + 数据表 + 轨迹）提取为 `InlineResult.vue`。完整代码：

```vue
<template>
  <div class="inline-result">
    <!-- Chart -->
    <div v-if="chartJson" class="ir-section">
      <div class="ir-section-header">
        <n-icon size="16" color="#64ffda"><svg viewBox="0 0 24 24"><path fill="currentColor" d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zM9 17H7v-7h2v7zm4 0h-2V7h2v10zm4 0h-2v-4h2v4z"/></svg></n-icon>
        <span>图表</span>
        <n-tag size="tiny" :bordered="false" type="success">{{ chartTypeLabel }}</n-tag>
      </div>
      <div class="ir-chart-wrap">
        <ChartRenderer :figure-json="chartJson" />
      </div>
    </div>

    <!-- Data table -->
    <div v-if="dataArtifact" class="ir-section">
      <div class="ir-section-header">
        <n-icon size="16" color="#64b5f6"><svg viewBox="0 0 24 24"><path fill="currentColor" d="M4 6h16v2H4zm0 5h16v2H4zm0 5h16v2H4z"/></svg></n-icon>
        <span>数据表</span>
        <n-tag size="tiny" :bordered="false">{{ rowsCols }}</n-tag>
      </div>
      <n-collapse :default-expanded-names="[]">
        <n-collapse-item title="SQL 语句" name="sql">
          <n-code :code="dataArtifact.sql || '(无)'" language="sql" :word-wrap="true" />
        </n-collapse-item>
      </n-collapse>
      <div class="ir-table-wrap">
        <n-data-table :columns="columns" :data="rows" :max-height="260" size="small" :bordered="false" striped virtual-scroll />
      </div>
    </div>

    <!-- Trace: collapsible -->
    <n-collapse :default-expanded-names="[]">
      <n-collapse-item name="trace">
        <template #header>
          <div class="ir-section-header">
            <n-icon size="16" color="#ffb86c"><svg viewBox="0 0 24 24"><path fill="currentColor" d="M19 3h-4.18C14.4 1.84 13.3 1 12 1c-1.3 0-2.4.84-2.82 2H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-7 0c.55 0 1 .45 1 1s-.45 1-1 1-1-.45-1-1 .45-1 1-1zm2 14H7v-2h7v2zm3-4H7v-2h10v2zm0-4H7V7h10v2z"/></svg></n-icon>
            <span>执行轨迹</span>
            <n-tag size="tiny" :bordered="false" type="warning">{{ trace.length }} 步</n-tag>
          </div>
        </template>
        <div class="ir-trace">
          <div v-for="(step, i) in trace" :key="i" class="irt-step">
            <div class="irt-line">
              <div class="irt-dot" :style="{ background: stepColor(step.tool) }" />
              <div v-if="i < trace.length - 1" class="irt-connector" />
            </div>
            <div class="irt-body">
              <div class="irt-header">
                <n-tag size="tiny" :bordered="false" :type="tagType(step.tool)" round>{{ toolLabel(step.tool) }}</n-tag>
                <span class="irt-num">Step {{ i + 1 }}</span>
              </div>
              <div class="irt-summary">{{ step.summary }}</div>
            </div>
          </div>
        </div>
      </n-collapse-item>
    </n-collapse>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import ChartRenderer from '@/components/result/ChartRenderer.vue'
import type { ToolTrace } from '@/stores/chat'
import { CHART_LABELS, toolLabel, tagType, stepColor } from '@/utils/tool-constants'
import { findDataArtifact, findChartArtifact } from '@/utils/artifact'

const props = defineProps<{ trace: ToolTrace[] }>()

const dataArtifact = computed(() => findDataArtifact(props.trace))
const chartArtifact = computed(() => findChartArtifact(props.trace))
const chartJson = computed(() => chartArtifact.value?.figure_json || null)
const columns = computed(() => {
  const cols = dataArtifact.value?.df_columns || []
  return cols.map((c: string) => ({ title: c, key: c, ellipsis: { tooltip: true }, minWidth: 80, maxWidth: 300 }))
})
const rows = computed(() => {
  if (!dataArtifact.value?.df_json) return []
  try { return JSON.parse(dataArtifact.value.df_json) } catch { return [] }
})
const rowsCols = computed(() => {
  const a = dataArtifact.value
  return a?.df_shape ? `${a.df_shape.rows}×${a.df_shape.cols}` : '--'
})
const chartTypeLabel = computed(() => {
  const t = chartArtifact.value?.chart_type || ''
  return CHART_LABELS[t] || t || '--'
})
</script>

<style scoped>
.inline-result {
  margin-top: 0.75rem;
  border: 1px solid var(--border);
  background: var(--bg-card);
  border-radius: 10px;
  overflow: hidden;
  box-shadow: var(--result-card-shadow);
}
.ir-section {
  border-bottom: 1px solid var(--border);
}
.ir-section:last-child {
  border-bottom: none;
}
.ir-section-header {
  display: flex; align-items: center; gap: 0.5rem;
  padding: 0.55rem 0.8rem; font-size: 0.83rem; color: var(--text-2);
  background: var(--bg-subtle);
}
.ir-chart-wrap { padding: 0.5rem; min-height: 300px; }
.ir-trace { padding: 0.5rem 0.8rem; }
.irt-step { display: flex; gap: 0.5rem; animation: fadeIn 0.25s ease-out; }
.irt-line { display: flex; flex-direction: column; align-items: center; width: 12px; flex-shrink: 0; }
.irt-dot { width: 8px; height: 8px; border-radius: 50%; margin-top: 5px; }
.irt-connector { width: 1px; flex: 1; background: var(--border); margin: 3px 0; }
.irt-body { flex: 1; padding: 0.2rem 0 0.7rem; }
.irt-header { display: flex; align-items: center; gap: 0.4rem; margin-bottom: 0.2rem; }
.irt-num { font-size: 0.68rem; color: var(--text-3); }
.irt-summary { font-size: 0.78rem; color: var(--text-2); line-height: 1.4; word-break: break-word; }
.ir-section :deep(.n-collapse-item__content-wrapper) {
  transition: max-height 0.3s ease, opacity 0.3s ease;
}
</style>
```

- [ ] **Step 3: 精简 ChatMessage.vue**

重构 `ChatMessage.vue`，使用 `MessageBubble` 和 `InlineResult` 子组件。模板简化为：

```vue
<template>
  <div class="chat-message" :class="[message.role, { 'chat-message-enter': animate && !isFirstAssistant, 'chat-message-enter-first': animate && isFirstAssistant, 'chat-message-consecutive': isConsecutive }]">
    <div class="msg-avatar">
      <n-avatar v-if="message.role === 'user'" :size="34" round :style="{ background: 'linear-gradient(135deg, #6366f1, #818cf8)' }">
        <n-icon size="18"><svg viewBox="0 0 24 24"><path fill="currentColor" d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg></n-icon>
      </n-avatar>
      <AnimatedAvatar v-else :state="avatarState" :size="34" />
    </div>

    <div class="msg-body">
      <div class="msg-role">
        {{ message.role === 'user' ? '你' : 'Xiehaoyu-Agent' }}
        <span v-if="!isConsecutive" class="msg-time">{{ formatTime(message.timestamp) }}</span>
      </div>

      <template v-if="message.content">
        <div class="msg-bubble-wrap" :class="{ 'msg-bubble-user': message.role === 'user', 'msg-bubble-assistant': message.role === 'assistant' }">
          <MessageBubble :content="message.content" :is-error="isError" :is-streaming="!!isStreaming" />
        </div>
      </template>
      <div v-else class="msg-loading">
        <!-- 加载状态保持不变 -->
        <div class="loading-row">
          <n-spin :size="16" />
          <span class="msg-tool-status">
            <template v-if="isStreaming && chat.currentTool">
              正在调用「{{ toolLabel(chat.currentTool) }}」<span class="dot-anim">…</span>
            </template>
            <template v-else-if="isStreaming">
              正在思考<span class="dot-anim">…</span>
            </template>
          </span>
        </div>
        <div v-if="isStreaming && chat.currentTrace.length > 0" class="live-trace">
          <div v-for="(step, i) in chat.currentTrace" :key="i" class="irt-step">
            <div class="irt-line">
              <div class="irt-dot" :style="{ background: stepColor(step.tool) }" />
              <div v-if="i < chat.currentTrace.length - 1 || chat.currentTool" class="irt-connector" :class="{ flow: i === chat.currentTrace.length - 1 }" />
            </div>
            <div class="irt-body">
              <div class="irt-header">
                <n-tag size="tiny" :bordered="false" :type="tagType(step.tool)" round>{{ toolLabel(step.tool) }}</n-tag>
                <span class="irt-num">Step {{ i + 1 }}</span>
              </div>
              <div class="irt-summary">{{ step.summary }}</div>
            </div>
          </div>
        </div>
      </div>

      <!-- 停止提示 -->
      <div v-if="wasStopped && message.content" class="msg-stopped-hint">
        已停止生成
        <button class="action-btn retry-btn" @click="retry">
          <n-icon size="14"><svg viewBox="0 0 24 24"><path fill="currentColor" d="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z"/></svg></n-icon>
          重试
        </button>
      </div>

      <!-- 操作按钮 -->
      <div v-if="message.role === 'assistant' && !isStreaming && message.content" class="msg-actions">
        <button class="action-btn" aria-label="复制内容" title="复制" @click="copyContent">
          <n-icon size="14"><svg viewBox="0 0 24 24"><path fill="currentColor" d="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z"/></svg></n-icon>
        </button>
        <button v-if="isError" class="action-btn retry-btn" aria-label="重试" title="重试" @click="retry">
          <n-icon size="14"><svg viewBox="0 0 24 24"><path fill="currentColor" d="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z"/></svg></n-icon>
          重试
        </button>
        <template v-if="message.steps">
          <n-tag size="tiny" :bordered="false" type="info" round>{{ message.steps }} 步</n-tag>
          <n-tag v-for="t in message.tools" :key="t" size="tiny" :bordered="false" round>{{ toolLabel(t) }}</n-tag>
        </template>
      </div>

      <!-- 内联结果 -->
      <InlineResult v-if="message.role === 'assistant' && message.trace && message.trace.length > 0 && !isStreaming" :trace="message.trace" />
    </div>
  </div>
</template>
```

- [ ] **Step 4: 更新 ChatMessage.vue 的 script**

```typescript
import { computed, ref, onMounted } from 'vue'
import { useMessage } from 'naive-ui'
import AnimatedAvatar from '@/components/shared/AnimatedAvatar.vue'
import MessageBubble from './MessageBubble.vue'
import InlineResult from './InlineResult.vue'
import type { ChatMessage } from '@/stores/chat'
import { useChatStore } from '@/stores/chat'
import { toolLabel, tagType, stepColor } from '@/utils/tool-constants'
import type { AvatarState } from '@/components/shared/AnimatedAvatar.vue'

const props = defineProps<{
  message: ChatMessage
  isStreaming?: boolean
  wasStopped?: boolean
}>()

const chat = useChatStore()
const messageApi = useMessage()
const animate = ref(true)

const isLastAssistant = computed(() =>
  props.message.role === 'assistant' &&
  props.message === chat.messages[chat.messages.length - 1]
)

const isFirstAssistant = computed(() => {
  const idx = chat.messages.indexOf(props.message as ChatMessage)
  return idx === 0
})

const isConsecutive = computed(() => {
  const idx = chat.messages.indexOf(props.message as ChatMessage)
  if (idx <= 0) return false
  return chat.messages[idx - 1].role === props.message.role
})

const avatarState = computed<AvatarState>(() => {
  if (!isLastAssistant.value) return 'idle'
  return chat.avatarState
})

const isError = computed(() => props.message.error === true)

function retry() { /* 保持不变 */ }
function formatTime(ts: number) { /* 保持不变 */ }
async function copyContent() { /* 保持不变 */ }

onMounted(() => {
  setTimeout(() => { animate.value = false }, 300)
})
```

- [ ] **Step 5: 简化 ChatMessage.vue 样式**

保留消息容器、头像、角色名、加载状态、操作按钮、停止提示的样式。删除气泡样式（移到 MessageBubble）、内联结果样式（移到 InlineResult）。新增：

```css
.msg-bubble-wrap { position: relative; }
.msg-bubble-assistant {
  border-left: 2px solid rgba(100, 255, 218, 0.25);
  padding-left: 0.75rem;
  background: var(--msg-assistant-bg);
  border-radius: 0 8px 8px 0;
}
.msg-bubble-user {
  background: linear-gradient(135deg, rgba(100,255,218,0.14) 0%, rgba(100,255,218,0.06) 100%);
  border: 1px solid rgba(100,255,218,0.18);
  box-shadow: var(--msg-user-shadow);
  padding: 0.75rem 1rem;
  max-width: 72%;
  border-radius: 18px 18px 4px 18px;
  margin-left: auto;
}
```

- [ ] **Step 6: 验证并提交**

```bash
cd frontend && npm run build
git add frontend/src/components/chat/
git commit -m "refactor: 拆分 ChatMessage 为 MessageBubble + InlineResult 子组件"
```

---

### Task 12: 最终验证 + 回归测试

**文件：**
- 所有前述文件

**说明：** 最终全量构建验证，确保深色/浅色主题均正常，移动端布局正确。

- [ ] **Step 1: 构建验证**

```bash
cd frontend && npm run build
```

预期：无 TypeScript 错误，无构建警告。

- [ ] **Step 2: 启动后端 + 前端联调**

```bash
# 终端 1
cd d:/Users/Desktop/Xiehaoyu-agent/xiehaoyu-agent
uvicorn backend.app.main:app --port 8000

# 终端 2
cd frontend && npm run dev
```

- [ ] **Step 3: 手动验证清单**

| 验证项 | 操作 |
|---|---|
| 深色主题 | 打开 http://localhost:5173/chat，确认所有视觉元素正常 |
| 浅色主题 | 切换浅色主题，确认气泡、卡片、阴影正确 |
| 欢迎页 | 空状态显示径向光晕 + Chip 入场动画 |
| 发送消息 | 输入"介绍一下你自己"，确认流式输出 + 光标 |
| 气泡分组 | 连续发送多条消息，确认同角色消息合并 + 头像隐藏 |
| 代码复制 | 输入"帮我写一段 Python 代码"，hover 代码块确认复制按钮 |
| 回到底部 | 发送多条消息后上翻，确认按钮出现，点击确认滚回 |
| 停止生成 | 发送消息后立即点击停止，确认内容保留 + 提示 |
| 重试 | 点击重试按钮，确认重新发送 |
| 移动端 | Chrome DevTools 切换到 375px 宽度，确认标题栏简化、输入区适配 |
| Escape | 输入文字后按 Escape，确认清空输入框 |

- [ ] **Step 4: 提交最终版本**

```bash
git add -A
git commit -m "feat: AI聊天助手前端全面优化完成

视觉：气泡层次、欢迎页氛围、结果卡片、间距排版
动效：消息入场分级、流式光标平滑消失、Tool标签入场、回到底部按钮
交互：代码块复制、停止体验、移动端适配、Escape清空、错误分类
代码：ChatMessage拆分为MessageBubble+InlineResult，提取WelcomeScreen"
```

---

## 任务依赖关系

```
Task 1 (CSS 变量) ──────────────────────────────────────────────────────────────┐
Task 2 (气泡视觉) ───── 依赖 Task 1                                           │
Task 3 (欢迎页) ─────── 依赖 Task 1                                           │
Task 4 (结果卡片) ───── 依赖 Task 1                                           │
Task 5 (动效) ───────── 依赖 Task 1, 2                                       │
Task 6 (滚动按钮) ───── 依赖 Task 1                                           │
Task 7 (代码复制) ───── 依赖 Task 1                                           │
Task 8 (停止体验) ───── 依赖 Task 1, 2                                       │
Task 9 (移动端) ─────── 依赖 Task 1, 2, 3                                    │
Task 10 (错误状态) ──── 依赖 Task 1, 2, 8                                    │
Task 11 (组件拆分) ──── 依赖 Task 1-10 全部完成                               │
Task 12 (最终验证) ──── 依赖 Task 1-11 全部完成                               │
```

**推荐执行顺序**：Task 1 → Task 2, 3, 4, 6, 7 并行 → Task 5 → Task 8 → Task 9 → Task 10 → Task 11 → Task 12