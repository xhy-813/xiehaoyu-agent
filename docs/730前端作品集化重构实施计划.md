# 前端作品集化重构 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将前端重构为深色单页作品集（布局对标 brittanychiang.com v5，视觉以 `docs/参考实现稿-index.html` / `docs/参考实现稿-ai-chat.html` 为准：桌面端 fixed 左侧栏 + 右侧滚动区块——关于/经历/项目/AI 问答 Banner/极简 Footer），AI 问答以 Banner 入口呈现、点击直达 `/chat` 全屏聊天，后端改为公开访问 + IP 限流 + 全局日上限。

**Architecture:** 在现有 Vue 3 + FastAPI 工程内重构。后端先做（TDD）：删除 JWT 登录体系，`/api/chat` 公开化，限流从 JWT user_id 改为客户端 IP + 全局每日上限。前端先立深色设计系统（CSS 变量 + Naive UI darkTheme，色板与参考实现稿一致），再立布局骨架（SiteSidebar fixed 左栏 + 滚动侦测 + SectionHeading + 区块组件），然后从下到上抽取 `ChatWidget`（复用现有消息/输入/图表/SSE 全部逻辑）供 `/chat` 全屏页使用，最后组装单页作品集（聊天为 AI Banner 入口）并切换路由。

**Tech Stack:** Vue 3.5 + TypeScript + Vite 8 + Naive UI 2.44 + Pinia 4 + vue-router 4 + Plotly.js + Lottie-web / FastAPI + pytest

**设计依据:** `docs/730前端作品集化重构设计.md`（2026-07-30 已确认；2026-07-31 两次修订：先按 brittanychiang.com v5 布局，后按参考实现稿 `docs/参考实现稿-index.html` / `docs/参考实现稿-ai-chat.html` 定稿视觉——编号章节标题回归、AI Banner 入口、技术栈并入关于、极简 Footer；2026-08-01 **修订 3** 依据实机截图对齐 v5 —— 章节标题改为 sr-only、新增鼠标聚光灯 `#spotlight`、经历/项目容器加 group-hover dim、左栏改为竖向 `space-between`、经历减为 2 条、项目减为 4 卡、`.section-header` 视觉隐藏但 DOM 保留，见设计文档修订 3）

**修订 3 对本实施计划的影响（增量清单）：**
- Task 3（SiteSidebar / 视觉标志）：新增 `#spotlight` DOM 与 rAF 更新 `--mx/--my`；`.left-col` 改 `flex-direction: column; justify-content: space-between`（顶部 `.left-col-top` 包 hero+nav，底部 social-row 贴底）；≤980px + reduced-motion 下 `#spotlight { display: none }`
- Task 4（SectionHeading 组件）：`.section-header` 用 `position:absolute; width:1px; height:1px; clip:rect(0,0,0,0); overflow:hidden` 视觉隐藏，DOM 保留供 IntersectionObserver 与屏读器；后续如恢复可见标题，从组件类切换即可
- Task 5（ExperienceSection）：`data/profile.ts` 中 experience 仅保留 2 条（龙腾出行 2026.04—至今 / 吉首大学 2024.09—至今）；`.exp-year` 宽 110px + uppercase 等宽；`.exp-list:hover .exp-item { opacity: 0.5 }` + `.exp-list:hover .exp-item:hover { opacity: 1 }`
- Task 6（ProjectsSection）：`projects` 数组 4 条（Xiehaoyu-Agent / 抖音种草 / 淘宝 RFM / 股市舆情）；K12 数仓卡片从计划移到 §范围外，待真截图/看板链接回填；「你正在使用它」徽章列为 Post-launch 增强；`.proj-grid:hover .proj-card { opacity: 0.5 }` + hover 项复原
- Task 7（ChatSection）：保持 `.ai-banner` 卡片式（badge + h3 + 描述 + 实心大按钮）；不使用 inline-link 版；`.inline-cta` 相关样式与组件禁止引入
- Task 8（PortfolioView 组装）：验收增补——鼠标聚光灯桌面端可见（薄荷绿 700px radial，无 mix-blend-mode）、经历/项目 hover 时同级 dim、左栏社交图标贴底
- Task 修 bug：技能列表 `Git / Docker` 使用了错误 class `skill-git`，前端组件化时统一改为 `skill-tag`

## Global Constraints

- 文案全中文（装饰性编号用阿拉伯数字 + 等宽字体）
- 色板（verbatim）：背景 `#0a192f` / 卡片 `#112240` / 浅层 `#1d3461` / 点缀 `#64ffda` / 亮白文本 `#e6f1ff` / 主文本 `#ccd6f6` / 次文本 `#8892b0` / 结构边框 `#233554` / 点缀描边 `rgba(100,255,218,0.2)` / hover 面板 `rgba(17,34,64,0.5~0.6)`
- 视觉基准：主页与聊天页的布局/组件细节以 `docs/参考实现稿-index.html`、`docs/参考实现稿-ai-chat.html`（WorkBuddy 生成，2026-07-31 收录入 docs）为准
- 工具轨迹色：查询 `#64b5f6` / 图表 `#64ffda` / RAG `#ffb86c` / 解读 `#c792ea`
- 不新增任何 npm 依赖；不引入 Redis
- Lottie 头像与聊天组件（含 Plotly）仅出现在 `/chat` 页；作品集主页不加载聊天链路
- Plotly / Lottie 保持按需懒加载；全站遵守 `prefers-reduced-motion`
- 后端 pytest 全部通过；前端 `npm run build`（含 vue-tsc 类型检查）零错误
- 提交信息风格：conventional commits 中文（如 `feat(frontend): ...`、`refactor(backend): ...`），每个 Task 至少一次提交
- 项目根目录：`d:\Users\Desktop\Xiehaoyu-agent\xiehaoyu-agent`（以下相对路径均基于此）
- 中间态说明：Task 2 深色变量生效后，旧浅色页面（登录/旧聊天页）会出现视觉异常，**属预期**，Task 9 清理后消失；Task 8 完成时主页视觉已定稿，`/chat` 旧页在 Task 9/10 收尾

---

## 文件结构

**后端（修改/删除/新增）**

| 文件 | 动作 | 职责 |
|------|------|------|
| `configs/settings.py` | 修改 | 删 JWT/访问码配置，新增 IP/全局限流配置 |
| `.env.example` | 修改 | 同步配置项 |
| `backend/app/deps/rate_limit.py` | 重写 | 按 IP 小时限流 + 全局日上限 + `get_client_ip` |
| `backend/app/routers/chat.py` | 修改 | 移除 JWT 依赖，接入新限流 |
| `backend/app/main.py` | 修改 | 移除 auth router |
| `backend/app/dependencies.py` | 删除 | JWT 校验（不再需要） |
| `backend/app/routers/auth.py` | 删除 | 登录路由 |
| `backend/app/schemas/auth.py` | 删除 | 登录 schema |
| `tests/test_rate_limit.py` | 重写 | 新限流逻辑测试 |
| `tests/test_public_chat.py` | 新增 | 公开聊天/限流/健康检查测试 |
| `tests/test_auth.py` | 删除 | 登录测试（TestHealth 移入 test_public_chat.py） |

**前端（新增）**

| 文件 | 职责 |
|------|------|
| `src/data/profile.ts` | 全部个人内容静态数据 + 类型 |
| `src/composables/useRevealOnScroll.ts` | `v-reveal` 滚动入场指令 |
| `src/composables/useScrollSpy.ts` | 滚动侦测（当前区块 id，供左栏导航高亮） |
| `src/components/portfolio/SiteSidebar.vue` | 左侧固定栏（身份 + 横线导航[含 AI 问答高亮项] + 社交图标）；移动端顶部身份块 + 横向导航 |
| `src/components/portfolio/SectionHeading.vue` | 编号章节标题（`01.` + 标题，无装饰横线） |
| `src/components/portfolio/AboutSection.vue` | 关于（简介 v-html + 技术栈 ▹ 平铺） |
| `src/components/portfolio/ExperienceSection.vue` | 经历（时间线条目） |
| `src/components/portfolio/ProjectsSection.vue` | 项目行卡片（缩略图 + 指标行 + 胶囊） |
| `src/components/portfolio/ChatSection.vue` | AI 问答 Banner 入口（badge + 说明 + 实心大按钮，跳 `/chat`） |
| `src/components/portfolio/SiteFooter.vue` | 极简 Footer 一行 |
| `src/components/chat/ChatWidget.vue` | 可嵌入聊天（消息列表 + 输入框 + empty 插槽），供 `/chat` 使用 |
| `src/views/PortfolioView.vue` | 单页组装（fixed 左栏 + 右列 margin-left 让位） |

**前端（修改/删除）**

| 文件 | 动作 |
|------|------|
| `src/styles/global.css` | 重写（深色设计系统 + `.pill`/`.stagger`/脉冲与光斑动效） |
| `src/App.vue` | 重写（darkTheme + overrides） |
| `src/utils/tool-constants.ts` | 修改（STEP_COLORS 深色版） |
| `src/utils/markdown.ts` | 修改（hljs 主题换 github-dark） |
| `src/utils/sse.ts` | 修改（去 token 参数与 Authorization 头） |
| `src/stores/chat.ts` | 修改（去 auth 依赖） |
| `src/router/index.ts` | 重写（`/` + `/chat`，无守卫） |
| `src/components/chat/ChatMain.vue` | 重写为 ChatWidget 薄封装（Task 9 删除） |
| `src/views/ChatView.vue` | 重写为全屏聊天页（参考实现稿顶栏 + 欢迎 chips） |
| `src/components/chat/ChatMessage.vue` | 修改（深色适配） |
| `src/components/chat/ChatInput.vue` | 修改（深色适配） |
| `src/components/result/ChartRenderer.vue` | 修改（Plotly 深色 layout） |
| `index.html` | 修改（title/meta） |
| `src/views/LoginView.vue` / `src/components/chat/ChatSidebar.vue` / `src/components/chat/WelcomeCard.vue` / `src/stores/auth.ts` / `src/api/client.ts` / `src/composables/useAutoScroll.ts` | 删除（Task 9） |
| `README.md` / `deploy/xiehaoyu-agent.service` | 修改（Task 11） |

---

## Task 1: 后端公开聊天改造（公开访问 + IP 限流 + 全局日上限）

**Files:**
- Modify: `configs/settings.py`
- Modify: `.env.example`
- Rewrite: `backend/app/deps/rate_limit.py`
- Modify: `backend/app/routers/chat.py:12-16,53-75`
- Modify: `backend/app/main.py:52,71-72`
- Delete: `backend/app/dependencies.py`、`backend/app/routers/auth.py`、`backend/app/schemas/auth.py`、`tests/test_auth.py`
- Rewrite: `tests/test_rate_limit.py`
- Create: `tests/test_public_chat.py`

**Interfaces:**
- Consumes: 现有 `agent.graph.stream_run(question)` SSE 事件流（不变）
- Produces:
  - `check_rate_limit(client_ip: str = "unknown") -> None`（按 IP 小时配额，429 抛 HTTPException）
  - `check_global_daily_cap() -> None`（全局日配额，429 抛 HTTPException）
  - `get_client_ip(request: fastapi.Request) -> str`（优先取 `X-Forwarded-For` 首跳）
  - `settings.ip_hourly_quota: int`（默认 20）、`settings.global_daily_quota: int`（默认 200）
  - 限流内部状态 `_hourly_buckets: dict[str, list[float]]`、`_daily_state: dict[str, object]`（测试重置用）

- [ ] **Step 1: 重写 `tests/test_rate_limit.py`（失败测试先行）**

```python
"""Unit tests for backend/app/deps/rate_limit.py — IP quota + global daily cap."""

import os
os.environ["SKIP_CONFIG_VALIDATION"] = "1"

import time

import pytest
from fastapi import HTTPException

from backend.app.deps.rate_limit import (
    check_rate_limit,
    check_global_daily_cap,
    get_client_ip,
    _hourly_buckets,
    _daily_state,
)
import configs.settings as cfg


@pytest.fixture(autouse=True)
def _clean_state():
    _hourly_buckets.clear()
    _daily_state["date"] = ""
    _daily_state["count"] = 0
    yield
    _hourly_buckets.clear()
    _daily_state["date"] = ""
    _daily_state["count"] = 0


class TestIpHourlyQuota:
    def test_first_request_does_not_raise(self):
        check_rate_limit("1.2.3.4")

    def test_exceeding_quota_raises_429(self):
        quota = cfg.settings.ip_hourly_quota
        for _ in range(quota):
            check_rate_limit("1.2.3.4")
        with pytest.raises(HTTPException) as exc_info:
            check_rate_limit("1.2.3.4")
        assert exc_info.value.status_code == 429
        assert "本小时" in exc_info.value.detail

    def test_ips_are_isolated(self):
        quota = cfg.settings.ip_hourly_quota
        for _ in range(quota):
            check_rate_limit("1.1.1.1")
        with pytest.raises(HTTPException):
            check_rate_limit("1.1.1.1")
        check_rate_limit("2.2.2.2")  # 另一个 IP 不受影响

    def test_expired_entries_are_cleaned(self):
        now = time.time()
        _hourly_buckets["3.3.3.3"] = [now - 3700, now - 100]
        check_rate_limit("3.3.3.3")
        assert len(_hourly_buckets["3.3.3.3"]) == 2  # 过期 1 条被清 + 新增 1 条


class TestGlobalDailyCap:
    def test_under_cap_does_not_raise(self):
        check_global_daily_cap()
        assert _daily_state["count"] == 1

    def test_exceeding_cap_raises_429(self):
        _daily_state["date"] = time.strftime("%Y-%m-%d")
        _daily_state["count"] = cfg.settings.global_daily_quota
        with pytest.raises(HTTPException) as exc_info:
            check_global_daily_cap()
        assert exc_info.value.status_code == 429
        assert "今日体验名额已用完" in exc_info.value.detail

    def test_date_rollover_resets_count(self):
        _daily_state["date"] = "2000-01-01"
        _daily_state["count"] = cfg.settings.global_daily_quota
        check_global_daily_cap()  # 跨天后重置
        assert _daily_state["date"] == time.strftime("%Y-%m-%d")
        assert _daily_state["count"] == 1


class TestGetClientIp:
    def test_prefers_x_forwarded_for_first_hop(self):
        from fastapi import Request
        scope = {
            "type": "http", "method": "POST", "path": "/api/chat",
            "headers": [(b"x-forwarded-for", b"9.9.9.9, 10.0.0.1")],
            "client": ("127.0.0.1", 8000), "server": ("test", 80),
            "scheme": "http", "query_string": b"",
        }
        assert get_client_ip(Request(scope)) == "9.9.9.9"

    def test_falls_back_to_client_host(self):
        from fastapi import Request
        scope = {
            "type": "http", "method": "POST", "path": "/api/chat",
            "headers": [], "client": ("8.8.8.8", 8000),
            "server": ("test", 80), "scheme": "http", "query_string": b"",
        }
        assert get_client_ip(Request(scope)) == "8.8.8.8"
```

- [ ] **Step 2: 新建 `tests/test_public_chat.py`（失败测试先行）**

```python
"""Tests for public /api/chat access (no JWT) + health endpoints.

`test_auth.py` was removed along with the login system; the health-check
tests live here now.
"""

import os
os.environ["SKIP_CONFIG_VALIDATION"] = "1"

import time

import pytest
from fastapi.testclient import TestClient

from backend.app.main import app
from backend.app.deps.rate_limit import _hourly_buckets, _daily_state
import configs.settings as cfg

client = TestClient(app)


@pytest.fixture(autouse=True)
def _clean_state():
    _hourly_buckets.clear()
    _daily_state["date"] = ""
    _daily_state["count"] = 0
    yield
    _hourly_buckets.clear()
    _daily_state["date"] = ""
    _daily_state["count"] = 0


def _fake_stream(question: str):
    async def gen():
        yield {"type": "final_answer", "node": "finalize", "data": {"answer": "ok"}}
    return gen()


class TestPublicChat:
    def test_chat_requires_no_token(self, monkeypatch):
        monkeypatch.setattr("backend.app.routers.chat.stream_run", _fake_stream)
        resp = client.post("/api/chat", json={"question": "你好"})
        assert resp.status_code == 200
        assert "final_answer" in resp.text

    def test_chat_empty_question_returns_422(self):
        resp = client.post("/api/chat", json={"question": ""})
        assert resp.status_code == 422

    def test_login_route_removed(self):
        resp = client.post("/api/auth/login", json={"access_code": "whatever"})
        assert resp.status_code == 404

    def test_ip_hourly_quota_returns_429(self, monkeypatch):
        monkeypatch.setattr("backend.app.routers.chat.stream_run", _fake_stream)
        headers = {"x-forwarded-for": "203.0.113.7"}
        for _ in range(cfg.settings.ip_hourly_quota):
            resp = client.post("/api/chat", json={"question": "hi"}, headers=headers)
            assert resp.status_code == 200
        resp = client.post("/api/chat", json={"question": "hi"}, headers=headers)
        assert resp.status_code == 429
        assert "本小时" in resp.json()["detail"]

    def test_global_daily_cap_returns_429(self, monkeypatch):
        monkeypatch.setattr("backend.app.routers.chat.stream_run", _fake_stream)
        _daily_state["date"] = time.strftime("%Y-%m-%d")
        _daily_state["count"] = cfg.settings.global_daily_quota
        resp = client.post("/api/chat", json={"question": "hi"})
        assert resp.status_code == 429
        assert "今日体验名额已用完" in resp.json()["detail"]


class TestHealth:
    def test_health_endpoint(self):
        resp = client.get("/api/health")
        assert resp.status_code == 200
        assert resp.json() == {"status": "ok"}
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd xiehaoyu-agent && .venv/Scripts/python -m pytest tests/test_rate_limit.py tests/test_public_chat.py -v`
Expected: FAIL（`ImportError: cannot import name 'check_global_daily_cap' / 'get_client_ip'`）

- [ ] **Step 4: 重写 `backend/app/deps/rate_limit.py`**

```python
"""In-memory rate limiter (per-IP hourly quota + site-wide daily cap).

Keyed by client IP.  Suitable for single-process, low-concurrency deployments.

**Limitation**: In-memory storage is NOT shared across worker processes
or server restarts.  For multi-worker deployments, replace with Redis-backed
storage (e.g. ``redis-py`` + ``cachetools``).
"""

from __future__ import annotations

import time
from collections import defaultdict

from fastapi import HTTPException, Request, status

from configs.settings import settings

_hourly_buckets: dict[str, list[float]] = defaultdict(list)

# Site-wide daily cap state: {"date": "YYYY-MM-DD", "count": int}
_daily_state: dict[str, object] = {"date": "", "count": 0}


def get_client_ip(request: Request) -> str:
    """Extract the real client IP, honoring X-Forwarded-For from Nginx."""
    xff = request.headers.get("x-forwarded-for")
    if xff:
        return xff.split(",")[0].strip()
    return request.client.host if request.client else "unknown"


def check_rate_limit(client_ip: str = "unknown") -> None:
    """Raise 429 if this IP has exhausted its hourly quota."""
    now = time.time()
    cutoff = now - 3600
    bucket = _hourly_buckets[client_ip]
    bucket[:] = [t for t in bucket if t > cutoff]

    # Prune expired entries before checking (M11: prune before append, not after)
    if len(bucket) >= settings.ip_hourly_quota:
        remaining = int(3600 - (now - min(bucket)))
        raise HTTPException(
            status_code=status.HTTP_429_TOO_MANY_REQUESTS,
            detail=(
                f"本小时提问次数已达上限（{settings.ip_hourly_quota} 次），"
                f"约 {max(1, remaining // 60)} 分钟后重置，请稍后再试。"
            ),
        )
    bucket.append(now)

    # Prune empty buckets to prevent unbounded dict growth
    if not bucket:
        del _hourly_buckets[client_ip]


def check_global_daily_cap() -> None:
    """Raise 429 if the site-wide daily quota is exhausted (anti-abuse backstop)."""
    today = time.strftime("%Y-%m-%d")
    if _daily_state["date"] != today:
        _daily_state["date"] = today
        _daily_state["count"] = 0

    if _daily_state["count"] >= settings.global_daily_quota:
        raise HTTPException(
            status_code=status.HTTP_429_TOO_MANY_REQUESTS,
            detail="今日体验名额已用完，欢迎明天再来，或通过页脚联系方式找我。",
        )
    _daily_state["count"] += 1
```

- [ ] **Step 5: 修改 `configs/settings.py`**

Settings dataclass 中，删除以下 4 行：

```python
    access_code: str = os.getenv("ACCESS_CODE", "")
    session_hourly_quota: int = _get_int("SESSION_HOURLY_QUOTA", "50")
    jwt_secret: str = os.getenv("JWT_SECRET", "change-me-in-production-use-a-long-random-string-here")
    jwt_expire_hours: int = _get_int("JWT_EXPIRE_HOURS", "24")
```

在原 `session_hourly_quota` 位置新增：

```python
    ip_hourly_quota: int = _get_int("IP_HOURLY_QUOTA", "20")
    global_daily_quota: int = _get_int("GLOBAL_DAILY_QUOTA", "200")
```

`_validate()` 中删除以下两段检查（保留 deepseek_api_key 检查）：

```python
    if s.access_code == "":
        errors.append(
            "ACCESS_CODE is empty — set ACCESS_CODE in .env to a non-empty value."
        )
    if s.jwt_secret == "change-me-in-production-use-a-long-random-string-here":
        errors.append(
            "JWT_SECRET is still the default placeholder — "
            "generate a real secret with: python -c \"import secrets; print(secrets.token_hex(32))\""
        )
```

- [ ] **Step 6: 修改 `backend/app/routers/chat.py`**

导入块改为：

```python
from fastapi import APIRouter, Request
from fastapi.responses import StreamingResponse

from backend.app.deps.rate_limit import (
    check_global_daily_cap,
    check_rate_limit,
    get_client_ip,
)
from backend.app.schemas.chat import ChatRequest

from agent.graph import stream_run
```

端点函数改为：

```python
@router.post("")
async def chat(body: ChatRequest, request: Request) -> StreamingResponse:
    """Send a question and receive agent execution steps as SSE events.

    Public endpoint (no auth).  Rate-limited per client IP (hourly) with a
    site-wide daily cap as an anti-abuse backstop.  Each SSE event is a JSON
    object with ``type``, ``node``, and ``data`` fields.  The stream ends
    with ``data: [DONE]``.
    """
    client_ip = get_client_ip(request)
    check_rate_limit(client_ip)       # 先查 IP：被拒请求不消耗全局名额
    check_global_daily_cap()

    return StreamingResponse(
        _event_generator(body.question, request),
        media_type="text/event-stream",
        headers={
            "Cache-Control": "no-cache",
            "Connection": "keep-alive",
            "X-Accel-Buffering": "no",  # disable Nginx buffering
        },
    )
```

（`_event_generator` 保持不变。）

- [ ] **Step 7: 修改 `backend/app/main.py` 并删除登录体系文件**

`main.py` 第 52 行改为：

```python
from backend.app.routers import chat  # noqa: E402
```

第 71-72 行改为：

```python
app.include_router(chat.router)
```

删除文件（bash）：

```bash
cd xiehaoyu-agent
rm backend/app/dependencies.py backend/app/routers/auth.py backend/app/schemas/auth.py tests/test_auth.py
```

- [ ] **Step 8: 更新 `.env.example`**

删除 `ACCESS_CODE`、`JWT_SECRET`、`JWT_EXPIRE_HOURS`、`SESSION_HOURLY_QUOTA` 四段，在「会话配额」位置替换为：

```
# 限流配置（公开访问）
IP_HOURLY_QUOTA=20
GLOBAL_DAILY_QUOTA=200
```

同时提醒用户同步修改本地 `.env`（gitignored，不入库）：删除 `ACCESS_CODE`/`JWT_SECRET`/`JWT_EXPIRE_HOURS`/`SESSION_HOURLY_QUOTA`，新增 `IP_HOURLY_QUOTA=20`、`GLOBAL_DAILY_QUOTA=200`。

- [ ] **Step 9: 运行全部后端测试**

Run: `cd xiehaoyu-agent && .venv/Scripts/python -m pytest tests/ -v`
Expected: PASS（含新测试；无 test_auth.py）

- [ ] **Step 10: Commit**

```bash
git add configs/settings.py .env.example backend/app/deps/rate_limit.py backend/app/routers/chat.py backend/app/main.py backend/app/dependencies.py backend/app/routers/auth.py backend/app/schemas/auth.py tests/
git commit -m 'refactor(backend): 聊天接口公开化，限流改为按 IP + 全局日上限，删除登录体系'
```

---

## Task 2: 深色设计系统（global.css + App.vue + 工具色 + Markdown 代码高亮）

**Files:**
- Rewrite: `frontend/src/styles/global.css`
- Rewrite: `frontend/src/App.vue`
- Modify: `frontend/src/utils/tool-constants.ts:18-23`
- Modify: `frontend/src/utils/markdown.ts:5`

**Interfaces:**
- Consumes: 无（纯样式层）
- Produces（后续任务依赖的 CSS 变量）:
  - `--bg-base #0a192f`、`--bg-card #112240`、`--bg-card-lighter #1d3461`、`--bg-bubble-user #112240`、`--bg-subtle rgba(100,255,218,0.04)`
  - `--accent #64ffda`、`--accent-strong #64ffda`、`--accent-strong-hover #7dffe3`、`--accent-border rgba(100,255,218,0.2)`、`--ink #64ffda`、`--ink-hover #7dffe3`
  - `--text-bright #e6f1ff`、`--text-1 #ccd6f6`、`--text-2 #8892b0`、`--text-3 #7a8cb0`
  - `--border #233554`、`--border-strong rgba(100,255,218,0.35)`、`--shadow-pop/--shadow-card 0 10px 30px -15px rgba(2,12,27,0.7)`、`--radius-card 10px`、`--radius-pill 999px`
  - `--tool-query/--tool-viz/--tool-rag/--tool-explain`（与 tool-constants.ts 同值）
  - `.reveal` / `.revealed` 入场类 + `.stagger` 阶梯延迟规则（Task 4 的指令与 Task 5 的列表使用）
  - `.pill`（描边薄荷胶囊，Task 5 的技能/项目/经历标签使用）
  - `pulse-glow` / `float-blob` 关键帧（Task 4 导航脉冲点、Task 8 Banner 光斑与 badge、Task 9 在线状态使用）

- [ ] **Step 1: 重写 `frontend/src/styles/global.css`**

```css
/* ============================================================
   Xiehaoyu-Agent — Global Styles (Dark Geek)
   ============================================================ */

:root {
  /* 背景 */
  --bg-base: #0a192f;
  --bg-card: #112240;
  --bg-card-lighter: #1d3461;
  --bg-bubble-user: #112240;
  --bg-subtle: rgba(100, 255, 218, 0.04);        /* 卡片头/表头浅底 */
  /* 品牌 */
  --accent: #64ffda;
  --accent-strong: #64ffda;                       /* 工作色：链接/强调/焦点/流光 */
  --accent-strong-hover: #7dffe3;
  --accent-border: rgba(100, 255, 218, 0.2);
  --ink: #64ffda;                                 /* 主按钮色 */
  --ink-hover: #7dffe3;
  /* 文本 */
  --text-bright: #e6f1ff;                         /* 亮白（姓名/章节标题/导航当前横线） */
  --text-1: #ccd6f6;                              /* 主文本 */
  --text-2: #8892b0;                              /* 次要文本 */
  --text-3: #7a8cb0;                              /* 辅助/时间戳 */
  /* 结构 */
  --border: #233554;
  --border-strong: rgba(100, 255, 218, 0.35);
  --shadow-pop: 0 10px 30px -15px rgba(2, 12, 27, 0.7);
  --shadow-card: 0 10px 30px -15px rgba(2, 12, 27, 0.7);
  --radius-card: 10px;
  --radius-pill: 999px;
  /* 工具轨迹色（深色可读版，与 tool-constants.ts 同步） */
  --tool-query: #64b5f6;
  --tool-viz: #64ffda;
  --tool-rag: #ffb86c;
  --tool-explain: #c792ea;
  /* 等宽字体（编号/装饰） */
  --font-mono: 'Fira Code', 'SF Mono', 'Cascadia Code', Consolas, monospace;
}

html {
  scroll-behavior: smooth;
}

html, body, #app {
  margin: 0;
  padding: 0;
  height: 100%;
  background: var(--bg-base);
  color: var(--text-1);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Noto Sans SC', 'PingFang SC', 'Microsoft YaHei', Inter, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* ── Scrollbar ────────────────────────────────────────── */

::-webkit-scrollbar { width: 5px; height: 5px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(136, 146, 176, 0.35); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: rgba(136, 146, 176, 0.55); }

/* ── Selection ─────────────────────────────── */

::selection {
  background: rgba(100, 255, 218, 0.25);
  color: var(--text-1);
}

/* ── Focus ring ─────────────────────────────── */

:focus-visible {
  outline: 2px solid var(--accent-strong);
  outline-offset: 2px;
  border-radius: 4px;
}

/* ── Markdown Code Blocks ─────────────────────────────── */

pre code.hljs {
  border-radius: 10px;
  padding: 1rem 1.1rem;
  font-size: 0.84rem;
  line-height: 1.55;
  font-family: var(--font-mono);
}

/* Inline code */
:not(pre) > code {
  background: rgba(100, 255, 218, 0.08);
  color: var(--accent-strong);
  padding: 0.12em 0.4em;
  border-radius: 4px;
  font-size: 0.88em;
}

/* ── Tabular numbers ─────────────────────────── */

.n-data-table, .status-text { font-variant-numeric: tabular-nums; }

/* ── Scroll reveal ─────────────────────────────── */

.reveal {
  opacity: 0;
  transform: translateY(28px);
  transition: opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1),
              transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
}
.reveal.revealed {
  opacity: 1;
  transform: none;
}
/* stagger：容器内 .reveal 子项按 0.08s 阶梯延迟入场 */
.stagger > .reveal:nth-child(1) { transition-delay: 0s; }
.stagger > .reveal:nth-child(2) { transition-delay: 0.08s; }
.stagger > .reveal:nth-child(3) { transition-delay: 0.16s; }
.stagger > .reveal:nth-child(4) { transition-delay: 0.24s; }
.stagger > .reveal:nth-child(5) { transition-delay: 0.32s; }
.stagger > .reveal:nth-child(6) { transition-delay: 0.4s; }

/* ── Animations ────────────────────────────────────────── */

@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(10px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeIn {
  from { opacity: 0; }
  to   { opacity: 1; }
}
@keyframes slideInRight {
  from { opacity: 0; transform: translateX(20px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes slideInLeft {
  from { opacity: 0; transform: translateX(-20px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-6px); }
  75% { transform: translateX(6px); }
}
@keyframes flowDown {
  from { background-position: 0 -100%; }
  to   { background-position: 0 100%; }
}
@keyframes blink {
  0%, 100% { opacity: 1; }
  50% { opacity: 0; }
}
@keyframes pulse-glow {
  0%, 100% { opacity: 1; box-shadow: 0 0 0 0 rgba(100, 255, 218, 0.4); }
  50% { opacity: 0.6; box-shadow: 0 0 0 5px rgba(100, 255, 218, 0); }
}
@keyframes float-blob {
  0%, 100% { transform: translate(0, 0); }
  33% { transform: translate(15px, -20px); }
  66% { transform: translate(-10px, 15px); }
}

.chat-message-enter {
  animation: fadeInUp 0.35s ease-out;
}
.fade-in {
  animation: fadeIn 0.3s ease-out;
}
.slide-in {
  animation: slideInRight 0.3s ease-out;
}

/* ── Streaming Cursor ──────────────────────────────────── */

.streaming-cursor::after {
  content: '▊';
  animation: blink 1s step-end infinite;
  color: var(--accent-strong);
}

/* ── Naive UI Overrides ────────────────────────────────── */

.n-card {
  --n-border-color: var(--border) !important;
}

.n-tabs .n-tabs-tab {
  transition: color 0.2s, background 0.2s;
}

.n-button {
  transition: all 0.2s ease;
}

.n-code {
  border-radius: 8px;
}

/* ── Reduced motion ─────────────────────────── */

@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  .reveal { opacity: 1; transform: none; transition: none; }
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* ── Pill tags（描边薄荷胶囊，按参考实现稿） ─────────────── */

.pill {
  display: inline-flex;
  align-items: center;
  font-family: var(--font-mono);
  font-size: 0.75rem;
  color: var(--accent-strong);
  background: rgba(100, 255, 218, 0.08);
  border: 1px solid rgba(100, 255, 218, 0.18);
  border-radius: 20px;
  padding: 4px 14px;
  line-height: 1.4;
  white-space: nowrap;
  transition: background 0.2s ease, border-color 0.2s ease, transform 0.2s ease;
}
.pill:hover {
  background: rgba(100, 255, 218, 0.15);
  border-color: rgba(100, 255, 218, 0.35);
  transform: translateY(-1px);
}

/* ── Utility ───────────────────────────────────────────── */

.truncate {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
```

- [ ] **Step 2: 重写 `frontend/src/App.vue`**

```vue
<template>
  <n-config-provider :theme="darkTheme" :theme-overrides="themeOverrides">
    <n-message-provider>
      <router-view />
    </n-message-provider>
  </n-config-provider>
</template>

<script setup lang="ts">
import { darkTheme } from 'naive-ui'
import type { GlobalThemeOverrides } from 'naive-ui'

const themeOverrides: GlobalThemeOverrides = {
  common: {
    fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans SC", "PingFang SC", "Microsoft YaHei", Inter, sans-serif',
    borderRadius: '10px',
    primaryColor: '#64ffda',
    primaryColorHover: '#7dffe3',
    primaryColorPressed: '#4ee6c3',
    primaryColorSuppl: '#64ffda',
  },
  Card: {
    color: '#112240',
    borderColor: '#233554',
  },
  Input: {
    color: '#0d1b36',
    colorFocus: '#0d1b36',
    border: '1px solid #233554',
    borderHover: '1px solid rgba(100,255,218,0.35)',
    borderFocus: '1px solid #64ffda',
    boxShadowFocus: '0 0 0 2px rgba(100,255,218,0.15)',
  },
  DataTable: {
    thColor: 'rgba(100,255,218,0.04)',
    tdColor: '#112240',
    tdColorStriped: 'rgba(100,255,218,0.02)',
    borderColor: '#233554',
  },
  Tag: {
    // 使用默认深色配色即可
  },
}
</script>

<style>
@import '@/styles/global.css';
</style>
```

- [ ] **Step 3: 修改 `frontend/src/utils/tool-constants.ts` 的 STEP_COLORS**

```ts
export const STEP_COLORS: Record<string, string> = {
  query_data: '#64b5f6',
  visualize: '#64ffda',
  introduce_me: '#ffb86c',
  explain_result: '#c792ea',
}
```

- [ ] **Step 4: 修改 `frontend/src/utils/markdown.ts` 第 5 行**

```ts
import 'highlight.js/styles/github-dark.css'
```

- [ ] **Step 5: 构建验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功（vue-tsc 零错误）。旧页面视觉异常属预期（Global Constraints 已说明）。

- [ ] **Step 6: Commit**

```bash
git add frontend/src/styles/global.css frontend/src/App.vue frontend/src/utils/tool-constants.ts frontend/src/utils/markdown.ts
git commit -m 'feat(frontend): 全局切换深色极客风设计系统'
```

---

## Task 3: 内容数据 `src/data/profile.ts`

**Files:**
- Create: `frontend/src/data/profile.ts`

**Interfaces:**
- Consumes: 无
- Produces（后续区块组件 import 的名字，签名必须与下文一致）:
  - `profile: { name, role, tagline, intro: string[], email, wechat, repo, repoLabel }`（`intro` 段落含 `<strong>` 高亮标签，AboutSection 以 `v-html` 渲染 —— 静态可信内容，无 XSS 风险）
  - `skillsFlat: string[]`（关于我区块的 ▹ 平铺技术栈列表）
  - `projects: Project[]`（`{ id, title, description, icon, stat, tags, link?, featured? }`；`icon` 为缩略图占位 emoji，`stat` 为一行关键指标）
  - `experiences: Experience[]`（`{ company, role, period, desc, tags }`，v5 时间线条目，目前仅龙腾一段）

- [ ] **Step 1: 创建 `frontend/src/data/profile.ts`**

```ts
/** 个人内容静态数据（来源：data/知识库/简历、自我介绍、项目、工作经历 + 参考实现稿文案）。
 *  作品集区块全部从这里取数，改文案只动这个文件。 */

export interface Project {
  id: string
  title: string
  description: string
  icon: string        // 缩略图占位 emoji
  stat: string        // 一行关键指标（星标行）
  tags: string[]
  link?: string
  featured?: boolean
}

export interface Experience {
  company: string
  role: string
  period: string
  desc: string
  tags: string[]
}

export const profile = {
  name: '谢浩宇',
  role: '数据工程师 · AI 实践者',
  tagline: '我专注于数据工程与智能分析，擅长从原始数据中提炼洞察，用 AI 工具放大生产力，构建可解释、可落地的数据产品。',
  // 段落含 <strong> 高亮（静态可信内容，AboutSection 用 v-html 渲染）
  intro: [
    '你好！我是<strong>谢浩宇</strong>，来自吉首大学<strong>数据科学与大数据技术</strong>专业，目前本科在读，专业排名前 <strong>5%</strong>。',
    '我对数据的热情始于对「数字背后故事」的好奇——从一条 SQL 查询到完整的数仓分层，从一张散点图到驱动业务决策的指标体系，我喜欢把杂乱的数据变成清晰的洞察。',
    '目前在<strong>龙腾出行 AI 研发部</strong>担任数据实习生，负责数据管道建设、数据质量监控、归因分析和 AI 赋能等工作。此前独立开发了<strong>Xiehaoyu-Agent</strong>个人智能体系统——一个基于 LLM Agent 的 ChatBI 应用，集成了 RAG 知识库检索、Text2SQL 引擎和自动可视化能力。',
    '工作之外，我喜欢研究新的 AI 工具和工作流，探索如何将大语言模型融入日常数据分析流程中。也热衷于参加数学建模和数据竞赛，享受从问题抽象到方案落地的全过程。',
  ],
  email: 'xiehaoyu12138@163.com',
  wechat: 'xhy18711807395',
  repo: 'https://gitee.com/xiehaoyu12138/xiehaoyu-agent/tree/main/',
  repoLabel: '代码仓库',
}

export const skillsFlat: string[] = [
  'Python', 'SQL', 'Pandas / NumPy', 'Hadoop / Hive', 'Spark', 'LangGraph',
  'FastAPI', 'Vue 3', 'Tableau / FineBI', 'ChromaDB', 'DeepSeek / Claude', 'Git / Docker',
]

export const projects: Project[] = [
  {
    id: 'xiehaoyu-agent',
    title: 'Xiehaoyu-Agent',
    description:
      '基于 LLM Agent 的个人智能体与 ChatBI 系统。LangGraph 状态机驱动多 Tool 编排，集成 RAG 个人知识库检索、Text2SQL 查数引擎与自动可视化。你正在使用的这个网站就是它。',
    icon: '🤖',
    stat: '99,441 订单 · 9 表关联',
    tags: ['Python', 'LangGraph', 'DeepSeek', 'ChromaDB', 'FastAPI', 'Vue 3', 'Plotly'],
    link: 'https://gitee.com/xiehaoyu12138/xiehaoyu-agent/tree/main/',
    featured: true,
  },
  {
    id: 'douyin-seeding',
    title: '抖音达人种草复盘与策略分析',
    description:
      '对 447.5 万投放费用、1.2 亿声量做深度归因分析。竞品对标转化率达行业均值 6 倍、单位成本仅行业 1/12；发现 34% 达人单位转化成本超 100 元/人，提出小博主优先策略并建立实时监控看板。',
    icon: '📊',
    stat: '447.5w 投放 · 1.2 亿声量',
    tags: ['Excel', 'FineBI', '星图数据', '漏斗分析', '归因分析'],
  },
  {
    id: 'taobao-rfm',
    title: '淘宝用户价值分层与精准营销',
    description:
      '基于用户行为数据构建 RFM 模型，划分 8 类用户群体。发现浏览到购买转化率仅 9.47%、82.65% 商品仅被购买一次，针对五类重点人群输出差异化策略组合。',
    icon: '📈',
    stat: '预估 ROI +33.5%',
    tags: ['Python', 'Pandas', 'RFM 模型', 'Seaborn', 'Matplotlib'],
  },
  {
    id: 'stock-sentiment',
    title: '股市舆情分析系统',
    description:
      '采集 3.2 万条股吧评论，人工标注 2500 条样本微调 BERT 中文金融情感分类器（准确率 90%+），构建日度情绪指数并验证与沪深 300 的相关性。',
    icon: '💹',
    stat: '3.2 万条评论 · 准确率 90%+',
    tags: ['Python', 'BERT', 'NLP', 'Scrapy', 'StatsModels'],
  },
  {
    id: 'k12-warehouse',
    title: 'K12 数仓与 PowerBI 看板',
    description:
      '从零搭建 ODS → DWD → DWS → DIM 四层 Hive 数仓，解决联报课程业绩归属难题（按学科数均摊，各科 GMV 与总 GMV 对齐），交付三方经营看板。',
    icon: '🏫',
    stat: '50 万订单 · 四层数仓',
    tags: ['Hive', 'DataX', 'PowerBI', 'MySQL', 'Java UDF'],
  },
]

export const experiences: Experience[] = [
  {
    company: '龙腾出行 · AI 研发部',
    role: '数据实习生',
    period: '2026.04 — 至今',
    desc: '基于 DataPipeline 分层架构打通多源 API 采集清洗入库全链路，覆盖国内外十万余条数据；设计四层数据完整性评估脚本并搭建 FineBI 实时质量看板；对 POI 匹配失败案例做聚类归因，定位根因后准确率提升 62%；基于千问大模型搭建「生成→验证→重生成」AI 清洗流程，自动补全字段。',
    tags: ['Python', 'SQL', 'FineBI', '归因分析', '千问大模型'],
  },
]
```

- [ ] **Step 2: 构建验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

- [ ] **Step 3: Commit**

```bash
git add frontend/src/data/profile.ts
git commit -m 'feat(frontend): 新增作品集内容静态数据 profile.ts'
```

---

## Task 4: 共享小组件（v-reveal 指令 + useScrollSpy + SectionHeading + SiteSidebar）

**Files:**
- Create: `frontend/src/composables/useRevealOnScroll.ts`
- Create: `frontend/src/composables/useScrollSpy.ts`
- Create: `frontend/src/components/portfolio/SectionHeading.vue`
- Create: `frontend/src/components/portfolio/SiteSidebar.vue`

**Interfaces:**
- Consumes: Task 2 的 `.reveal`/`.revealed`/`.stagger` 规则与 CSS 变量；Task 3 的 `profile.name/role/tagline/email/wechat/repo`
- Produces:
  - `vReveal` 指令（`<script setup>` 中 `import { vReveal } from '@/composables/useRevealOnScroll'` 后即可用 `v-reveal`）
  - `useScrollSpy(ids: string[]): Ref<string>`（返回视口中部当前区块 id；本任务的 SiteSidebar 使用）
  - `<SectionHeading num="01." title="关于" />`（props: `num: string`、`title: string`）
  - `<SiteSidebar />`（无 props，PortfolioView 直接组装；桌面端 fixed 左栏 / 移动端顶部身份块 + 横向导航）
- 锚点 id 约定（SiteSidebar 导航与各区块组件共同依赖）：`#about`、`#experience`、`#projects`、`#ai-chat`
- 断点约定（与 PortfolioView 右列保持同步）：>1200px 左栏 42%；≤1200px 38%；≤980px 单列

- [ ] **Step 1: 创建 `frontend/src/composables/useRevealOnScroll.ts`**

```ts
import type { Directive } from 'vue'

/** v-reveal：元素进入视口时添加 .revealed 类（配合 global.css 的 .reveal）。
 *  一次性触发；reduced-motion 时直接显示。 */

const reduced = () =>
  typeof window !== 'undefined' &&
  window.matchMedia('(prefers-reduced-motion: reduce)').matches

const observer: IntersectionObserver | null =
  typeof window !== 'undefined' && 'IntersectionObserver' in window
    ? new IntersectionObserver(
        (entries) => {
          for (const entry of entries) {
            if (entry.isIntersecting) {
              entry.target.classList.add('revealed')
              observer?.unobserve(entry.target)
            }
          }
        },
        { threshold: 0.1, rootMargin: '0px 0px -60px 0px' },
      )
    : null

export const vReveal: Directive = {
  mounted(el: HTMLElement) {
    el.classList.add('reveal')
    if (reduced() || !observer) {
      el.classList.add('revealed')
      return
    }
    observer.observe(el)
  },
  unmounted(el: HTMLElement) {
    observer?.unobserve(el)
  },
}
```

- [ ] **Step 2: 创建 `frontend/src/composables/useScrollSpy.ts`**

```ts
import { ref, onMounted, onUnmounted, type Ref } from 'vue'

/** 滚动侦测：观察给定 id 的区块，返回当前位于视口中部的区块 id。
 *  用于左侧导航高亮（横线指示器）。无 IntersectionObserver 时恒为首个 id。 */
export function useScrollSpy(ids: string[]): Ref<string> {
  const activeId = ref(ids[0] ?? '')
  let observer: IntersectionObserver | null = null

  onMounted(() => {
    if (!('IntersectionObserver' in window)) return
    const sections = ids
      .map((id) => document.getElementById(id))
      .filter((el): el is HTMLElement => el !== null)
    if (sections.length === 0) return

    observer = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (entry.isIntersecting) activeId.value = entry.target.id
        }
      },
      // 视口上 30% / 下 60% 之间为"当前区块"判定带
      { rootMargin: '-30% 0px -60% 0px' },
    )
    for (const el of sections) observer.observe(el)
  })

  onUnmounted(() => {
    observer?.disconnect()
    observer = null
  })

  return activeId
}
```

- [ ] **Step 3: 创建 `frontend/src/components/portfolio/SectionHeading.vue`（编号 + 标题，baseline 对齐，无装饰横线）**

```vue
<template>
  <h2 class="section-heading">
    <span class="sh-num">{{ num }}</span>
    <span class="sh-title">{{ title }}</span>
  </h2>
</template>

<script setup lang="ts">
defineProps<{ num: string; title: string }>()
</script>

<style scoped>
.section-heading {
  display: flex;
  align-items: baseline;
  gap: 0.75rem;
  margin: 0 0 2.25rem;
}
.sh-num {
  font-family: var(--font-mono);
  font-size: 1.5rem;
  font-weight: 500;
  color: var(--accent-strong);
  line-height: 1;
}
.sh-title {
  font-size: 1.75rem;
  font-weight: 700;
  color: var(--text-bright);
  line-height: 1;
}
</style>
```

- [ ] **Step 4: 创建 `frontend/src/components/portfolio/SiteSidebar.vue`（按参考实现稿）**

```vue
<template>
  <aside class="site-sidebar">
    <div class="ss-hero">
      <h1 class="ss-name">{{ profile.name }}</h1>
      <p class="ss-role">{{ profile.role }}</p>
      <p class="ss-tagline">{{ profile.tagline }}</p>
    </div>

    <nav class="ss-nav" aria-label="页面区块导航">
      <a
        v-for="l in links"
        :key="l.id"
        :href="`#${l.id}`"
        class="ss-link"
        :class="{ active: activeId === l.id, 'ai-entry': l.id === 'ai-chat' }"
      >
        <span class="ss-line" aria-hidden="true" />
        <span class="ss-label">
          {{ l.label }}<span v-if="l.id === 'ai-chat'" class="ai-dot" aria-hidden="true" />
        </span>
      </a>
    </nav>

    <ul class="ss-socials">
      <li>
        <a :href="profile.repo" target="_blank" rel="noopener" aria-label="代码仓库" title="代码仓库">
          <svg viewBox="0 0 24 24" width="19" height="19"><path fill="currentColor" d="M12 .5C5.65.5.5 5.65.5 12c0 5.08 3.29 9.39 7.86 10.91.58.11.79-.25.79-.55v-2.17c-3.2.7-3.87-1.36-3.87-1.36-.52-1.33-1.28-1.68-1.28-1.68-1.04-.71.08-.7.08-.7 1.15.08 1.76 1.19 1.76 1.19 1.03 1.75 2.69 1.25 3.34.95.1-.74.4-1.25.72-1.53-2.55-.29-5.23-1.28-5.23-5.68 0-1.26.45-2.28 1.19-3.09-.12-.29-.52-1.46.11-3.05 0 0 .97-.31 3.17 1.18a11.1 11.1 0 0 1 5.78 0c2.2-1.49 3.17-1.18 3.17-1.18.63 1.59.23 2.76.11 3.05.74.81 1.19 1.83 1.19 3.09 0 4.41-2.69 5.38-5.25 5.66.41.35.77 1.05.77 2.12v3.15c0 .3.21.67.8.55A11.51 11.51 0 0 0 23.5 12C23.5 5.65 18.35.5 12 .5z"/></svg>
        </a>
      </li>
      <li>
        <a :href="`mailto:${profile.email}`" aria-label="邮箱" :title="profile.email">
          <svg viewBox="0 0 24 24" width="19" height="19"><path fill="currentColor" d="M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4-8 5-8-5V6l8 5 8-5v2z"/></svg>
        </a>
      </li>
      <li>
        <button class="ss-wechat" aria-label="复制微信号" :title="`微信：${profile.wechat}（点击复制）`" @click="copyWechat">
          <svg viewBox="0 0 24 24" width="19" height="19"><path fill="currentColor" d="M20 2H4c-1.1 0-2 .9-2 2v18l4-4h14c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm-9 9H7V9h4v2zm6 0h-4V9h4v2z"/></svg>
        </button>
      </li>
    </ul>
  </aside>
</template>

<script setup lang="ts">
import { useMessage } from 'naive-ui'
import { profile } from '@/data/profile'
import { useScrollSpy } from '@/composables/useScrollSpy'

const links = [
  { id: 'about', label: '关于' },
  { id: 'experience', label: '经历' },
  { id: 'projects', label: '项目' },
  { id: 'ai-chat', label: 'AI 问答' },
]

const activeId = useScrollSpy(links.map((l) => l.id))

const message = useMessage()

async function copyWechat() {
  try {
    await navigator.clipboard.writeText(profile.wechat)
    message.success('微信号已复制')
  } catch {
    message.warning(`复制失败，请手动添加：${profile.wechat}`)
  }
}
</script>

<style scoped>
/* 桌面端：fixed 左栏（右列用 margin-left 让位，见 PortfolioView） */
.site-sidebar {
  position: fixed;
  left: 0;
  top: 0;
  bottom: 0;
  width: 42%;
  max-width: 480px;
  display: flex;
  flex-direction: column;
  padding: 10vh 40px 4vh 10vw;
  z-index: 50;
  overflow-y: auto;
  scrollbar-width: none;
}
.site-sidebar::-webkit-scrollbar { display: none; }

.ss-hero {
  display: flex;
  flex-direction: column;
  gap: 16px;
  margin-bottom: 48px;
}
.ss-name {
  font-size: clamp(32px, 4.5vw, 56px);
  font-weight: 700;
  color: var(--text-bright);
  letter-spacing: -0.02em;
  line-height: 1.1;
  margin: 0;
}
.ss-role {
  font-size: clamp(16px, 2vw, 22px);
  font-weight: 500;
  color: var(--text-2);
  margin: -4px 0 0;
}
.ss-tagline {
  font-size: clamp(14px, 1.6vw, 17px);
  color: var(--text-2);
  line-height: 1.65;
  max-width: 360px;
  margin: 0;
  padding-top: 12px;
  border-top: 1px solid rgba(136, 146, 176, 0.15);
}

/* 横线指示器导航 */
.ss-nav {
  display: flex;
  flex-direction: column;
  margin-bottom: auto;
}
.ss-link {
  display: flex;
  align-items: center;
  gap: 18px;
  padding: 13px 0;
  text-decoration: none;
}
.ss-line {
  width: 44px;
  height: 1px;
  background: var(--text-2);
  flex-shrink: 0;
  transition: width 0.3s ease, background 0.3s ease;
}
.ss-label {
  font-family: var(--font-mono);
  font-size: 12.5px;
  font-weight: 500;
  color: var(--text-2);
  letter-spacing: 0.12em;
  white-space: nowrap;
  transition: color 0.25s ease, transform 0.25s ease;
}
.ss-link:hover .ss-line,
.ss-link.active .ss-line {
  width: 70px;
  background: var(--text-bright);
}
.ss-link:hover .ss-label,
.ss-link.active .ss-label {
  color: var(--accent-strong);
  transform: translateX(4px);
}
/* AI 问答入口项：常驻高亮 + 脉冲圆点 */
.ss-link.ai-entry .ss-label {
  color: var(--accent-strong);
  font-weight: 700;
}
.ss-link.ai-entry .ss-line {
  width: 60px;
  background: var(--text-bright);
}
.ai-dot {
  display: inline-block;
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--accent-strong);
  margin-left: 8px;
  vertical-align: middle;
  animation: pulse-glow 2s infinite;
}

/* 社交图标 */
.ss-socials {
  display: flex;
  align-items: center;
  gap: 20px;
  list-style: none;
  margin: 0;
  padding: 24px 0 0;
}
.ss-socials a,
.ss-wechat {
  width: 38px;
  height: 38px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--text-2);
  background: none;
  border: 1px solid transparent;
  padding: 0;
  cursor: pointer;
  transition: color 0.3s ease, border-color 0.3s ease, transform 0.3s ease, box-shadow 0.3s ease;
}
.ss-socials a:hover,
.ss-wechat:hover {
  color: var(--accent-strong);
  border-color: rgba(100, 255, 218, 0.25);
  transform: translateY(-3px);
  box-shadow: 0 4px 14px rgba(100, 255, 218, 0.12);
}

/* ≤1200px：左栏收窄 */
@media (max-width: 1200px) {
  .site-sidebar { width: 38%; padding-left: 6vw; }
}

/* ≤980px：顶部静态身份块 + 横向导航行 */
@media (max-width: 980px) {
  .site-sidebar {
    position: relative;
    width: 100%;
    max-width: none;
    padding: 8vh 6vw 4vh;
    border-bottom: 1px solid rgba(136, 146, 176, 0.1);
    overflow: visible;
  }
  .ss-hero { margin-bottom: 32px; }
  .ss-tagline { max-width: 100%; }
  .ss-nav { flex-direction: row; flex-wrap: wrap; gap: 0 8px; margin-bottom: 0; }
  .ss-link { padding: 10px 0; }
  .ss-line { width: 32px; }
  .ss-socials { justify-content: center; padding-top: 20px; }
}
@media (max-width: 480px) {
  .site-sidebar { padding: 6vh 5vw; }
}
</style>
```

- [ ] **Step 5: 构建验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功（组件尚未被引用，无视觉验证，到 Task 8 统一检查）

- [ ] **Step 6: Commit**

```bash
git add frontend/src/composables/useRevealOnScroll.ts frontend/src/composables/useScrollSpy.ts frontend/src/components/portfolio/
git commit -m 'feat(frontend): 新增作品集共享组件（reveal 指令/滚动侦测/编号章节标题/左侧固定栏）'
```

---

## Task 5: 作品集区块组件（About + Experience + Projects + SiteFooter）

**Files:**
- Create: `frontend/src/components/portfolio/AboutSection.vue`
- Create: `frontend/src/components/portfolio/ExperienceSection.vue`
- Create: `frontend/src/components/portfolio/ProjectsSection.vue`
- Create: `frontend/src/components/portfolio/SiteFooter.vue`

**Interfaces:**
- Consumes: Task 3 的 `profile / skillsFlat / projects / experiences`；Task 4 的 `<SectionHeading>`、`vReveal`；Task 2 的全局类 `.pill` / `.stagger` 规则
- Produces: `<AboutSection />`、`<ExperienceSection />`、`<ProjectsSection />`、`<SiteFooter />`（无 props，PortfolioView 直接组装）
- 锚点 id 约定（SiteSidebar 导航依赖）：`#about`、`#experience`、`#projects`（`#ai-chat` 在 Task 8）
- 说明：关于我为纯文本版式，`src/assets/avatar.jpg` 不使用（文件保留不删）；区块纵向间距由 PortfolioView 右列 `gap: 80px` 承担，组件自身不加 section padding

- [ ] **Step 1: 创建 `frontend/src/components/portfolio/AboutSection.vue`（简介 v-html + 技术栈 ▹ 平铺）**

```vue
<template>
  <section id="about" v-reveal>
    <SectionHeading num="01." title="关于" />
    <div class="about-text">
      <!-- intro 为 profile.ts 中的静态可信内容，含 <strong> 高亮 -->
      <p v-for="(p, i) in profile.intro" :key="i" v-html="p" />
    </div>
    <div class="skills-section">
      <p class="skills-title">技术栈 / Tech Stack</p>
      <div class="skills-grid">
        <span v-for="s in skillsFlat" :key="s" class="skill-tag">{{ s }}</span>
      </div>
    </div>
  </section>
</template>

<script setup lang="ts">
import SectionHeading from './SectionHeading.vue'
import { vReveal } from '@/composables/useRevealOnScroll'
import { profile, skillsFlat } from '@/data/profile'
</script>

<style scoped>
.about-text p {
  font-size: 1rem;
  color: var(--text-2);
  line-height: 1.75;
  margin: 0 0 1.25rem;
}
.about-text p:last-child { margin-bottom: 0; }
.about-text :deep(strong) {
  color: var(--text-1);
  font-weight: 600;
}

/* 技术栈平铺（▹ 前缀，等宽字体） */
.skills-section {
  margin-top: 2.25rem;
  padding-top: 1.75rem;
  border-top: 1px solid rgba(136, 146, 176, 0.1);
}
.skills-title {
  font-family: var(--font-mono);
  font-size: 0.82rem;
  color: var(--accent-strong);
  font-weight: 500;
  margin: 0 0 1rem;
}
.skills-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0.5rem 1.75rem;
}
.skill-tag {
  font-family: var(--font-mono);
  font-size: 0.82rem;
  color: var(--text-1);
  position: relative;
  padding-left: 1rem;
}
.skill-tag::before {
  content: '▹';
  position: absolute;
  left: 0;
  color: var(--accent-strong);
}
@media (max-width: 480px) {
  .skills-grid { grid-template-columns: 1fr; }
}
</style>
```

- [ ] **Step 2: 创建 `frontend/src/components/portfolio/ExperienceSection.vue`（v5 时间线条目）**

```vue
<template>
  <section id="experience" v-reveal>
    <SectionHeading num="02." title="经历" />
    <ol class="exp-list stagger">
      <li v-for="e in experiences" :key="e.company" class="exp-item" v-reveal>
        <span class="exp-period">{{ e.period }}</span>
        <div class="exp-detail">
          <div class="exp-role-line">
            <span class="exp-role">{{ e.role }}</span>
            <span class="exp-company">{{ e.company }}</span>
          </div>
          <p class="exp-desc">{{ e.desc }}</p>
          <div class="exp-tags">
            <span v-for="t in e.tags" :key="t" class="pill">{{ t }}</span>
          </div>
        </div>
      </li>
    </ol>
  </section>
</template>

<script setup lang="ts">
import SectionHeading from './SectionHeading.vue'
import { vReveal } from '@/composables/useRevealOnScroll'
import { experiences } from '@/data/profile'
</script>

<style scoped>
.exp-list { list-style: none; margin: 0; padding: 0; }
.exp-item {
  display: flex;
  gap: 1.5rem;
  padding: 1.6rem 1.25rem;
  margin: 0 -1.25rem;
  border-radius: 10px;
  position: relative;
  transition: background-color 0.25s ease, box-shadow 0.25s ease;
}
.exp-item:hover {
  background-color: rgba(17, 34, 64, 0.6);
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.2);
}
/* 条目底部渐变分隔线 */
.exp-item::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 1px;
  background: linear-gradient(90deg, rgba(136, 146, 176, 0.08), transparent 70%);
}
.exp-item:last-child::after { display: none; }
.exp-period {
  font-family: var(--font-mono);
  font-size: 0.78rem;
  color: var(--text-2);
  width: 150px;
  flex-shrink: 0;
  padding-top: 0.2rem;
  white-space: nowrap;
}
.exp-detail { flex: 1; min-width: 0; }
.exp-role-line {
  display: flex;
  align-items: baseline;
  gap: 0.4rem;
  flex-wrap: wrap;
  margin-bottom: 0.5rem;
}
.exp-role {
  font-size: 1.12rem;
  font-weight: 600;
  color: var(--text-bright);
}
.exp-company {
  font-size: 1.12rem;
  font-weight: 600;
  color: var(--accent-strong);
}
.exp-desc {
  font-size: 0.9rem;
  color: var(--text-2);
  line-height: 1.65;
  margin: 0 0 0.9rem;
}
.exp-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
}
@media (max-width: 480px) {
  .exp-item { flex-direction: column; gap: 0.4rem; }
  .exp-period { width: auto; }
}
</style>
```

- [ ] **Step 3: 创建 `frontend/src/components/portfolio/ProjectsSection.vue`（横向行卡片：缩略图 + 指标行 + 胶囊）**

```vue
<template>
  <section id="projects" v-reveal>
    <SectionHeading num="03." title="项目" />
    <div class="proj-grid stagger">
      <article v-for="p in projects" :key="p.id" class="proj-card" v-reveal>
        <div class="proj-thumb" aria-hidden="true">
          <span class="proj-thumb-icon">{{ p.icon }}</span>
        </div>
        <div class="proj-info">
          <div class="proj-title-row">
            <h3 class="proj-title">{{ p.title }}</h3>
            <span v-if="p.featured" class="proj-badge">你正在使用它</span>
            <a v-if="p.link" :href="p.link" target="_blank" rel="noopener" class="proj-link" aria-label="查看项目">↗</a>
          </div>
          <p class="proj-desc">{{ p.description }}</p>
          <span class="proj-stat">
            <svg viewBox="0 0 24 24" width="13" height="13" stroke="currentColor" fill="none" stroke-width="2" aria-hidden="true"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
            {{ p.stat }}
          </span>
          <div class="proj-tags">
            <span v-for="t in p.tags" :key="t" class="pill">{{ t }}</span>
          </div>
        </div>
      </article>
    </div>
  </section>
</template>

<script setup lang="ts">
import SectionHeading from './SectionHeading.vue'
import { vReveal } from '@/composables/useRevealOnScroll'
import { projects } from '@/data/profile'
</script>

<style scoped>
.proj-grid { display: flex; flex-direction: column; }
.proj-card {
  display: flex;
  gap: 1.4rem;
  padding: 1.5rem 1.25rem;
  margin: 0 -1.25rem;
  border-radius: 10px;
  transition: background-color 0.25s ease, box-shadow 0.25s ease;
}
.proj-card:hover {
  background-color: rgba(17, 34, 64, 0.5);
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.18);
}
.proj-thumb {
  width: 110px;
  height: 78px;
  border-radius: 6px;
  flex-shrink: 0;
  background: linear-gradient(135deg, #1a3a5c, #0d2137);
  display: flex;
  align-items: center;
  justify-content: center;
}
.proj-thumb-icon {
  font-size: 1.4rem;
  opacity: 0.85;
  transition: transform 0.3s ease;
}
.proj-card:hover .proj-thumb-icon { transform: scale(1.1); }
.proj-info {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}
.proj-title-row {
  display: flex;
  align-items: baseline;
  gap: 0.5rem;
  flex-wrap: wrap;
}
.proj-title {
  font-size: 1.18rem;
  font-weight: 600;
  color: var(--text-bright);
  line-height: 1.3;
  margin: 0;
}
.proj-badge {
  font-size: 0.68rem;
  font-family: var(--font-mono);
  color: var(--accent-strong);
  border: 1px solid var(--accent-border);
  border-radius: var(--radius-pill);
  padding: 0.1rem 0.55rem;
  font-weight: 400;
}
.proj-link {
  color: var(--accent-strong);
  text-decoration: none;
  font-size: 0.9rem;
  transition: transform 0.2s ease;
  display: inline-block;
}
.proj-link:hover { transform: translateX(3px); }
.proj-desc {
  font-size: 0.85rem;
  color: var(--text-2);
  line-height: 1.6;
  margin: 0;
}
.proj-stat {
  font-family: var(--font-mono);
  font-size: 0.75rem;
  color: var(--text-2);
  display: flex;
  align-items: center;
  gap: 0.3rem;
}
.proj-stat svg { stroke: var(--accent-strong); }
.proj-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 0.45rem;
}
@media (max-width: 980px) {
  .proj-card { flex-direction: column; padding: 1.25rem; margin: 0; }
  .proj-thumb { width: 100%; height: 160px; }
}
</style>
```

- [ ] **Step 4: 创建 `frontend/src/components/portfolio/SiteFooter.vue`（极简一行）**

```vue
<template>
  <footer class="site-footer" v-reveal>
    <p>
      由 <a :href="`mailto:${profile.email}`">谢浩宇</a> 设计构建 ·
      Vue 3 · FastAPI · LangGraph · DeepSeek · ChromaDB · © 2026
    </p>
  </footer>
</template>

<script setup lang="ts">
import { vReveal } from '@/composables/useRevealOnScroll'
import { profile } from '@/data/profile'
</script>

<style scoped>
.site-footer {
  text-align: center;
  padding-top: 1.9rem;
  border-top: 1px solid rgba(136, 146, 176, 0.1);
}
.site-footer p {
  font-family: var(--font-mono);
  font-size: 0.75rem;
  color: var(--text-2);
  margin: 0;
}
.site-footer a {
  color: var(--accent-strong);
  text-decoration: none;
}
.site-footer a:hover { text-decoration: underline; }
</style>
```

- [ ] **Step 5: 构建验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/portfolio/
git commit -m 'feat(frontend): 新增 关于/经历/项目/Footer 区块组件（按参考实现稿版式）'
```

---

## Task 6: 聊天链路去鉴权 + 路由公开化过渡

**Files:**
- Modify: `frontend/src/utils/sse.ts`
- Modify: `frontend/src/stores/chat.ts:1-5,88-149`
- Rewrite: `frontend/src/router/index.ts`

**Interfaces:**
- Consumes: Task 1 的公开 `/api/chat`（后端已无需 token）
- Produces:
  - `sseChatStream(question: string, callbacks: SSECallbacks, signal?: AbortSignal): Promise<void>`（**无 token 参数**，Task 8/9 依赖此签名）
  - 路由：`/login`（暂存，Task 9 删）、`/chat`（公开）、`*` → `/chat`；无守卫

- [ ] **Step 1: 修改 `frontend/src/utils/sse.ts`**

`sseChatStream` 签名与 fetch 改为：

```ts
export async function sseChatStream(
  question: string,
  callbacks: SSECallbacks,
  signal?: AbortSignal,
): Promise<void> {
  const response = await fetch('/api/chat', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ question }),
    signal,
  })
  // ……其余逻辑（错误处理 / ReadableStream 解析）保持不变
```

（删除 `token: string` 参数与 `Authorization` 头，其余逐行不变。）

- [ ] **Step 2: 修改 `frontend/src/stores/chat.ts`**

删除顶部 `import { useAuthStore } from '@/stores/auth'`。

`sendMessage` 开头改为：

```ts
  async function sendMessage(question: string) {
    if (isStreaming.value) return

    // Abort any existing stream
    if (abortController.value) {
      abortController.value.abort()
      abortController.value = null
    }
```

（删除 `const auth = useAuthStore()` 与 token 检查；后续 `sseChatStream(question, auth.token, {...}, signal)` 调用改为 `sseChatStream(question, {...}, abortController.value.signal)`，即删去 `auth.token` 实参。）

- [ ] **Step 3: 重写 `frontend/src/router/index.ts`（过渡态：路由公开、守卫删除）**

```ts
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/login',
      name: 'Login',
      component: () => import('@/views/LoginView.vue'),
    },
    {
      path: '/chat',
      name: 'Chat',
      component: () => import('@/views/ChatView.vue'),
    },
    {
      path: '/:pathMatch(.*)*',
      redirect: '/chat',
    },
  ],
})

export default router
```

- [ ] **Step 4: 构建 + 联调验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

联调（后端已在 Task 1 完成公开化）：
1. 终端 1：`cd xiehaoyu-agent && .venv/Scripts/python -m uvicorn backend.app.main:app --port 8000`
2. 终端 2：`cd xiehaoyu-agent/frontend && npm run dev`
3. 浏览器开 `http://localhost:5173/chat`（**不登录直接可达**），发送"介绍一下你自己"，预期正常流式回答

- [ ] **Step 5: Commit**

```bash
git add frontend/src/utils/sse.ts frontend/src/stores/chat.ts frontend/src/router/index.ts
git commit -m 'feat(frontend): 聊天链路去除 token 依赖，路由公开化（过渡态）'
```

---

## Task 7: ChatWidget 抽取（从 ChatMain）

**Files:**
- Create: `frontend/src/components/chat/ChatWidget.vue`
- Rewrite: `frontend/src/components/chat/ChatMain.vue`

**Interfaces:**
- Consumes: 现有 `useChatStore`、`ChatMessage`、`ChatInput`、`WelcomeCard`
- Produces:
  - `<ChatWidget>`：消息区（含自动滚动）+ 输入区；具名插槽 `empty`（无消息时渲染，Task 9 的 ChatView 注入欢迎内容）

- [ ] **Step 1: 创建 `frontend/src/components/chat/ChatWidget.vue`**

```vue
<template>
  <div class="chat-widget">
    <!-- Messages area -->
    <div class="messages-area" ref="messagesRef">
      <slot v-if="chat.messages.length === 0" name="empty" />
      <div v-else class="messages-list">
        <ChatMessage
          v-for="msg in chat.messages"
          :key="msg.id"
          :message="msg"
          :is-streaming="chat.isStreaming && msg === chat.messages[chat.messages.length - 1] && msg.role === 'assistant'"
        />
      </div>
    </div>

    <!-- Input area -->
    <div class="input-area">
      <ChatInput
        :disabled="chat.isStreaming"
        :streaming="chat.isStreaming"
        @send="handleSend"
        @stop="chat.stopStreaming"
      />
    </div>
  </div>
</template>

<script setup lang="ts">
import { watch, nextTick, ref } from 'vue'
import ChatMessage from './ChatMessage.vue'
import ChatInput from './ChatInput.vue'
import { useChatStore } from '@/stores/chat'

const chat = useChatStore()
const messagesRef = ref<HTMLElement>()

async function handleSend(question: string) {
  await chat.sendMessage(question)
}

// Auto-scroll to bottom only when the user is already near the bottom
// (within 100px).  This lets the user scroll up to read history during
// streaming without being yanked back down.
function scrollToBottomIfNear() {
  const el = messagesRef.value
  if (!el) return
  const nearBottom = el.scrollHeight - el.scrollTop - el.clientHeight < 100
  if (nearBottom) {
    el.scrollTop = el.scrollHeight
  }
}

watch(
  () => chat.messages.length,
  () => nextTick(scrollToBottomIfNear)
)
watch(
  () => chat.currentTrace.length,
  () => nextTick(scrollToBottomIfNear)
)
</script>

<style scoped>
.chat-widget {
  display: flex;
  flex-direction: column;
  height: 100%;
  min-height: 0;
}
.messages-area {
  flex: 1;
  overflow-y: auto;
  padding: 1.25rem 1.5rem;
}
.messages-list {
  max-width: 768px;
  margin: 0 auto;
}
.input-area {
  padding: 0 1.5rem 1.25rem;
}

@media (max-width: 768px) {
  .messages-area { padding: 0.75rem 1rem; }
  .input-area { padding: 0 0.75rem 0.75rem; }
}
</style>
```

- [ ] **Step 2: 重写 `frontend/src/components/chat/ChatMain.vue` 为薄封装（旧 `/chat` 页保持可用）**

```vue
<template>
  <ChatWidget>
    <template #empty>
      <WelcomeCard />
    </template>
  </ChatWidget>
</template>

<script setup lang="ts">
import ChatWidget from './ChatWidget.vue'
import WelcomeCard from './WelcomeCard.vue'
</script>
```

- [ ] **Step 3: 构建 + 手动验证旧聊天页回归**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

手动：`npm run dev` 打开 `/chat`，确认欢迎卡、发问、流式回答、停止按钮均与抽取前一致。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/chat/ChatWidget.vue frontend/src/components/chat/ChatMain.vue
git commit -m 'refactor(frontend): 抽取可嵌入 ChatWidget，ChatMain 改为薄封装'
```

---

## Task 8: ChatSection（AI Banner）+ PortfolioView 组装 + 首页路由切换

**Files:**
- Create: `frontend/src/components/portfolio/ChatSection.vue`
- Create: `frontend/src/views/PortfolioView.vue`
- Modify: `frontend/src/router/index.ts`

**Interfaces:**
- Consumes: Task 4 的 `<SiteSidebar />` + `vReveal`；Task 5 的全部区块组件；Task 6 的公开路由
- Produces:
  - 路由 `/` → PortfolioView（公开）；`#ai-chat` 锚点（Banner 所在区块，SiteSidebar 导航依赖）
  - Banner 大按钮 `router.push('/chat')`（Task 9 的全屏页承接）

- [ ] **Step 1: 创建 `frontend/src/components/portfolio/ChatSection.vue`（AI 问答 Banner 入口，按参考实现稿）**

```vue
<template>
  <section id="ai-chat" v-reveal>
    <div class="ai-banner">
      <div class="ai-info">
        <span class="ai-badge">智能对话入口</span>
        <h3 class="ai-title">AI 问答助手</h3>
        <p class="ai-desc">
          基于大语言模型的智能问答系统：检索个人知识库回答「我是谁」，
          也能用自然语言查询 Olist 电商数据（Text2SQL → 自动可视化 → 结果解读）。
          无需登录，点击即可进入对话界面。
        </p>
      </div>
      <button class="ai-btn" @click="goChat">
        进入 AI 问答
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
      </button>
    </div>
  </section>
</template>

<script setup lang="ts">
import { useRouter } from 'vue-router'
import { vReveal } from '@/composables/useRevealOnScroll'

const router = useRouter()

function goChat() {
  router.push('/chat')
}
</script>

<style scoped>
.ai-banner {
  background: linear-gradient(135deg, rgba(100, 255, 218, 0.07) 0%, rgba(100, 255, 218, 0.02) 50%, transparent 100%);
  border: 1px solid rgba(100, 255, 218, 0.16);
  border-radius: 14px;
  padding: 46px 42px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 36px;
  position: relative;
  overflow: hidden;
  transition: border-color 0.3s ease, box-shadow 0.3s ease;
}
.ai-banner:hover {
  border-color: rgba(100, 255, 218, 0.3);
  box-shadow: 0 0 40px rgba(100, 255, 218, 0.06);
}
/* 浮动光斑 */
.ai-banner::before {
  content: '';
  position: absolute;
  top: -60%;
  right: -15%;
  width: 420px;
  height: 420px;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(100, 255, 218, 0.07) 0%, transparent 70%);
  pointer-events: none;
  animation: float-blob 8s ease-in-out infinite;
}
.ai-info { position: relative; z-index: 1; }
.ai-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-family: var(--font-mono);
  font-size: 0.72rem;
  color: var(--accent-strong);
  background: rgba(100, 255, 218, 0.1);
  padding: 5px 13px;
  border-radius: 20px;
  margin-bottom: 16px;
  font-weight: 500;
  border: 1px solid rgba(100, 255, 218, 0.15);
}
.ai-badge::before {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--accent-strong);
  animation: pulse-glow 2s infinite;
}
.ai-title {
  font-size: 1.7rem;
  font-weight: 700;
  color: var(--text-bright);
  margin: 0 0 0.5rem;
}
.ai-desc {
  font-size: 0.95rem;
  color: var(--text-2);
  line-height: 1.6;
  max-width: 450px;
  margin: 0;
}
.ai-btn {
  position: relative;
  z-index: 1;
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 16px 34px;
  background: var(--accent-strong);
  color: var(--bg-base);
  font-size: 0.95rem;
  font-weight: 700;
  border: none;
  border-radius: 10px;
  cursor: pointer;
  white-space: nowrap;
  flex-shrink: 0;
  transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.3s ease;
}
.ai-btn:hover {
  transform: translateY(-3px) scale(1.03);
  box-shadow: 0 10px 32px rgba(100, 255, 218, 0.35);
}
.ai-btn svg {
  width: 18px;
  height: 18px;
  transition: transform 0.3s ease;
}
.ai-btn:hover svg { transform: translateX(4px); }

@media (max-width: 980px) {
  .ai-banner { flex-direction: column; text-align: center; padding: 36px 28px; }
  .ai-desc { max-width: 100%; }
}
</style>
```

- [ ] **Step 2: 创建 `frontend/src/views/PortfolioView.vue`（fixed 左栏 + 右列 margin-left 让位，按参考实现稿）**

```vue
<template>
  <div class="portfolio">
    <SiteSidebar />
    <main class="right-col">
      <AboutSection />
      <ExperienceSection />
      <ProjectsSection />
      <ChatSection />
      <SiteFooter />
    </main>
  </div>
</template>

<script setup lang="ts">
import SiteSidebar from '@/components/portfolio/SiteSidebar.vue'
import AboutSection from '@/components/portfolio/AboutSection.vue'
import ExperienceSection from '@/components/portfolio/ExperienceSection.vue'
import ProjectsSection from '@/components/portfolio/ProjectsSection.vue'
import ChatSection from '@/components/portfolio/ChatSection.vue'
import SiteFooter from '@/components/portfolio/SiteFooter.vue'
</script>

<style scoped>
.portfolio {
  min-height: 100vh;
  background: var(--bg-base);
}
/* 右列：margin-left 与 SiteSidebar 宽度联动（42% / ≤1200px 38% / ≤980px 单列） */
.right-col {
  margin-left: 42%;
  width: 58%;
  min-width: 0;
  padding: 10vh 8vw 10vh 4vw;
  display: flex;
  flex-direction: column;
  gap: 80px;
}
@media (max-width: 1200px) {
  .right-col { margin-left: 38%; width: 62%; padding-right: 6vw; }
}
@media (max-width: 980px) {
  .right-col { margin-left: 0; width: 100%; padding: 6vh 6vw; gap: 56px; }
}
@media (max-width: 480px) {
  .right-col { padding: 5vh 5vw; }
}
</style>
```

- [ ] **Step 3: 修改 `frontend/src/router/index.ts` —— 新增 `/` 路由，catch-all 改为 `/`**

```ts
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/',
      name: 'Portfolio',
      component: () => import('@/views/PortfolioView.vue'),
    },
    {
      path: '/login',
      name: 'Login',
      component: () => import('@/views/LoginView.vue'),
    },
    {
      path: '/chat',
      name: 'Chat',
      component: () => import('@/views/ChatView.vue'),
    },
    {
      path: '/:pathMatch(.*)*',
      redirect: '/',
    },
  ],
})

export default router
```

- [ ] **Step 4: 构建 + 视觉走查（本计划的第一个完整视觉检查点；走完主页视觉即定稿）**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

`npm run dev` 打开 `http://localhost:5173/`，逐项走查（对照 `docs/参考实现稿-index.html`）：
- 左栏（>980px）：姓名/职位/简介（带上边框线）/横线导航/底部社交图标；fixed 固定；滚动时导航高亮跟随当前区块（scrollspy）；「AI 问答」项薄荷加粗 + 脉冲圆点；微信图标点击复制成功提示
- 关于 / 经历 / 项目：编号章节标题（01./02./03. 无横线）；技术栈 ▹ 平铺；经历时间线（左年份右内容、渐变分隔线）；项目卡片（缩略图 + 星标指标行 + 描边胶囊、hover 浅蓝面板）
- AI Banner：渐变底 + 光斑 + badge 脉冲点；大按钮实心薄荷、hover 上浮发光；点击直达 `/chat`（此时 `/chat` 仍是旧版全屏页，Task 9 重写）
- Footer：极简一行署名
- 移动端（≤980px）：左栏变顶部身份块、导航横向换行行；项目卡片转纵向（缩略图全宽）；Banner 转纵向居中
- 说明：本任务完成后**主页视觉已定稿**；`/chat` 旧浅色页在 Task 9 重写、Task 10 深色适配后收尾

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/portfolio/ChatSection.vue frontend/src/views/PortfolioView.vue frontend/src/router/index.ts
git commit -m 'feat(frontend): 组装左侧栏单页作品集，首页路由切换为 PortfolioView，AI Banner 入口就位'
```

---

## Task 9: 全屏 `/chat` 页重写 + 清理删除

**Files:**
- Rewrite: `frontend/src/views/ChatView.vue`
- Modify: `frontend/src/router/index.ts`（删 `/login` 路由）
- Delete: `frontend/src/views/LoginView.vue`、`frontend/src/components/chat/ChatSidebar.vue`、`frontend/src/components/chat/ChatMain.vue`、`frontend/src/components/chat/WelcomeCard.vue`、`frontend/src/stores/auth.ts`、`frontend/src/api/client.ts`、`frontend/src/composables/useAutoScroll.ts`（死代码，无引用且内部 watchTriggers 未完成）

**Interfaces:**
- Consumes: Task 7 的 `<ChatWidget>`（`empty` 插槽）、`WELCOME_QUESTIONS`、`composables/useMediaQuery.ts` 中已存在的 `useEscapeKey(handler: () => void)`
- Produces: `/chat` 全屏页（参考实现稿式顶栏 + ChatWidget 铺满 + 空会话欢迎气泡与 chips）；路由表最终态：`/`、`/chat`、`*` → `/`

- [ ] **Step 1: 重写 `frontend/src/views/ChatView.vue`（顶栏按 `docs/参考实现稿-ai-chat.html`：返回按钮 + 标题组 + 在线状态）**

```vue
<template>
  <div class="chat-fullscreen">
    <header class="cf-bar">
      <div class="cf-left">
        <button class="cf-back" aria-label="返回作品集" @click="goBack">
          <svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 12H5"/><path d="m12 19-7-7 7-7"/></svg>
          <span>返回作品集</span>
        </button>
        <div class="cf-title-group">
          <span class="cf-title">AI 问答助手 · 谢浩宇的数字分身</span>
          <span class="cf-subtitle">基于 LLM Agent · RAG 知识库 · Text2SQL · 自动可视化</span>
        </div>
      </div>
      <div class="cf-status">
        <span class="cf-dot" aria-hidden="true" />
        在线
      </div>
    </header>
    <div class="cf-body">
      <ChatWidget>
        <template #empty>
          <div class="cf-empty">
            <AnimatedAvatar state="idle" :size="88" />
            <p class="cf-welcome">
              你好，我是 Xiehaoyu-Agent。可以问我个人经历与技术栈，
              或让我查数据、画图表。
            </p>
            <div class="chips">
              <button
                v-for="q in WELCOME_QUESTIONS"
                :key="q.question"
                class="chip"
                :disabled="chat.isStreaming"
                @click="ask(q.question)"
              >
                {{ q.question }}
              </button>
            </div>
          </div>
        </template>
      </ChatWidget>
    </div>
  </div>
</template>

<script setup lang="ts">
import { useRouter } from 'vue-router'
import ChatWidget from '@/components/chat/ChatWidget.vue'
import AnimatedAvatar from '@/components/shared/AnimatedAvatar.vue'
import { useChatStore } from '@/stores/chat'
import { useEscapeKey } from '@/composables/useMediaQuery'
import { WELCOME_QUESTIONS } from '@/utils/quick-questions'

const router = useRouter()
const chat = useChatStore()

function goBack() {
  if (window.history.length > 1) {
    router.back()
  } else {
    router.push({ path: '/', hash: '#ai-chat' })
  }
}
useEscapeKey(goBack)

function ask(q: string) {
  if (chat.isStreaming) return
  chat.sendMessage(q)
}
</script>

<style scoped>
.chat-fullscreen {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: var(--bg-base);
}
.cf-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.75rem;
  padding: 0.85rem 1.75rem;
  border-bottom: 1px solid rgba(100, 255, 218, 0.08);
  background: rgba(17, 34, 64, 0.88);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
}
.cf-left {
  display: flex;
  align-items: center;
  gap: 1rem;
  min-width: 0;
}
.cf-back {
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
  font-family: var(--font-mono);
  font-size: 0.78rem;
  color: var(--text-2);
  padding: 0.5rem 0.85rem;
  border: 1px solid transparent;
  border-radius: 7px;
  background: transparent;
  cursor: pointer;
  white-space: nowrap;
  transition: color 0.25s ease, background 0.25s ease, border-color 0.25s ease, transform 0.25s ease;
}
.cf-back:hover {
  color: var(--accent-strong);
  background: rgba(100, 255, 218, 0.06);
  border-color: rgba(100, 255, 218, 0.15);
  transform: translateX(-2px);
}
.cf-title-group {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}
.cf-title {
  font-size: 0.95rem;
  font-weight: 600;
  color: var(--text-bright);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.cf-subtitle {
  font-family: var(--font-mono);
  font-size: 0.66rem;
  color: var(--text-2);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.cf-status {
  display: flex;
  align-items: center;
  gap: 7px;
  font-family: var(--font-mono);
  font-size: 0.72rem;
  color: var(--accent-strong);
  flex-shrink: 0;
}
.cf-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--accent-strong);
  animation: pulse-glow 2s infinite;
}
.cf-body {
  flex: 1;
  min-height: 0;
  max-width: 900px;
  width: 100%;
  margin: 0 auto;
}
.cf-empty {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 1rem;
  gap: 1rem;
}
.cf-welcome {
  color: var(--text-2);
  font-size: 0.95rem;
  line-height: 1.7;
  max-width: 420px;
  margin: 0;
}
.chips {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 0.5rem;
  max-width: 520px;
}
.chip {
  font-family: var(--font-mono);
  border: 1px solid rgba(100, 255, 218, 0.16);
  border-radius: var(--radius-pill);
  background: rgba(100, 255, 218, 0.06);
  color: var(--accent-strong);
  font-size: 0.75rem;
  padding: 0.45rem 0.95rem;
  cursor: pointer;
  transition: color 0.2s, border-color 0.2s, background 0.2s, transform 0.2s;
}
.chip:hover:not(:disabled) {
  border-color: rgba(100, 255, 218, 0.35);
  background: rgba(100, 255, 218, 0.14);
  transform: translateY(-2px);
}
.chip:disabled {
  opacity: 0.45;
  cursor: not-allowed;
}

@media (max-width: 640px) {
  .cf-bar { padding: 0.75rem 1rem; }
  .cf-subtitle { display: none; }
}
</style>
```

- [ ] **Step 2: 修改 `frontend/src/router/index.ts` —— 删除 `/login` 路由**

```ts
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/',
      name: 'Portfolio',
      component: () => import('@/views/PortfolioView.vue'),
    },
    {
      path: '/chat',
      name: 'Chat',
      component: () => import('@/views/ChatView.vue'),
    },
    {
      path: '/:pathMatch(.*)*',
      redirect: '/',
    },
  ],
})

export default router
```

- [ ] **Step 3: 删除废弃文件**

```bash
cd xiehaoyu-agent/frontend
rm src/views/LoginView.vue src/components/chat/ChatSidebar.vue src/components/chat/ChatMain.vue src/components/chat/WelcomeCard.vue src/stores/auth.ts src/api/client.ts src/composables/useAutoScroll.ts
```

- [ ] **Step 4: 构建 + 回归验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功（vue-tsc 零错误；若有残留引用报错，逐一清除）

手动验证：
- `/` 作品集正常；AI Banner 大按钮与左栏「AI 问答」导航项均可进入 `/chat`
- `/chat`：顶栏（返回作品集 / 标题组 / 在线状态）正常；空会话显示欢迎语 + chips，chips 点击直接发问
- 发问、流式、图表、轨迹正常；返回按钮与 ESC 均回到作品集
- 消息状态跨路由保留：`/chat` 发一条消息 → 返回主页 → 再进 `/chat` → 消息仍在；流式中切换路由不中断
- 直接访问 `/login` → 重定向到 `/`

- [ ] **Step 5: Commit**

```bash
git add frontend/
git commit -m 'feat(frontend): 全屏聊天页重写为 /chat（参考实现稿顶栏），删除登录页与废弃组件'
```

---

## Task 10: 聊天组件深色适配（ChatMessage / ChatInput / ChartRenderer / AnimatedAvatar）

**Files:**
- Modify: `frontend/src/components/chat/ChatMessage.vue`（样式区）
- Modify: `frontend/src/components/chat/ChatInput.vue`（样式 + 发送图标色）
- Modify: `frontend/src/components/result/ChartRenderer.vue`（render 函数 layout 合并逻辑）
- Modify: `frontend/src/components/shared/AnimatedAvatar.vue`（样式区）

**Interfaces:**
- Consumes: Task 2 的 CSS 变量（`--bg-card`、`--accent-border` 等）
- Produces: 深色下完整可用的聊天展示链（消息气泡 / inline-result / 数据表 / 图表）

- [ ] **Step 1: `ChatMessage.vue` 样式修改（`<style scoped>` 内逐条替换）**

```css
.chat-message.user .msg-bubble {
  background: linear-gradient(135deg, rgba(100, 255, 218, 0.13), rgba(100, 255, 218, 0.05));
  border: 1px solid rgba(100, 255, 218, 0.1);
  padding: 0.7rem 1rem;
  max-width: 70%;
  border-radius: 18px;
  margin-left: auto;
}
.inline-result {
  margin-top: 0.6rem;
  border: 1px solid var(--border);
  background: var(--bg-card);
  border-radius: 10px;
  overflow: hidden;
}
.ir-section {
  border-bottom: 1px solid var(--border);
}
.action-btn:hover { background: rgba(255, 255, 255, 0.06); color: var(--text-1); }
.irt-connector { width: 1px; flex: 1; background: var(--border); margin: 3px 0; }
.irt-connector.flow {
  background: linear-gradient(180deg, var(--accent-strong) 0%, rgba(136,146,176,0.25) 50%, var(--accent-strong) 100%);
  background-size: 100% 250%;
  animation: flowDown 1.2s linear infinite;
}
```

（模板中两个 SVG 图标的 `color="#0b7a55"` / `color="#4f83e0"` / `color="#d97706"` 分别改为 `#64ffda` / `#64b5f6` / `#ffb86c`。）

- [ ] **Step 2: `ChatInput.vue` 样式修改**

```css
.input-container {
  display: flex;
  align-items: flex-end;
  gap: 0.5rem;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 28px;
  box-shadow: var(--shadow-pop);
  padding: 0.4rem 0.5rem 0.4rem 1.2rem;
  transition: border-color 0.2s;
}
.input-container:focus-within {
  border-color: var(--accent-strong);
  background: var(--bg-card);
}
.send-btn {
  background: var(--accent-strong) !important;
  transition: background 0.2s ease;
}
.send-btn:hover:not(:disabled) { background: var(--accent-strong-hover) !important; }
.send-btn:disabled { background: #3a4a63 !important; }
```

模板中发送/停止两个 `n-icon` 的 `color="#fff"` 改为 `color="#0a192f"`。

- [ ] **Step 3: `ChartRenderer.vue` 的 `render()` 中 layout 合并逻辑替换为**

```ts
    const fig = JSON.parse(props.figureJson)
    const layout = { ...(fig.layout || {}) }
    layout.paper_bgcolor = 'rgba(0,0,0,0)'
    layout.plot_bgcolor = 'rgba(0,0,0,0)'
    layout.font = { ...(layout.font || {}), color: '#8892b0', size: 11 }
    layout.margin = { t: 40, r: 20, b: 50, l: 50, ...(layout.margin || {}) }
    for (const key of Object.keys(layout)) {
      if (/^[xy]axis/.test(key)) {
        layout[key] = {
          gridcolor: 'rgba(136,146,176,0.15)',
          zerolinecolor: 'rgba(136,146,176,0.25)',
          ...layout[key],
        }
      }
    }
    Plotly.newPlot(el, fig.data, layout, {
      responsive: true,
      displaylogo: false,
      displayModeBar: false,
    }).then((plot: any) => { chartInstance = plot })
```

- [ ] **Step 4: `AnimatedAvatar.vue` 样式修改**

```css
.animated-avatar {
  display: inline-block;
  border-radius: 50%;
  overflow: hidden;
  flex-shrink: 0;
  background: rgba(100, 255, 218, 0.06);
}
```

- [ ] **Step 5: 构建 + 深色聊天全链路视觉验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

`npm run dev` 验证：
- 发送"2018 年每月订单数，帮我画个图"：图表深色可读（字体/网格线）、数据表深色、SQL 折叠面板正常
- 发送"介绍一下你自己"：RAG 回答气泡、引用、轨迹时间线在深色下清晰
- 用户气泡薄荷描边、流式光标、流光轨迹可见
- 输入框深色、发送按钮薄荷底深色图标

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/chat/ChatMessage.vue frontend/src/components/chat/ChatInput.vue frontend/src/components/result/ChartRenderer.vue frontend/src/components/shared/AnimatedAvatar.vue
git commit -m 'feat(frontend): 聊天组件全链路深色适配'
```

---

## Task 11: 收尾（meta / README / deploy / 全量验收）

**Files:**
- Modify: `frontend/index.html`
- Modify: `README.md`（技术栈表「鉴权/限流」行 + 架构章节路由描述）
- Modify: `deploy/xiehaoyu-agent.service:10`
- Modify: `.env`（用户本地，gitignored —— 在最终回复中提醒用户手动同步，不入库）

**Interfaces:**
- Consumes: 前序全部任务
- Produces: 可部署的最终态

- [ ] **Step 1: 修改 `frontend/index.html`**

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="谢浩宇的个人作品集与数字分身 —— 基于个人知识库的 RAG 问答 + ChatBI 数据问答的个人智能体" />
    <title>谢浩宇 — 数据工程师 & AI 实践者</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

- [ ] **Step 2: 修改 `deploy/xiehaoyu-agent.service` 第 10 行（uvicorn 加 `--proxy-headers`，配合 Nginx 已有 `X-Forwarded-For` 获取真实 IP）**

```
ExecStart=/srv/xiehaoyu-agent/.venv/bin/uvicorn backend.app.main:app --host 127.0.0.1 --port 8000 --workers 1 --proxy-headers
```

- [ ] **Step 3: 修改 `README.md`**

技术栈表「鉴权/限流」行替换为：

```
| **限流**       | 公开访问 + 按 IP 小时限流 + 全局每日上限           | 防刷 API                        |
```

架构章节中「JWT 鉴权」相关表述改为「公开访问（按 IP 限流）」；登录/访问码相关段落删除；「GitHub 公开仓」表述统一为 Gitee（与 git remote 及 profile.ts 链接一致）。

- [ ] **Step 4: 全量测试 + 构建**

Run: `cd xiehaoyu-agent && .venv/Scripts/python -m pytest tests/ -v`
Expected: 全部 PASS

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

- [ ] **Step 5: 全量验收清单（手动，对照设计文档 §10.2）**

桌面 + 移动端（DevTools 375px）各过一遍（主页对照 `docs/参考实现稿-index.html`、`/chat` 对照 `docs/参考实现稿-ai-chat.html`）：
- [ ] 左栏 / 关于 / 经历 / 项目 / AI Banner / Footer 六张截图检查点均达标
- [ ] 左栏导航：锚点平滑滚动到位；滚动侦测高亮跟随；「AI 问答」项薄荷加粗 + 脉冲点；微信图标点击复制有提示
- [ ] 移动端（≤980px）：顶部身份块 + 横向导航行；项目卡片转纵向；Banner 转纵向居中
- [ ] AI Banner：大按钮直达 `/chat`；badge 脉冲点与浮动光斑正常
- [ ] `/chat` 顶栏：返回作品集 / 标题组 / 在线状态；空会话欢迎语 + chips 可点击发问
- [ ] RAG 问答（"介绍一下你自己"）：引用展示正常
- [ ] ChatBI（"2018 年每月订单数，帮我画个图"）：图表 + 数据表 + SQL 折叠 + 执行轨迹
- [ ] 停止生成 / 重试按钮
- [ ] 全屏 `/chat`：进入 / 返回 / ESC / 流式中切换路由 / 刷新直达
- [ ] reduced-motion（DevTools 模拟）下无动效（含脉冲点与光斑）
- [ ] 429 文案：用 `for` 循环快速打满 IP 配额验证提示（或临时把 `IP_HOURLY_QUOTA` 调小）

- [ ] **Step 6: Commit**

```bash
git add frontend/index.html README.md deploy/xiehaoyu-agent.service
git commit -m 'chore: 更新站点 meta、README 鉴权描述与 uvicorn proxy-headers 配置'
```

---

## Self-Review 记录

- **Spec 覆盖**：设计文档 §3-§10 各节均有对应 Task（§3 路由→T6/T8/T9；§4 视觉→T2/T10；§5 组件→T4/T5/T7/T8/T9；§6 后端→T1；§7 内容→T3；§8 错误处理→T1 429 文案 + T9 边界 + T11 验收；§9 性能/无障碍→T2 reduced-motion + T11 验收；§10 测试→T1 pytest + T11 清单）
- **类型一致性**：`sseChatStream(question, callbacks, signal)` 新签名在 T6 定义、T9 沿用；`ChatWidget` 的 `empty` 插槽在 T7 定义、T9 使用；`profile/skillsFlat/projects/experiences` 字段名在 T3 定义、T4/T5 使用一致；锚点 id（`#about/#experience/#projects/#ai-chat`）在 T4 约定、T5/T8 落位
- **视觉基准一致性**：T2 的 CSS 变量/动效、T4 的 SiteSidebar/SectionHeading、T5 的区块版式、T8 的 Banner 均按 `docs/参考实现稿-index.html` 转写；T9 顶栏按 `docs/参考实现稿-ai-chat.html` 转写
- **已知中间态**：T2-T8 期间旧页面视觉异常（预期，T9 后消失）；T6 后 `/login` 页仍可访问但无入口（T9 删除）；T8 完成时主页视觉已定稿，`/chat` 旧页在 T9/T10 收尾
