# 前端作品集化重构 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将前端重构为深色极客风单页作品集（Hero/关于我/技能项目/嵌入聊天/Footer），聊天全功能嵌入并支持 `/chat` 全屏路由，后端改为公开访问 + IP 限流 + 全局日上限。

**Architecture:** 在现有 Vue 3 + FastAPI 工程内重构。后端先做（TDD）：删除 JWT 登录体系，`/api/chat` 公开化，限流从 JWT user_id 改为客户端 IP + 全局每日上限。前端先立深色设计系统（CSS 变量 + Naive UI darkTheme），再从下到上抽取 `ChatWidget`（复用现有消息/输入/图表/SSE 全部逻辑），最后组装单页作品集并切换路由。

**Tech Stack:** Vue 3.5 + TypeScript + Vite 8 + Naive UI 2.44 + Pinia 4 + vue-router 4 + Plotly.js + Lottie-web / FastAPI + pytest

**设计依据:** `docs/730前端作品集化重构设计.md`（已确认）

## Global Constraints

- 文案全中文（装饰性编号用阿拉伯数字 + 等宽字体）
- 色板（verbatim）：背景 `#0a192f` / 卡片 `#112240` / 点缀 `#64ffda` / 主文本 `#ccd6f6` / 次文本 `#8892b0` / 结构边框 `#233554` / 点缀描边 `rgba(100,255,218,0.2)`
- 工具轨迹色：查询 `#64b5f6` / 图表 `#64ffda` / RAG `#ffb86c` / 解读 `#c792ea`
- 不新增任何 npm 依赖；不引入 Redis
- Lottie 头像仅出现在聊天区块（含 `/chat` 全屏页），主页其他区块不使用
- Plotly / Lottie 保持按需懒加载；全站遵守 `prefers-reduced-motion`
- 后端 pytest 全部通过；前端 `npm run build`（含 vue-tsc 类型检查）零错误
- 提交信息风格：conventional commits 中文（如 `feat(frontend): ...`、`refactor(backend): ...`），每个 Task 至少一次提交
- 项目根目录：`d:\Users\Desktop\Xiehaoyu-agent\xiehaoyu-agent`（以下相对路径均基于此）
- 中间态说明：Task 2 深色变量生效后，旧浅色页面（登录/旧聊天页）会出现视觉异常，**属预期**，Task 9 清理后消失

---

## 文件结构

**后端（修改/删除/新增）**

| 文件 | 动作 | 职责 |
|------|------|------|
| `configs/settings.py` | 修改 | 删 JWT/访问码配置，新增 IP/全局限流配置 |
| `.env.example` | 修改 | 同步配置项 |
| `backend/app/deps/rate_limit.py` | 重写 | 按 IP 小时限流 + 全局日上限 + `get_client_ip` |
| `backend/app/routers/chat.py` | 修改 | 移除 JWT 依赖，接入新限流 |
| `backend/app/main.py` | 修改 | 移除 auth router |
| `backend/app/dependencies.py` | 删除 | JWT 校验（不再需要） |
| `backend/app/routers/auth.py` | 删除 | 登录路由 |
| `backend/app/schemas/auth.py` | 删除 | 登录 schema |
| `tests/test_rate_limit.py` | 重写 | 新限流逻辑测试 |
| `tests/test_public_chat.py` | 新增 | 公开聊天/限流/健康检查测试 |
| `tests/test_auth.py` | 删除 | 登录测试（TestHealth 移入 test_public_chat.py） |

**前端（新增）**

| 文件 | 职责 |
|------|------|
| `src/data/profile.ts` | 全部个人内容静态数据 + 类型 |
| `src/composables/useRevealOnScroll.ts` | `v-reveal` 滚动入场指令 |
| `src/components/portfolio/SectionHeading.vue` | 编号章节标题 |
| `src/components/portfolio/NavBar.vue` | 顶部导航 |
| `src/components/portfolio/SideRails.vue` | 桌面端左右固定边栏 |
| `src/components/portfolio/HeroSection.vue` | Hero |
| `src/components/portfolio/AboutSection.vue` | 关于我 |
| `src/components/portfolio/ProjectsSection.vue` | 技能 + 项目 + 实习 |
| `src/components/portfolio/ChatSection.vue` | 聊天区块（嵌入 ChatWidget + chips + 全屏按钮） |
| `src/components/portfolio/ContactFooter.vue` | 联系 Footer |
| `src/components/chat/ChatWidget.vue` | 可嵌入聊天（消息列表 + 输入框 + empty 插槽） |
| `src/views/PortfolioView.vue` | 单页组装 |

**前端（修改/删除）**

| 文件 | 动作 |
|------|------|
| `src/styles/global.css` | 重写（深色设计系统） |
| `src/App.vue` | 重写（darkTheme + overrides） |
| `src/utils/tool-constants.ts` | 修改（STEP_COLORS 深色版） |
| `src/utils/markdown.ts` | 修改（hljs 主题换 github-dark） |
| `src/utils/sse.ts` | 修改（去 token 参数与 Authorization 头） |
| `src/stores/chat.ts` | 修改（去 auth 依赖） |
| `src/router/index.ts` | 重写（`/` + `/chat`，无守卫） |
| `src/components/chat/ChatMain.vue` | 重写为 ChatWidget 薄封装（Task 9 删除） |
| `src/views/ChatView.vue` | 重写为全屏聊天页 |
| `src/components/chat/ChatMessage.vue` | 修改（深色适配） |
| `src/components/chat/ChatInput.vue` | 修改（深色适配） |
| `src/components/result/ChartRenderer.vue` | 修改（Plotly 深色 layout） |
| `index.html` | 修改（title/meta） |
| `src/views/LoginView.vue` / `src/components/chat/ChatSidebar.vue` / `src/components/chat/WelcomeCard.vue` / `src/stores/auth.ts` / `src/api/client.ts` | 删除（Task 9） |
| `README.md` / `deploy/xiehaoyu-agent.service` | 修改（Task 11） |

---

## Task 1: 后端公开聊天改造（公开访问 + IP 限流 + 全局日上限）

**Files:**
- Modify: `configs/settings.py`
- Modify: `.env.example`
- Rewrite: `backend/app/deps/rate_limit.py`
- Modify: `backend/app/routers/chat.py:12-16,53-75`
- Modify: `backend/app/main.py:52,71-72`
- Delete: `backend/app/dependencies.py`、`backend/app/routers/auth.py`、`backend/app/schemas/auth.py`、`tests/test_auth.py`
- Rewrite: `tests/test_rate_limit.py`
- Create: `tests/test_public_chat.py`

**Interfaces:**
- Consumes: 现有 `agent.graph.stream_run(question)` SSE 事件流（不变）
- Produces:
  - `check_rate_limit(client_ip: str = "unknown") -> None`（按 IP 小时配额，429 抛 HTTPException）
  - `check_global_daily_cap() -> None`（全局日配额，429 抛 HTTPException）
  - `get_client_ip(request: fastapi.Request) -> str`（优先取 `X-Forwarded-For` 首跳）
  - `settings.ip_hourly_quota: int`（默认 20）、`settings.global_daily_quota: int`（默认 200）
  - 限流内部状态 `_hourly_buckets: dict[str, list[float]]`、`_daily_state: dict[str, object]`（测试重置用）

- [ ] **Step 1: 重写 `tests/test_rate_limit.py`（失败测试先行）**

```python
"""Unit tests for backend/app/deps/rate_limit.py — IP quota + global daily cap."""

import os
os.environ["SKIP_CONFIG_VALIDATION"] = "1"

import time

import pytest
from fastapi import HTTPException

from backend.app.deps.rate_limit import (
    check_rate_limit,
    check_global_daily_cap,
    get_client_ip,
    _hourly_buckets,
    _daily_state,
)
import configs.settings as cfg


@pytest.fixture(autouse=True)
def _clean_state():
    _hourly_buckets.clear()
    _daily_state["date"] = ""
    _daily_state["count"] = 0
    yield
    _hourly_buckets.clear()
    _daily_state["date"] = ""
    _daily_state["count"] = 0


class TestIpHourlyQuota:
    def test_first_request_does_not_raise(self):
        check_rate_limit("1.2.3.4")

    def test_exceeding_quota_raises_429(self):
        quota = cfg.settings.ip_hourly_quota
        for _ in range(quota):
            check_rate_limit("1.2.3.4")
        with pytest.raises(HTTPException) as exc_info:
            check_rate_limit("1.2.3.4")
        assert exc_info.value.status_code == 429
        assert "本小时" in exc_info.value.detail

    def test_ips_are_isolated(self):
        quota = cfg.settings.ip_hourly_quota
        for _ in range(quota):
            check_rate_limit("1.1.1.1")
        with pytest.raises(HTTPException):
            check_rate_limit("1.1.1.1")
        check_rate_limit("2.2.2.2")  # 另一个 IP 不受影响

    def test_expired_entries_are_cleaned(self):
        now = time.time()
        _hourly_buckets["3.3.3.3"] = [now - 3700, now - 100]
        check_rate_limit("3.3.3.3")
        assert len(_hourly_buckets["3.3.3.3"]) == 2  # 过期 1 条被清 + 新增 1 条


class TestGlobalDailyCap:
    def test_under_cap_does_not_raise(self):
        check_global_daily_cap()
        assert _daily_state["count"] == 1

    def test_exceeding_cap_raises_429(self):
        _daily_state["date"] = time.strftime("%Y-%m-%d")
        _daily_state["count"] = cfg.settings.global_daily_quota
        with pytest.raises(HTTPException) as exc_info:
            check_global_daily_cap()
        assert exc_info.value.status_code == 429
        assert "今日体验名额已用完" in exc_info.value.detail

    def test_date_rollover_resets_count(self):
        _daily_state["date"] = "2000-01-01"
        _daily_state["count"] = cfg.settings.global_daily_quota
        check_global_daily_cap()  # 跨天后重置
        assert _daily_state["date"] == time.strftime("%Y-%m-%d")
        assert _daily_state["count"] == 1


class TestGetClientIp:
    def test_prefers_x_forwarded_for_first_hop(self):
        from fastapi import Request
        scope = {
            "type": "http", "method": "POST", "path": "/api/chat",
            "headers": [(b"x-forwarded-for", b"9.9.9.9, 10.0.0.1")],
            "client": ("127.0.0.1", 8000), "server": ("test", 80),
            "scheme": "http", "query_string": b"",
        }
        assert get_client_ip(Request(scope)) == "9.9.9.9"

    def test_falls_back_to_client_host(self):
        from fastapi import Request
        scope = {
            "type": "http", "method": "POST", "path": "/api/chat",
            "headers": [], "client": ("8.8.8.8", 8000),
            "server": ("test", 80), "scheme": "http", "query_string": b"",
        }
        assert get_client_ip(Request(scope)) == "8.8.8.8"
```

- [ ] **Step 2: 新建 `tests/test_public_chat.py`（失败测试先行）**

```python
"""Tests for public /api/chat access (no JWT) + health endpoints.

`test_auth.py` was removed along with the login system; the health-check
tests live here now.
"""

import os
os.environ["SKIP_CONFIG_VALIDATION"] = "1"

import time

import pytest
from fastapi.testclient import TestClient

from backend.app.main import app
from backend.app.deps.rate_limit import _hourly_buckets, _daily_state
import configs.settings as cfg

client = TestClient(app)


@pytest.fixture(autouse=True)
def _clean_state():
    _hourly_buckets.clear()
    _daily_state["date"] = ""
    _daily_state["count"] = 0
    yield
    _hourly_buckets.clear()
    _daily_state["date"] = ""
    _daily_state["count"] = 0


def _fake_stream(question: str):
    async def gen():
        yield {"type": "final_answer", "node": "finalize", "data": {"answer": "ok"}}
    return gen()


class TestPublicChat:
    def test_chat_requires_no_token(self, monkeypatch):
        monkeypatch.setattr("backend.app.routers.chat.stream_run", _fake_stream)
        resp = client.post("/api/chat", json={"question": "你好"})
        assert resp.status_code == 200
        assert "final_answer" in resp.text

    def test_chat_empty_question_returns_422(self):
        resp = client.post("/api/chat", json={"question": ""})
        assert resp.status_code == 422

    def test_login_route_removed(self):
        resp = client.post("/api/auth/login", json={"access_code": "whatever"})
        assert resp.status_code == 404

    def test_ip_hourly_quota_returns_429(self, monkeypatch):
        monkeypatch.setattr("backend.app.routers.chat.stream_run", _fake_stream)
        headers = {"x-forwarded-for": "203.0.113.7"}
        for _ in range(cfg.settings.ip_hourly_quota):
            resp = client.post("/api/chat", json={"question": "hi"}, headers=headers)
            assert resp.status_code == 200
        resp = client.post("/api/chat", json={"question": "hi"}, headers=headers)
        assert resp.status_code == 429
        assert "本小时" in resp.json()["detail"]

    def test_global_daily_cap_returns_429(self, monkeypatch):
        monkeypatch.setattr("backend.app.routers.chat.stream_run", _fake_stream)
        _daily_state["date"] = time.strftime("%Y-%m-%d")
        _daily_state["count"] = cfg.settings.global_daily_quota
        resp = client.post("/api/chat", json={"question": "hi"})
        assert resp.status_code == 429
        assert "今日体验名额已用完" in resp.json()["detail"]


class TestHealth:
    def test_health_endpoint(self):
        resp = client.get("/api/health")
        assert resp.status_code == 200
        assert resp.json() == {"status": "ok"}
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd xiehaoyu-agent && .venv/Scripts/python -m pytest tests/test_rate_limit.py tests/test_public_chat.py -v`
Expected: FAIL（`ImportError: cannot import name 'check_global_daily_cap' / 'get_client_ip'`）

- [ ] **Step 4: 重写 `backend/app/deps/rate_limit.py`**

```python
"""In-memory rate limiter (per-IP hourly quota + site-wide daily cap).

Keyed by client IP.  Suitable for single-process, low-concurrency deployments.

**Limitation**: In-memory storage is NOT shared across worker processes
or server restarts.  For multi-worker deployments, replace with Redis-backed
storage (e.g. ``redis-py`` + ``cachetools``).
"""

from __future__ import annotations

import time
from collections import defaultdict

from fastapi import HTTPException, Request, status

from configs.settings import settings

_hourly_buckets: dict[str, list[float]] = defaultdict(list)

# Site-wide daily cap state: {"date": "YYYY-MM-DD", "count": int}
_daily_state: dict[str, object] = {"date": "", "count": 0}


def get_client_ip(request: Request) -> str:
    """Extract the real client IP, honoring X-Forwarded-For from Nginx."""
    xff = request.headers.get("x-forwarded-for")
    if xff:
        return xff.split(",")[0].strip()
    return request.client.host if request.client else "unknown"


def check_rate_limit(client_ip: str = "unknown") -> None:
    """Raise 429 if this IP has exhausted its hourly quota."""
    now = time.time()
    cutoff = now - 3600
    bucket = _hourly_buckets[client_ip]
    bucket[:] = [t for t in bucket if t > cutoff]

    # Prune expired entries before checking (M11: prune before append, not after)
    if len(bucket) >= settings.ip_hourly_quota:
        remaining = int(3600 - (now - min(bucket)))
        raise HTTPException(
            status_code=status.HTTP_429_TOO_MANY_REQUESTS,
            detail=(
                f"本小时提问次数已达上限（{settings.ip_hourly_quota} 次），"
                f"约 {max(1, remaining // 60)} 分钟后重置，请稍后再试。"
            ),
        )
    bucket.append(now)

    # Prune empty buckets to prevent unbounded dict growth
    if not bucket:
        del _hourly_buckets[client_ip]


def check_global_daily_cap() -> None:
    """Raise 429 if the site-wide daily quota is exhausted (anti-abuse backstop)."""
    today = time.strftime("%Y-%m-%d")
    if _daily_state["date"] != today:
        _daily_state["date"] = today
        _daily_state["count"] = 0

    if _daily_state["count"] >= settings.global_daily_quota:
        raise HTTPException(
            status_code=status.HTTP_429_TOO_MANY_REQUESTS,
            detail="今日体验名额已用完，欢迎明天再来，或通过页脚联系方式找我。",
        )
    _daily_state["count"] += 1
```

- [ ] **Step 5: 修改 `configs/settings.py`**

Settings dataclass 中，删除以下 4 行：

```python
    access_code: str = os.getenv("ACCESS_CODE", "")
    session_hourly_quota: int = _get_int("SESSION_HOURLY_QUOTA", "50")
    jwt_secret: str = os.getenv("JWT_SECRET", "change-me-in-production-use-a-long-random-string-here")
    jwt_expire_hours: int = _get_int("JWT_EXPIRE_HOURS", "24")
```

在原 `session_hourly_quota` 位置新增：

```python
    ip_hourly_quota: int = _get_int("IP_HOURLY_QUOTA", "20")
    global_daily_quota: int = _get_int("GLOBAL_DAILY_QUOTA", "200")
```

`_validate()` 中删除以下两段检查（保留 deepseek_api_key 检查）：

```python
    if s.access_code == "":
        errors.append(
            "ACCESS_CODE is empty — set ACCESS_CODE in .env to a non-empty value."
        )
    if s.jwt_secret == "change-me-in-production-use-a-long-random-string-here":
        errors.append(
            "JWT_SECRET is still the default placeholder — "
            "generate a real secret with: python -c \"import secrets; print(secrets.token_hex(32))\""
        )
```

- [ ] **Step 6: 修改 `backend/app/routers/chat.py`**

导入块改为：

```python
from fastapi import APIRouter, Request
from fastapi.responses import StreamingResponse

from backend.app.deps.rate_limit import (
    check_global_daily_cap,
    check_rate_limit,
    get_client_ip,
)
from backend.app.schemas.chat import ChatRequest

from agent.graph import stream_run
```

端点函数改为：

```python
@router.post("")
async def chat(body: ChatRequest, request: Request) -> StreamingResponse:
    """Send a question and receive agent execution steps as SSE events.

    Public endpoint (no auth).  Rate-limited per client IP (hourly) with a
    site-wide daily cap as an anti-abuse backstop.  Each SSE event is a JSON
    object with ``type``, ``node``, and ``data`` fields.  The stream ends
    with ``data: [DONE]``.
    """
    client_ip = get_client_ip(request)
    check_rate_limit(client_ip)       # 先查 IP：被拒请求不消耗全局名额
    check_global_daily_cap()

    return StreamingResponse(
        _event_generator(body.question, request),
        media_type="text/event-stream",
        headers={
            "Cache-Control": "no-cache",
            "Connection": "keep-alive",
            "X-Accel-Buffering": "no",  # disable Nginx buffering
        },
    )
```

（`_event_generator` 保持不变。）

- [ ] **Step 7: 修改 `backend/app/main.py` 并删除登录体系文件**

`main.py` 第 52 行改为：

```python
from backend.app.routers import chat  # noqa: E402
```

第 71-72 行改为：

```python
app.include_router(chat.router)
```

删除文件（bash）：

```bash
cd xiehaoyu-agent
rm backend/app/dependencies.py backend/app/routers/auth.py backend/app/schemas/auth.py tests/test_auth.py
```

- [ ] **Step 8: 更新 `.env.example`**

删除 `ACCESS_CODE`、`JWT_SECRET`、`JWT_EXPIRE_HOURS`、`SESSION_HOURLY_QUOTA` 四段，在「会话配额」位置替换为：

```
# 限流配置（公开访问）
IP_HOURLY_QUOTA=20
GLOBAL_DAILY_QUOTA=200
```

同时提醒用户同步修改本地 `.env`（gitignored，不入库）：删除 `ACCESS_CODE`/`JWT_SECRET`/`JWT_EXPIRE_HOURS`/`SESSION_HOURLY_QUOTA`，新增 `IP_HOURLY_QUOTA=20`、`GLOBAL_DAILY_QUOTA=200`。

- [ ] **Step 9: 运行全部后端测试**

Run: `cd xiehaoyu-agent && .venv/Scripts/python -m pytest tests/ -v`
Expected: PASS（含新测试；无 test_auth.py）

- [ ] **Step 10: Commit**

```bash
git add configs/settings.py .env.example backend/app/deps/rate_limit.py backend/app/routers/chat.py backend/app/main.py backend/app/dependencies.py backend/app/routers/auth.py backend/app/schemas/auth.py tests/
git commit -m 'refactor(backend): 聊天接口公开化，限流改为按 IP + 全局日上限，删除登录体系'
```

---

## Task 2: 深色设计系统（global.css + App.vue + 工具色 + Markdown 代码高亮）

**Files:**
- Rewrite: `frontend/src/styles/global.css`
- Rewrite: `frontend/src/App.vue`
- Modify: `frontend/src/utils/tool-constants.ts:18-23`
- Modify: `frontend/src/utils/markdown.ts:5`

**Interfaces:**
- Consumes: 无（纯样式层）
- Produces（后续任务依赖的 CSS 变量）:
  - `--bg-base #0a192f`、`--bg-card #112240`、`--bg-bubble-user #112240`、`--bg-subtle rgba(100,255,218,0.04)`
  - `--accent #64ffda`、`--accent-strong #64ffda`、`--accent-strong-hover #7dffe3`、`--accent-border rgba(100,255,218,0.2)`、`--ink #64ffda`、`--ink-hover #7dffe3`
  - `--text-1 #ccd6f6`、`--text-2 #8892b0`、`--text-3 #7a8cb0`
  - `--border #233554`、`--border-strong rgba(100,255,218,0.35)`、`--shadow-pop/--shadow-card 0 10px 30px -15px rgba(2,12,27,0.7)`、`--radius-card 10px`、`--radius-pill 999px`
  - `--tool-query/--tool-viz/--tool-rag/--tool-explain`（与 tool-constants.ts 同值）
  - `.reveal` / `.revealed` 入场类（Task 4 的指令使用）

- [ ] **Step 1: 重写 `frontend/src/styles/global.css`**

```css
/* ============================================================
   Xiehaoyu-Agent — Global Styles (Dark Geek)
   ============================================================ */

:root {
  /* 背景 */
  --bg-base: #0a192f;
  --bg-card: #112240;
  --bg-bubble-user: #112240;
  --bg-subtle: rgba(100, 255, 218, 0.04);        /* 卡片头/表头浅底 */
  /* 品牌 */
  --accent: #64ffda;
  --accent-strong: #64ffda;                       /* 工作色：链接/强调/焦点/流光 */
  --accent-strong-hover: #7dffe3;
  --accent-border: rgba(100, 255, 218, 0.2);
  --ink: #64ffda;                                 /* 主按钮色 */
  --ink-hover: #7dffe3;
  /* 文本 */
  --text-1: #ccd6f6;                              /* 主文本 */
  --text-2: #8892b0;                              /* 次要文本 */
  --text-3: #7a8cb0;                              /* 辅助/时间戳 */
  /* 结构 */
  --border: #233554;
  --border-strong: rgba(100, 255, 218, 0.35);
  --shadow-pop: 0 10px 30px -15px rgba(2, 12, 27, 0.7);
  --shadow-card: 0 10px 30px -15px rgba(2, 12, 27, 0.7);
  --radius-card: 10px;
  --radius-pill: 999px;
  /* 工具轨迹色（深色可读版，与 tool-constants.ts 同步） */
  --tool-query: #64b5f6;
  --tool-viz: #64ffda;
  --tool-rag: #ffb86c;
  --tool-explain: #c792ea;
  /* 等宽字体（编号/装饰） */
  --font-mono: ui-monospace, 'Cascadia Code', 'JetBrains Mono', Consolas, monospace;
}

html {
  scroll-behavior: smooth;
}

html, body, #app {
  margin: 0;
  padding: 0;
  height: 100%;
  background: var(--bg-base);
  color: var(--text-1);
  font-family: 'PingFang SC', 'Microsoft YaHei', 'Segoe UI', -apple-system, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* ── Scrollbar ────────────────────────────────────────── */

::-webkit-scrollbar { width: 5px; height: 5px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: rgba(136, 146, 176, 0.35); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: rgba(136, 146, 176, 0.55); }

/* ── Selection ─────────────────────────────── */

::selection {
  background: rgba(100, 255, 218, 0.25);
  color: var(--text-1);
}

/* ── Focus ring ─────────────────────────────── */

:focus-visible {
  outline: 2px solid var(--accent-strong);
  outline-offset: 2px;
  border-radius: 4px;
}

/* ── Markdown Code Blocks ─────────────────────────────── */

pre code.hljs {
  border-radius: 10px;
  padding: 1rem 1.1rem;
  font-size: 0.84rem;
  line-height: 1.55;
  font-family: var(--font-mono);
}

/* Inline code */
:not(pre) > code {
  background: rgba(100, 255, 218, 0.08);
  color: var(--accent-strong);
  padding: 0.12em 0.4em;
  border-radius: 4px;
  font-size: 0.88em;
}

/* ── Tabular numbers ─────────────────────────── */

.n-data-table, .status-text { font-variant-numeric: tabular-nums; }

/* ── Scroll reveal ─────────────────────────────── */

.reveal {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}
.reveal.revealed {
  opacity: 1;
  transform: none;
}

/* ── Animations ────────────────────────────────────────── */

@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(10px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeIn {
  from { opacity: 0; }
  to   { opacity: 1; }
}
@keyframes slideInRight {
  from { opacity: 0; transform: translateX(20px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes slideInLeft {
  from { opacity: 0; transform: translateX(-20px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-6px); }
  75% { transform: translateX(6px); }
}
@keyframes flowDown {
  from { background-position: 0 -100%; }
  to   { background-position: 0 100%; }
}
@keyframes blink {
  0%, 100% { opacity: 1; }
  50% { opacity: 0; }
}

.chat-message-enter {
  animation: fadeInUp 0.35s ease-out;
}
.fade-in {
  animation: fadeIn 0.3s ease-out;
}
.slide-in {
  animation: slideInRight 0.3s ease-out;
}

/* ── Streaming Cursor ──────────────────────────────────── */

.streaming-cursor::after {
  content: '▊';
  animation: blink 1s step-end infinite;
  color: var(--accent-strong);
}

/* ── Naive UI Overrides ────────────────────────────────── */

.n-card {
  --n-border-color: var(--border) !important;
}

.n-tabs .n-tabs-tab {
  transition: color 0.2s, background 0.2s;
}

.n-button {
  transition: all 0.2s ease;
}

.n-code {
  border-radius: 8px;
}

/* ── Reduced motion ─────────────────────────── */

@media (prefers-reduced-motion: reduce) {
  html { scroll-behavior: auto; }
  .reveal { opacity: 1; transform: none; transition: none; }
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* ── Utility ───────────────────────────────────────────── */

.truncate {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
```

- [ ] **Step 2: 重写 `frontend/src/App.vue`**

```vue
<template>
  <n-config-provider :theme="darkTheme" :theme-overrides="themeOverrides">
    <n-message-provider>
      <router-view />
    </n-message-provider>
  </n-config-provider>
</template>

<script setup lang="ts">
import { darkTheme } from 'naive-ui'
import type { GlobalThemeOverrides } from 'naive-ui'

const themeOverrides: GlobalThemeOverrides = {
  common: {
    fontFamily: '"PingFang SC", "Microsoft YaHei", "Segoe UI", sans-serif',
    borderRadius: '10px',
    primaryColor: '#64ffda',
    primaryColorHover: '#7dffe3',
    primaryColorPressed: '#4ee6c3',
    primaryColorSuppl: '#64ffda',
  },
  Card: {
    color: '#112240',
    borderColor: '#233554',
  },
  Input: {
    color: '#0d1b36',
    colorFocus: '#0d1b36',
    border: '1px solid #233554',
    borderHover: '1px solid rgba(100,255,218,0.35)',
    borderFocus: '1px solid #64ffda',
    boxShadowFocus: '0 0 0 2px rgba(100,255,218,0.15)',
  },
  DataTable: {
    thColor: 'rgba(100,255,218,0.04)',
    tdColor: '#112240',
    tdColorStriped: 'rgba(100,255,218,0.02)',
    borderColor: '#233554',
  },
  Tag: {
    // 使用默认深色配色即可
  },
}
</script>

<style>
@import '@/styles/global.css';
</style>
```

- [ ] **Step 3: 修改 `frontend/src/utils/tool-constants.ts` 的 STEP_COLORS**

```ts
export const STEP_COLORS: Record<string, string> = {
  query_data: '#64b5f6',
  visualize: '#64ffda',
  introduce_me: '#ffb86c',
  explain_result: '#c792ea',
}
```

- [ ] **Step 4: 修改 `frontend/src/utils/markdown.ts` 第 5 行**

```ts
import 'highlight.js/styles/github-dark.css'
```

- [ ] **Step 5: 构建验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功（vue-tsc 零错误）。旧页面视觉异常属预期（Global Constraints 已说明）。

- [ ] **Step 6: Commit**

```bash
git add frontend/src/styles/global.css frontend/src/App.vue frontend/src/utils/tool-constants.ts frontend/src/utils/markdown.ts
git commit -m 'feat(frontend): 全局切换深色极客风设计系统'
```

---

## Task 3: 内容数据 `src/data/profile.ts`

**Files:**
- Create: `frontend/src/data/profile.ts`

**Interfaces:**
- Consumes: 无
- Produces（后续区块组件 import 的名字，签名必须与下文一致）:
  - `profile: { name, greeting, tagline, intro: string[], facts: {label,value}[], email, wechat, repo, repoLabel }`
  - `skillGroups: SkillGroup[]`（`{ name: string; skills: string[] }`）
  - `projects: Project[]`（`{ id, title, subtitle, description, tags: string[], highlights: string[], link?, featured? }`）
  - `experience: { company, role, period, points: string[] }`

- [ ] **Step 1: 创建 `frontend/src/data/profile.ts`**

```ts
/** 个人内容静态数据（来源：data/知识库/简历、自我介绍、项目、工作经历）。
 *  作品集区块全部从这里取数，改文案只动这个文件。 */

export interface SkillGroup {
  name: string
  skills: string[]
}

export interface Project {
  id: string
  title: string
  subtitle: string
  description: string
  tags: string[]
  highlights: string[]
  link?: string
  featured?: boolean
}

export interface Experience {
  company: string
  role: string
  period: string
  points: string[]
}

export const profile = {
  name: '谢浩宇',
  greeting: '你好，我是',
  tagline: '我用数据讲故事，也把数据做成产品。',
  intro: [
    '吉首大学数据科学与大数据技术专业本科在读（2023—2027），GPA 3.7，专业排名前 5%，目前在龙腾出行 AI 研发部实习，求职方向是数据分析 / 数据工程。',
    '这个网站本身就是我的作品：一个能回答"我是谁"、也能查数据画图表的个人智能体。欢迎往下滚动了解我，或者直接开聊。',
  ],
  facts: [
    { label: '学校', value: '吉首大学（本科 2023—2027）' },
    { label: '专业', value: '数据科学与大数据技术' },
    { label: '成绩', value: 'GPA 3.7 · 专业前 5%' },
    { label: '荣誉', value: '国家励志奖学金 · 连续三年一等奖学金' },
    { label: '竞赛', value: '数学建模省一等奖 · 计算机设计大赛中南赛区二等奖' },
    { label: '实习', value: '龙腾出行 · AI 研发部 · 数据实习生' },
    { label: '意向', value: '数据分析 / 数据工程' },
  ],
  email: 'xiehaoyu12138@163.com',
  wechat: 'xhy18711807395',
  repo: 'https://gitee.com/xiehaoyu12138/xiehaoyu-agent/tree/main/',
  repoLabel: '代码仓库',
}

export const skillGroups: SkillGroup[] = [
  {
    name: '数据分析',
    skills: ['指标体系', '归因分析', '异动分析', 'SQL', 'Excel', 'Tableau', 'FineBI', '漏斗分析'],
  },
  {
    name: '数据工程',
    skills: ['数仓分层', '维度建模', 'Python', 'Hadoop', 'Hive', 'Spark', 'DataX'],
  },
  {
    name: 'AI 能力',
    skills: ['Claude', 'CodeX', '提示词调优', 'MCP', 'Skills', 'AI 工作流'],
  },
]

export const projects: Project[] = [
  {
    id: 'xiehaoyu-agent',
    title: 'Xiehaoyu-Agent',
    subtitle: '具有数据能力的个人智能体',
    description:
      '多 Tool Agent 编排 + Text2SQL 查数 + 自动可视化 + RAG 个人知识库。你正在使用的这个网站就是它。',
    tags: ['Python', 'LangGraph', 'DeepSeek', 'ChromaDB', 'FastAPI', 'Vue 3', 'Plotly'],
    highlights: [
      '设计多 Tool Agent 状态机（介绍本人 / Text2SQL / 自动可视化 / 结果解读）',
      'Text2SQL 引擎对接 Olist 电商数据集（99,441 订单、9 张关联表）',
      'BGE 中文嵌入 + ChromaDB 构建个人知识库，毫秒级检索',
    ],
    link: 'https://gitee.com/xiehaoyu12138/xiehaoyu-agent/tree/main/',
    featured: true,
  },
  {
    id: 'douyin-seeding',
    title: '抖音达人种草复盘与策略分析',
    subtitle: '447.5 万投放费用的深度归因',
    description:
      '对投放费用 447.5 万、声量 1.2 亿的达人种草项目做归因分析，输出可落地的投放策略。',
    tags: ['Excel', 'FineBI', '漏斗分析', '归因分析'],
    highlights: [
      '竞品对标：转化率达行业均值 6 倍，单位成本仅为行业 1/12',
      '发现 34% 达人单位转化成本超 100 元/人，提出小博主优先策略',
      '建立投放数据监控看板，异常波动自动预警',
    ],
  },
  {
    id: 'taobao-rfm',
    title: '淘宝用户价值分层与精准营销策略',
    subtitle: 'RFM 模型驱动的人群运营',
    description:
      '基于淘宝用户行为数据构建 RFM 模型，将用户分为八类并输出差异化营销策略。',
    tags: ['Python', 'RFM 模型', '漏斗分析', 'Pandas'],
    highlights: [
      '发现浏览到购买转化率仅 9.47%、82.65% 商品仅被购买一次',
      '针对五类重点人群输出策略组合，预估提升营销 ROI 约 33.5%',
    ],
  },
  {
    id: 'stock-sentiment',
    title: '股市舆情分析',
    subtitle: '基于 BERT 的投资者情绪与股价相关性研究',
    description:
      '采集 3.2 万条股吧评论，微调 BERT 中文金融情感分类器，构建日度情绪指数并验证与沪深 300 的相关性。',
    tags: ['Python', 'BERT', 'NLP', 'Scrapy', 'StatsModels'],
    highlights: [
      '人工标注 2500 条样本，情感分类准确率 90%+',
      'IP 代理池 + 请求随机化绕过反爬，采集 3.2 万条评论',
    ],
  },
  {
    id: 'k12-warehouse',
    title: 'K12 数仓与 PowerBI 看板',
    subtitle: 'Hive 四层数仓 + 经营可视化',
    description:
      '从零搭建 ODS → DWD → DWS → DIM 四层 Hive 数仓，解决联报课程业绩归属难题，交付三方经营看板。',
    tags: ['Hive', 'DataX', 'PowerBI', 'MySQL', 'Java UDF'],
    highlights: [
      '约 50 万条订单 + 9 科 1136 条课程维度标签',
      '联报课程按学科数均摊业绩，各科 GMV 加总与总 GMV 对齐',
    ],
  },
]

export const experience: Experience = {
  company: '龙腾出行',
  role: 'AI 研发部 · 数据实习生',
  period: '2026.04 — 至今',
  points: [
    '数据管道：打通多源 API 采集清洗入库全链路，覆盖国内外十万余条数据',
    '数据质量：设计四层完整性评估脚本，搭建 FineBI 实时质量看板',
    '归因分析：POI 匹配失败聚类归因，定位根因后准确率提升 62%',
    'AI 赋能：基于千问大模型搭建"生成→验证→重生成"清洗流程，自动补全字段',
  ],
}
```

- [ ] **Step 2: 构建验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

- [ ] **Step 3: Commit**

```bash
git add frontend/src/data/profile.ts
git commit -m 'feat(frontend): 新增作品集内容静态数据 profile.ts'
```

---

## Task 4: 共享小组件（v-reveal 指令 + SectionHeading + SideRails + NavBar）

**Files:**
- Create: `frontend/src/composables/useRevealOnScroll.ts`
- Create: `frontend/src/components/portfolio/SectionHeading.vue`
- Create: `frontend/src/components/portfolio/SideRails.vue`
- Create: `frontend/src/components/portfolio/NavBar.vue`

**Interfaces:**
- Consumes: Task 2 的 `.reveal`/`.revealed` 类与 CSS 变量；Task 3 的 `profile.email`、`profile.repo`
- Produces:
  - `vReveal` 指令（`<script setup>` 中 `import { vReveal } from '@/composables/useRevealOnScroll'` 后即可用 `v-reveal`）
  - `<SectionHeading num="01." title="关于我" />`（props: `num: string`、`title: string`）
  - `<NavBar />`、`<SideRails />`（无 props）

- [ ] **Step 1: 创建 `frontend/src/composables/useRevealOnScroll.ts`**

```ts
import type { Directive } from 'vue'

/** v-reveal：元素进入视口时添加 .revealed 类（配合 global.css 的 .reveal）。
 *  一次性触发；reduced-motion 时直接显示。 */

const reduced = () =>
  typeof window !== 'undefined' &&
  window.matchMedia('(prefers-reduced-motion: reduce)').matches

const observer: IntersectionObserver | null =
  typeof window !== 'undefined' && 'IntersectionObserver' in window
    ? new IntersectionObserver(
        (entries) => {
          for (const entry of entries) {
            if (entry.isIntersecting) {
              entry.target.classList.add('revealed')
              observer?.unobserve(entry.target)
            }
          }
        },
        { threshold: 0.12 },
      )
    : null

export const vReveal: Directive = {
  mounted(el: HTMLElement) {
    el.classList.add('reveal')
    if (reduced() || !observer) {
      el.classList.add('revealed')
      return
    }
    observer.observe(el)
  },
  unmounted(el: HTMLElement) {
    observer?.unobserve(el)
  },
}
```

- [ ] **Step 2: 创建 `frontend/src/components/portfolio/SectionHeading.vue`**

```vue
<template>
  <h2 class="section-heading">
    <span class="sh-num">{{ num }}</span>
    <span class="sh-title">{{ title }}</span>
    <span class="sh-line" aria-hidden="true" />
  </h2>
</template>

<script setup lang="ts">
defineProps<{ num: string; title: string }>()
</script>

<style scoped>
.section-heading {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  margin: 0 0 2rem;
  font-size: clamp(1.5rem, 3vw, 2rem);
  font-weight: 700;
  color: var(--text-1);
  white-space: nowrap;
}
.sh-num {
  font-family: var(--font-mono);
  font-size: 0.7em;
  font-weight: 400;
  color: var(--accent-strong);
}
.sh-line {
  flex: 1;
  height: 1px;
  background: var(--border);
  margin-left: 0.75rem;
}
</style>
```

- [ ] **Step 3: 创建 `frontend/src/components/portfolio/SideRails.vue`**

```vue
<template>
  <div class="side-rails" aria-hidden="false">
    <div class="rail rail-left">
      <a :href="profile.repo" target="_blank" rel="noopener" aria-label="代码仓库" title="代码仓库">
        <svg viewBox="0 0 24 24" width="20" height="20"><path fill="currentColor" d="M12 .5C5.65.5.5 5.65.5 12c0 5.08 3.29 9.39 7.86 10.91.58.11.79-.25.79-.55v-2.17c-3.2.7-3.87-1.36-3.87-1.36-.52-1.33-1.28-1.68-1.28-1.68-1.04-.71.08-.7.08-.7 1.15.08 1.76 1.19 1.76 1.19 1.03 1.75 2.69 1.25 3.34.95.1-.74.4-1.25.72-1.53-2.55-.29-5.23-1.28-5.23-5.68 0-1.26.45-2.28 1.19-3.09-.12-.29-.52-1.46.11-3.05 0 0 .97-.31 3.17 1.18a11.1 11.1 0 0 1 5.78 0c2.2-1.49 3.17-1.18 3.17-1.18.63 1.59.23 2.76.11 3.05.74.81 1.19 1.83 1.19 3.09 0 4.41-2.69 5.38-5.25 5.66.41.35.77 1.05.77 2.12v3.15c0 .3.21.67.8.55A11.51 11.51 0 0 0 23.5 12C23.5 5.65 18.35.5 12 .5z"/></svg>
      </a>
      <a :href="`mailto:${profile.email}`" aria-label="邮箱" title="邮箱">
        <svg viewBox="0 0 24 24" width="20" height="20"><path fill="currentColor" d="M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4-8 5-8-5V6l8 5 8-5v2z"/></svg>
      </a>
      <span class="rail-line" />
    </div>
    <div class="rail rail-right">
      <a class="rail-email" :href="`mailto:${profile.email}`">{{ profile.email }}</a>
      <span class="rail-line" />
    </div>
  </div>
</template>

<script setup lang="ts">
import { profile } from '@/data/profile'
</script>

<style scoped>
.side-rails { display: none; }
@media (min-width: 768px) {
  .side-rails { display: block; }
  .rail {
    position: fixed;
    bottom: 0;
    z-index: 10;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }
  .rail-left { left: 1.5rem; }
  .rail-right { right: 1.5rem; }
  .rail a {
    color: var(--text-2);
    transition: color 0.2s, transform 0.2s;
    display: block;
  }
  .rail a:hover { color: var(--accent-strong); transform: translateY(-2px); }
  .rail-email {
    writing-mode: vertical-rl;
    font-family: var(--font-mono);
    font-size: 0.78rem;
    letter-spacing: 0.08em;
    text-decoration: none;
  }
  .rail-line {
    width: 1px;
    height: 90px;
    background: var(--text-2);
    opacity: 0.5;
  }
}
</style>
```

- [ ] **Step 4: 创建 `frontend/src/components/portfolio/NavBar.vue`**

```vue
<template>
  <header class="nav" :class="{ scrolled }">
    <a class="brand" href="#hero">谢浩宇<span class="brand-dot">.</span></a>

    <nav class="links-desktop">
      <a v-for="l in links" :key="l.href" :href="l.href">
        <span class="link-num">{{ l.num }}</span>{{ l.label }}
      </a>
    </nav>

    <button class="burger" :aria-expanded="menuOpen" aria-label="菜单" @click="menuOpen = !menuOpen">
      <svg viewBox="0 0 24 24" width="22" height="22"><path fill="currentColor" d="M3 18h18v-2H3v2zm0-5h18v-2H3v2zm0-7v2h18V6H3z"/></svg>
    </button>

    <transition name="fade">
      <nav v-if="menuOpen" class="links-mobile" @click="menuOpen = false">
        <a v-for="l in links" :key="l.href" :href="l.href">
          <span class="link-num">{{ l.num }}</span>{{ l.label }}
        </a>
      </nav>
    </transition>
  </header>
</template>

<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue'

const links = [
  { num: '01.', label: '关于我', href: '#about' },
  { num: '02.', label: '技能与项目', href: '#projects' },
  { num: '03.', label: '和我聊聊', href: '#chat' },
  { num: '04.', label: '联系', href: '#contact' },
]

const scrolled = ref(false)
const menuOpen = ref(false)

function onScroll() {
  scrolled.value = window.scrollY > 24
}
onMounted(() => {
  onScroll()
  window.addEventListener('scroll', onScroll, { passive: true })
})
onUnmounted(() => window.removeEventListener('scroll', onScroll))
</script>

<style scoped>
.nav {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 50;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.9rem clamp(1.25rem, 5vw, 3rem);
  transition: background 0.25s ease, box-shadow 0.25s ease, backdrop-filter 0.25s ease;
}
.nav.scrolled {
  background: rgba(10, 25, 47, 0.85);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  box-shadow: 0 8px 24px -12px rgba(2, 12, 27, 0.8);
}
.brand {
  font-size: 1.05rem;
  font-weight: 700;
  color: var(--text-1);
  text-decoration: none;
  letter-spacing: 0.02em;
}
.brand-dot { color: var(--accent-strong); }
.links-desktop {
  display: flex;
  gap: 1.75rem;
}
.links-desktop a {
  font-size: 0.85rem;
  color: var(--text-2);
  text-decoration: none;
  transition: color 0.2s;
}
.links-desktop a:hover { color: var(--accent-strong); }
.link-num {
  font-family: var(--font-mono);
  font-size: 0.78em;
  color: var(--accent-strong);
  margin-right: 0.3rem;
}
.burger {
  display: none;
  background: none;
  border: none;
  color: var(--text-1);
  cursor: pointer;
  padding: 0.25rem;
}
.links-mobile {
  position: absolute;
  top: 100%; right: 1rem;
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-pop);
  padding: 0.6rem;
  min-width: 180px;
}
.links-mobile a {
  padding: 0.55rem 0.75rem;
  border-radius: 6px;
  color: var(--text-1);
  text-decoration: none;
  font-size: 0.9rem;
}
.links-mobile a:hover { background: var(--bg-subtle); }
.fade-enter-active, .fade-leave-active { transition: opacity 0.18s ease; }
.fade-enter-from, .fade-leave-to { opacity: 0; }

@media (max-width: 767px) {
  .links-desktop { display: none; }
  .burger { display: block; }
}
@media (min-width: 768px) {
  .links-mobile { display: none; }
}
</style>
```

- [ ] **Step 5: 构建验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功（组件尚未被引用，无视觉验证，到 Task 8 统一检查）

- [ ] **Step 6: Commit**

```bash
git add frontend/src/composables/useRevealOnScroll.ts frontend/src/components/portfolio/
git commit -m 'feat(frontend): 新增作品集共享组件（reveal 指令/章节标题/边栏/导航）'
```

---

## Task 5: 作品集区块组件（Hero + About + Projects + Footer）

**Files:**
- Create: `frontend/src/components/portfolio/HeroSection.vue`
- Create: `frontend/src/components/portfolio/AboutSection.vue`
- Create: `frontend/src/components/portfolio/ProjectsSection.vue`
- Create: `frontend/src/components/portfolio/ContactFooter.vue`

**Interfaces:**
- Consumes: Task 3 的 `profile / skillGroups / projects / experience`；Task 4 的 `<SectionHeading>`、`vReveal`；头像图 `@/assets/avatar.jpg`（已存在）
- Produces: `<HeroSection />`、`<AboutSection />`、`<ProjectsSection />`、`<ContactFooter />`（无 props，PortfolioView 直接组装）
- 锚点 id 约定（NavBar/CTA 依赖）：`#hero`、`#about`、`#projects`、`#contact`（ChatSection 的 `#chat` 在 Task 8）

- [ ] **Step 1: 创建 `frontend/src/components/portfolio/HeroSection.vue`**

```vue
<template>
  <section class="hero" id="hero">
    <p class="hero-hi">{{ profile.greeting }}</p>
    <h1 class="hero-name">{{ profile.name }}</h1>
    <h2 class="hero-tagline">{{ profile.tagline }}</h2>
    <div class="hero-intro">
      <p v-for="(p, i) in profile.intro" :key="i">{{ p }}</p>
    </div>
    <div class="hero-cta">
      <a class="btn" href="#chat">和我聊聊</a>
      <a class="btn" href="#projects">看看我的项目</a>
    </div>
  </section>
</template>

<script setup lang="ts">
import { profile } from '@/data/profile'
</script>

<style scoped>
.hero {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  max-width: 900px;
  margin: 0 auto;
  padding: 6rem clamp(1.25rem, 6vw, 3rem) 3rem;
  background:
    radial-gradient(600px 300px at 80% 10%, rgba(100, 255, 218, 0.05), transparent 70%);
}
.hero-hi {
  font-family: var(--font-mono);
  color: var(--accent-strong);
  font-size: 1rem;
  margin: 0 0 1.25rem;
  animation: fadeInUp 0.5s ease-out both;
}
.hero-name {
  font-size: clamp(2.5rem, 6vw, 4.5rem);
  font-weight: 800;
  color: var(--text-1);
  margin: 0;
  line-height: 1.1;
  animation: fadeInUp 0.5s ease-out 0.1s both;
}
.hero-tagline {
  font-size: clamp(1.5rem, 4vw, 2.8rem);
  font-weight: 700;
  color: var(--text-2);
  margin: 0.5rem 0 0;
  line-height: 1.2;
  animation: fadeInUp 0.5s ease-out 0.2s both;
}
.hero-intro {
  max-width: 560px;
  margin-top: 1.75rem;
  color: var(--text-2);
  font-size: 1rem;
  line-height: 1.8;
  animation: fadeInUp 0.5s ease-out 0.3s both;
}
.hero-intro p { margin: 0 0 0.75rem; }
.hero-cta {
  display: flex;
  gap: 1rem;
  margin-top: 2.5rem;
  animation: fadeInUp 0.5s ease-out 0.4s both;
}
.btn {
  display: inline-block;
  padding: 0.8rem 1.6rem;
  border: 1px solid var(--accent-strong);
  border-radius: 6px;
  color: var(--accent-strong);
  background: transparent;
  font-size: 0.9rem;
  text-decoration: none;
  transition: background 0.2s ease, transform 0.2s ease;
}
.btn:hover {
  background: rgba(100, 255, 218, 0.1);
  transform: translateY(-2px);
}
</style>
```

- [ ] **Step 2: 创建 `frontend/src/components/portfolio/AboutSection.vue`**

```vue
<template>
  <section class="about section-pad" id="about" v-reveal>
    <SectionHeading num="01." title="关于我" />
    <div class="about-grid">
      <div class="about-text">
        <p v-for="(p, i) in profile.intro" :key="i">{{ p }}</p>
        <ul class="fact-list">
          <li v-for="f in profile.facts" :key="f.label">
            <span class="fact-label">{{ f.label }}</span>
            <span class="fact-value">{{ f.value }}</span>
          </li>
        </ul>
      </div>
      <div class="about-photo">
        <img :src="avatarUrl" :alt="profile.name" loading="lazy" />
      </div>
    </div>
  </section>
</template>

<script setup lang="ts">
import SectionHeading from './SectionHeading.vue'
import { vReveal } from '@/composables/useRevealOnScroll'
import { profile } from '@/data/profile'
import avatarUrl from '@/assets/avatar.jpg'
</script>

<style scoped>
.section-pad {
  max-width: 900px;
  margin: 0 auto;
  padding: 6rem clamp(1.25rem, 6vw, 3rem);
}
.about-grid {
  display: grid;
  grid-template-columns: 1.6fr 1fr;
  gap: 3rem;
  align-items: start;
}
.about-text p {
  color: var(--text-2);
  line-height: 1.8;
  margin: 0 0 1rem;
}
.fact-list {
  list-style: none;
  margin: 1.5rem 0 0;
  padding: 0;
  display: grid;
  gap: 0.55rem;
}
.fact-list li {
  display: flex;
  gap: 0.75rem;
  font-size: 0.9rem;
}
.fact-label {
  flex-shrink: 0;
  min-width: 3.5rem;
  color: var(--accent-strong);
  font-family: var(--font-mono);
  font-size: 0.82rem;
  padding-top: 0.1rem;
}
.fact-value { color: var(--text-2); }
.about-photo img {
  width: 100%;
  max-width: 260px;
  border-radius: 8px;
  border: 1px solid var(--accent-border);
  filter: saturate(0.9);
  transition: filter 0.3s ease, transform 0.3s ease;
  display: block;
}
.about-photo img:hover {
  filter: none;
  transform: translateY(-4px);
}
@media (max-width: 767px) {
  .about-grid { grid-template-columns: 1fr; }
  .about-photo { order: -1; }
}
</style>
```

- [ ] **Step 3: 创建 `frontend/src/components/portfolio/ProjectsSection.vue`**

```vue
<template>
  <section class="projects section-pad" id="projects" v-reveal>
    <SectionHeading num="02." title="技能与项目" />

    <div class="skills">
      <div v-for="g in skillGroups" :key="g.name" class="skill-group">
        <h3 class="skill-name">{{ g.name }}</h3>
        <ul class="skill-tags">
          <li v-for="s in g.skills" :key="s">{{ s }}</li>
        </ul>
      </div>
    </div>

    <div class="project-list">
      <article v-for="p in projects" :key="p.id" class="project-card">
        <header class="pc-head">
          <div>
            <h3 class="pc-title">
              {{ p.title }}
              <span v-if="p.featured" class="pc-badge">你正在使用它</span>
            </h3>
            <p class="pc-subtitle">{{ p.subtitle }}</p>
          </div>
          <a v-if="p.link" :href="p.link" target="_blank" rel="noopener" class="pc-link" aria-label="查看项目">
            ↗
          </a>
        </header>
        <p class="pc-desc">{{ p.description }}</p>
        <ul class="pc-highlights">
          <li v-for="h in p.highlights" :key="h">{{ h }}</li>
        </ul>
        <ul class="pc-tags">
          <li v-for="t in p.tags" :key="t">{{ t }}</li>
        </ul>
      </article>
    </div>

    <div class="experience">
      <h3 class="exp-title">实习经历</h3>
      <div class="exp-card">
        <header class="exp-head">
          <div>
            <strong>{{ experience.company }}</strong>
            <span class="exp-role">{{ experience.role }}</span>
          </div>
          <span class="exp-period">{{ experience.period }}</span>
        </header>
        <ul class="exp-points">
          <li v-for="pt in experience.points" :key="pt">{{ pt }}</li>
        </ul>
      </div>
    </div>
  </section>
</template>

<script setup lang="ts">
import SectionHeading from './SectionHeading.vue'
import { vReveal } from '@/composables/useRevealOnScroll'
import { skillGroups, projects, experience } from '@/data/profile'
</script>

<style scoped>
.section-pad {
  max-width: 900px;
  margin: 0 auto;
  padding: 6rem clamp(1.25rem, 6vw, 3rem);
}

/* Skills */
.skills {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.25rem;
  margin-bottom: 3.5rem;
}
.skill-name {
  font-size: 0.85rem;
  color: var(--accent-strong);
  font-family: var(--font-mono);
  font-weight: 400;
  margin: 0 0 0.75rem;
}
.skill-tags {
  list-style: none;
  margin: 0; padding: 0;
  display: flex;
  flex-wrap: wrap;
  gap: 0.45rem;
}
.skill-tags li {
  font-size: 0.78rem;
  color: var(--text-2);
  border: 1px solid var(--border);
  border-radius: var(--radius-pill);
  padding: 0.2rem 0.7rem;
  transition: border-color 0.2s, color 0.2s;
}
.skill-tags li:hover {
  border-color: var(--accent-border);
  color: var(--accent-strong);
}

/* Projects */
.project-list {
  display: grid;
  gap: 1.5rem;
  margin-bottom: 3.5rem;
}
.project-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 1.6rem 1.75rem;
  transition: transform 0.25s ease, border-color 0.25s ease, box-shadow 0.25s ease;
}
.project-card:hover {
  transform: translateY(-4px);
  border-color: var(--accent-border);
  box-shadow: var(--shadow-card);
}
.pc-head {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 1rem;
}
.pc-title {
  margin: 0;
  font-size: 1.15rem;
  color: var(--text-1);
  display: flex;
  align-items: center;
  gap: 0.6rem;
  flex-wrap: wrap;
}
.pc-badge {
  font-size: 0.68rem;
  font-family: var(--font-mono);
  color: var(--accent-strong);
  border: 1px solid var(--accent-border);
  border-radius: var(--radius-pill);
  padding: 0.1rem 0.55rem;
  font-weight: 400;
}
.pc-subtitle {
  margin: 0.25rem 0 0;
  font-size: 0.85rem;
  color: var(--accent-strong);
  font-family: var(--font-mono);
}
.pc-link {
  color: var(--text-2);
  text-decoration: none;
  font-size: 1.2rem;
  transition: color 0.2s;
  flex-shrink: 0;
}
.pc-link:hover { color: var(--accent-strong); }
.pc-desc {
  color: var(--text-2);
  line-height: 1.7;
  font-size: 0.92rem;
  margin: 0.9rem 0 0.75rem;
}
.pc-highlights {
  margin: 0 0 1rem;
  padding-left: 1.1rem;
  color: var(--text-2);
  font-size: 0.86rem;
  line-height: 1.7;
}
.pc-highlights li::marker { color: var(--accent-strong); }
.pc-tags {
  list-style: none;
  margin: 0; padding: 0;
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
}
.pc-tags li {
  font-family: var(--font-mono);
  font-size: 0.72rem;
  color: var(--accent-strong);
  opacity: 0.85;
}

/* Experience */
.exp-title {
  font-size: 0.85rem;
  color: var(--accent-strong);
  font-family: var(--font-mono);
  font-weight: 400;
  margin: 0 0 1rem;
}
.exp-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  padding: 1.4rem 1.75rem;
}
.exp-head {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  gap: 1rem;
  flex-wrap: wrap;
  margin-bottom: 0.75rem;
}
.exp-head strong { color: var(--text-1); font-size: 1rem; }
.exp-role {
  color: var(--text-2);
  font-size: 0.85rem;
  margin-left: 0.75rem;
}
.exp-period {
  font-family: var(--font-mono);
  font-size: 0.78rem;
  color: var(--text-3);
}
.exp-points {
  margin: 0;
  padding-left: 1.1rem;
  color: var(--text-2);
  font-size: 0.88rem;
  line-height: 1.8;
}
.exp-points li::marker { color: var(--accent-strong); }

@media (max-width: 767px) {
  .skills { grid-template-columns: 1fr; }
}
</style>
```

- [ ] **Step 4: 创建 `frontend/src/components/portfolio/ContactFooter.vue`**

```vue
<template>
  <footer class="footer" id="contact" v-reveal>
    <p class="footer-num">04.</p>
    <h2 class="footer-title">找我聊聊</h2>
    <p class="footer-desc">
      正在寻找数据分析 / 数据工程方向的机会。无论是面试邀约、技术交流，
      还是对这个网站的任何想法，都欢迎联系我。
    </p>
    <div class="footer-links">
      <a class="btn" :href="`mailto:${profile.email}`">发邮件</a>
      <a class="btn" :href="profile.repo" target="_blank" rel="noopener">代码仓库</a>
    </div>
    <p class="footer-wechat">
      微信 <span class="wechat-id">{{ profile.wechat }}</span>
    </p>
    <p class="footer-stack">Vue 3 · FastAPI · LangGraph · DeepSeek · ChromaDB</p>
    <p class="footer-copy">© 2026 谢浩宇 · 本站由我的数字分身提供对话能力</p>
  </footer>
</template>

<script setup lang="ts">
import { vReveal } from '@/composables/useRevealOnScroll'
import { profile } from '@/data/profile'
</script>

<style scoped>
.footer {
  max-width: 640px;
  margin: 0 auto;
  padding: 6rem 1.5rem 4rem;
  text-align: center;
}
.footer-num {
  font-family: var(--font-mono);
  color: var(--accent-strong);
  font-size: 0.9rem;
  margin: 0 0 0.5rem;
}
.footer-title {
  font-size: clamp(1.8rem, 4vw, 2.6rem);
  color: var(--text-1);
  margin: 0 0 1rem;
}
.footer-desc {
  color: var(--text-2);
  line-height: 1.8;
  margin: 0 0 2rem;
}
.footer-links {
  display: flex;
  justify-content: center;
  gap: 1rem;
  margin-bottom: 1.5rem;
}
.btn {
  display: inline-block;
  padding: 0.75rem 1.6rem;
  border: 1px solid var(--accent-strong);
  border-radius: 6px;
  color: var(--accent-strong);
  background: transparent;
  font-size: 0.9rem;
  text-decoration: none;
  transition: background 0.2s ease, transform 0.2s ease;
}
.btn:hover {
  background: rgba(100, 255, 218, 0.1);
  transform: translateY(-2px);
}
.footer-wechat {
  color: var(--text-2);
  font-size: 0.85rem;
  margin: 0 0 3rem;
}
.wechat-id {
  font-family: var(--font-mono);
  color: var(--accent-strong);
  user-select: all;
}
.footer-stack {
  font-family: var(--font-mono);
  font-size: 0.75rem;
  color: var(--text-3);
  margin: 0 0 0.5rem;
}
.footer-copy {
  font-size: 0.75rem;
  color: var(--text-3);
  margin: 0;
}
</style>
```

- [ ] **Step 5: 构建验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/portfolio/
git commit -m 'feat(frontend): 新增 Hero/关于我/技能项目/Footer 区块组件'
```

---

## Task 6: 聊天链路去鉴权 + 路由公开化过渡

**Files:**
- Modify: `frontend/src/utils/sse.ts`
- Modify: `frontend/src/stores/chat.ts:1-5,88-149`
- Rewrite: `frontend/src/router/index.ts`

**Interfaces:**
- Consumes: Task 1 的公开 `/api/chat`（后端已无需 token）
- Produces:
  - `sseChatStream(question: string, callbacks: SSECallbacks, signal?: AbortSignal): Promise<void>`（**无 token 参数**，Task 8/9 依赖此签名）
  - 路由：`/login`（暂存，Task 9 删）、`/chat`（公开）、`*` → `/chat`；无守卫

- [ ] **Step 1: 修改 `frontend/src/utils/sse.ts`**

`sseChatStream` 签名与 fetch 改为：

```ts
export async function sseChatStream(
  question: string,
  callbacks: SSECallbacks,
  signal?: AbortSignal,
): Promise<void> {
  const response = await fetch('/api/chat', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ question }),
    signal,
  })
  // ……其余逻辑（错误处理 / ReadableStream 解析）保持不变
```

（删除 `token: string` 参数与 `Authorization` 头，其余逐行不变。）

- [ ] **Step 2: 修改 `frontend/src/stores/chat.ts`**

删除顶部 `import { useAuthStore } from '@/stores/auth'`。

`sendMessage` 开头改为：

```ts
  async function sendMessage(question: string) {
    if (isStreaming.value) return

    // Abort any existing stream
    if (abortController.value) {
      abortController.value.abort()
      abortController.value = null
    }
```

（删除 `const auth = useAuthStore()` 与 token 检查；后续 `sseChatStream(question, auth.token, {...}, signal)` 调用改为 `sseChatStream(question, {...}, abortController.value.signal)`，即删去 `auth.token` 实参。）

- [ ] **Step 3: 重写 `frontend/src/router/index.ts`（过渡态：路由公开、守卫删除）**

```ts
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/login',
      name: 'Login',
      component: () => import('@/views/LoginView.vue'),
    },
    {
      path: '/chat',
      name: 'Chat',
      component: () => import('@/views/ChatView.vue'),
    },
    {
      path: '/:pathMatch(.*)*',
      redirect: '/chat',
    },
  ],
})

export default router
```

- [ ] **Step 4: 构建 + 联调验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

联调（后端已在 Task 1 完成公开化）：
1. 终端 1：`cd xiehaoyu-agent && .venv/Scripts/python -m uvicorn backend.app.main:app --port 8000`
2. 终端 2：`cd xiehaoyu-agent/frontend && npm run dev`
3. 浏览器开 `http://localhost:5173/chat`（**不登录直接可达**），发送"介绍一下你自己"，预期正常流式回答

- [ ] **Step 5: Commit**

```bash
git add frontend/src/utils/sse.ts frontend/src/stores/chat.ts frontend/src/router/index.ts
git commit -m 'feat(frontend): 聊天链路去除 token 依赖，路由公开化（过渡态）'
```

---

## Task 7: ChatWidget 抽取（从 ChatMain）

**Files:**
- Create: `frontend/src/components/chat/ChatWidget.vue`
- Rewrite: `frontend/src/components/chat/ChatMain.vue`

**Interfaces:**
- Consumes: 现有 `useChatStore`、`ChatMessage`、`ChatInput`、`WelcomeCard`
- Produces:
  - `<ChatWidget>`：消息区（含自动滚动）+ 输入区；具名插槽 `empty`（无消息时渲染，Task 8/9 注入各自欢迎内容）

- [ ] **Step 1: 创建 `frontend/src/components/chat/ChatWidget.vue`**

```vue
<template>
  <div class="chat-widget">
    <!-- Messages area -->
    <div class="messages-area" ref="messagesRef">
      <slot v-if="chat.messages.length === 0" name="empty" />
      <div v-else class="messages-list">
        <ChatMessage
          v-for="msg in chat.messages"
          :key="msg.id"
          :message="msg"
          :is-streaming="chat.isStreaming && msg === chat.messages[chat.messages.length - 1] && msg.role === 'assistant'"
        />
      </div>
    </div>

    <!-- Input area -->
    <div class="input-area">
      <ChatInput
        :disabled="chat.isStreaming"
        :streaming="chat.isStreaming"
        @send="handleSend"
        @stop="chat.stopStreaming"
      />
    </div>
  </div>
</template>

<script setup lang="ts">
import { watch, nextTick, ref } from 'vue'
import ChatMessage from './ChatMessage.vue'
import ChatInput from './ChatInput.vue'
import { useChatStore } from '@/stores/chat'

const chat = useChatStore()
const messagesRef = ref<HTMLElement>()

async function handleSend(question: string) {
  await chat.sendMessage(question)
}

// Auto-scroll to bottom only when the user is already near the bottom
// (within 100px).  This lets the user scroll up to read history during
// streaming without being yanked back down.
function scrollToBottomIfNear() {
  const el = messagesRef.value
  if (!el) return
  const nearBottom = el.scrollHeight - el.scrollTop - el.clientHeight < 100
  if (nearBottom) {
    el.scrollTop = el.scrollHeight
  }
}

watch(
  () => chat.messages.length,
  () => nextTick(scrollToBottomIfNear)
)
watch(
  () => chat.currentTrace.length,
  () => nextTick(scrollToBottomIfNear)
)
</script>

<style scoped>
.chat-widget {
  display: flex;
  flex-direction: column;
  height: 100%;
  min-height: 0;
}
.messages-area {
  flex: 1;
  overflow-y: auto;
  padding: 1.25rem 1.5rem;
}
.messages-list {
  max-width: 768px;
  margin: 0 auto;
}
.input-area {
  padding: 0 1.5rem 1.25rem;
}

@media (max-width: 768px) {
  .messages-area { padding: 0.75rem 1rem; }
  .input-area { padding: 0 0.75rem 0.75rem; }
}
</style>
```

- [ ] **Step 2: 重写 `frontend/src/components/chat/ChatMain.vue` 为薄封装（旧 `/chat` 页保持可用）**

```vue
<template>
  <ChatWidget>
    <template #empty>
      <WelcomeCard />
    </template>
  </ChatWidget>
</template>

<script setup lang="ts">
import ChatWidget from './ChatWidget.vue'
import WelcomeCard from './WelcomeCard.vue'
</script>
```

- [ ] **Step 3: 构建 + 手动验证旧聊天页回归**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

手动：`npm run dev` 打开 `/chat`，确认欢迎卡、发问、流式回答、停止按钮均与抽取前一致。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/chat/ChatWidget.vue frontend/src/components/chat/ChatMain.vue
git commit -m 'refactor(frontend): 抽取可嵌入 ChatWidget，ChatMain 改为薄封装'
```

---

## Task 8: ChatSection + PortfolioView 组装 + 首页路由切换

**Files:**
- Create: `frontend/src/components/portfolio/ChatSection.vue`
- Create: `frontend/src/views/PortfolioView.vue`
- Modify: `frontend/src/router/index.ts`

**Interfaces:**
- Consumes: Task 4/5 全部区块组件、Task 7 的 `<ChatWidget>`（`empty` 插槽）、`utils/quick-questions.ts` 的 `WELCOME_QUESTIONS`、Task 6 的路由
- Produces:
  - 路由 `/` → PortfolioView（公开）；`#chat` 锚点
  - `chat.sendMessage(question)` 经 chips 触发（复用现有 store action）

- [ ] **Step 1: 创建 `frontend/src/components/portfolio/ChatSection.vue`**

```vue
<template>
  <section class="chat-section section-pad" id="chat" v-reveal>
    <SectionHeading num="03." title="和我聊聊" />
    <p class="chat-desc">
      这个窗口背后是我的数字分身：基于个人知识库的 RAG 问答，
      加上对接电商数据集的 ChatBI（Text2SQL → 自动可视化 → 结果解读）。
      无需登录，直接开聊。
    </p>

    <div class="chat-window">
      <div class="cw-bar">
        <span class="cw-dot" />
        <span class="cw-title">Xiehaoyu-Agent</span>
        <button class="cw-full" aria-label="全屏聊天" title="全屏聊天" @click="goFullscreen">
          <svg viewBox="0 0 24 24" width="16" height="16"><path fill="currentColor" d="M7 14H5v5h5v-2H7v-3zm-2-4h2V7h3V5H5v5zm12 7h-3v2h5v-5h-2v3zM14 5v2h3v3h2V5h-5z"/></svg>
        </button>
      </div>
      <div class="cw-body">
        <ChatWidget>
          <template #empty>
            <div class="cw-empty">
              <AnimatedAvatar state="idle" :size="72" />
              <p class="cw-welcome">
                你好，我是 Xiehaoyu-Agent。可以问我个人经历与技术栈，
                或让我查数据、画图表。
              </p>
              <div class="chips">
                <button
                  v-for="q in WELCOME_QUESTIONS"
                  :key="q.question"
                  class="chip"
                  :disabled="chat.isStreaming"
                  @click="ask(q.question)"
                >
                  {{ q.question }}
                </button>
              </div>
            </div>
          </template>
        </ChatWidget>
      </div>
    </div>
  </section>
</template>

<script setup lang="ts">
import { useRouter } from 'vue-router'
import SectionHeading from './SectionHeading.vue'
import ChatWidget from '@/components/chat/ChatWidget.vue'
import AnimatedAvatar from '@/components/shared/AnimatedAvatar.vue'
import { vReveal } from '@/composables/useRevealOnScroll'
import { useChatStore } from '@/stores/chat'
import { WELCOME_QUESTIONS } from '@/utils/quick-questions'

const router = useRouter()
const chat = useChatStore()

function goFullscreen() {
  router.push('/chat')
}
function ask(q: string) {
  if (chat.isStreaming) return
  chat.sendMessage(q)
}
</script>

<style scoped>
.section-pad {
  max-width: 900px;
  margin: 0 auto;
  padding: 6rem clamp(1.25rem, 6vw, 3rem);
}
.chat-desc {
  color: var(--text-2);
  line-height: 1.8;
  max-width: 640px;
  margin: 0 0 1.75rem;
}
.chat-window {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-card);
  box-shadow: var(--shadow-card);
  overflow: hidden;
  display: flex;
  flex-direction: column;
  height: 640px;
}
.cw-bar {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  padding: 0.7rem 1rem;
  border-bottom: 1px solid var(--border);
  background: var(--bg-subtle);
}
.cw-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: var(--accent-strong);
}
.cw-title {
  font-size: 0.85rem;
  font-weight: 600;
  color: var(--text-1);
  flex: 1;
}
.cw-full {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 28px; height: 28px;
  border: 1px solid var(--border);
  border-radius: 6px;
  background: transparent;
  color: var(--text-2);
  cursor: pointer;
  transition: color 0.2s, border-color 0.2s;
}
.cw-full:hover {
  color: var(--accent-strong);
  border-color: var(--accent-border);
}
.cw-body {
  flex: 1;
  min-height: 0;
}
.cw-empty {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 1rem;
  gap: 1rem;
}
.cw-welcome {
  color: var(--text-2);
  font-size: 0.9rem;
  line-height: 1.7;
  max-width: 380px;
  margin: 0;
}
.chips {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 0.5rem;
  max-width: 480px;
}
.chip {
  border: 1px solid var(--border);
  border-radius: var(--radius-pill);
  background: transparent;
  color: var(--text-2);
  font-size: 0.8rem;
  padding: 0.4rem 0.9rem;
  cursor: pointer;
  transition: color 0.2s, border-color 0.2s, background 0.2s;
}
.chip:hover:not(:disabled) {
  color: var(--accent-strong);
  border-color: var(--accent-border);
  background: rgba(100, 255, 218, 0.06);
}
.chip:disabled {
  opacity: 0.45;
  cursor: not-allowed;
}

@media (max-width: 768px) {
  .chat-window { height: 70vh; }
}
</style>
```

- [ ] **Step 2: 创建 `frontend/src/views/PortfolioView.vue`**

```vue
<template>
  <div class="portfolio">
    <NavBar />
    <SideRails />
    <main>
      <HeroSection />
      <AboutSection />
      <ProjectsSection />
      <ChatSection />
    </main>
    <ContactFooter />
  </div>
</template>

<script setup lang="ts">
import NavBar from '@/components/portfolio/NavBar.vue'
import SideRails from '@/components/portfolio/SideRails.vue'
import HeroSection from '@/components/portfolio/HeroSection.vue'
import AboutSection from '@/components/portfolio/AboutSection.vue'
import ProjectsSection from '@/components/portfolio/ProjectsSection.vue'
import ChatSection from '@/components/portfolio/ChatSection.vue'
import ContactFooter from '@/components/portfolio/ContactFooter.vue'
</script>

<style scoped>
.portfolio {
  min-height: 100vh;
  background: var(--bg-base);
}
</style>
```

- [ ] **Step 3: 修改 `frontend/src/router/index.ts` —— 新增 `/` 路由，catch-all 改为 `/`**

```ts
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/',
      name: 'Portfolio',
      component: () => import('@/views/PortfolioView.vue'),
    },
    {
      path: '/login',
      name: 'Login',
      component: () => import('@/views/LoginView.vue'),
    },
    {
      path: '/chat',
      name: 'Chat',
      component: () => import('@/views/ChatView.vue'),
    },
    {
      path: '/:pathMatch(.*)*',
      redirect: '/',
    },
  ],
})

export default router
```

- [ ] **Step 4: 构建 + 视觉走查（本计划的第一个完整视觉检查点）**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

`npm run dev` 打开 `http://localhost:5173/`，逐项走查：
- Hero：大字、定位句、两个 CTA（"和我聊聊"平滑滚动到聊天区块）
- 导航：滚动后毛玻璃；四个锚点到位；移动端汉堡可用
- 关于我 / 技能与项目 / 实习经历：排版与 hover 效果
- 聊天区块：chips 点击可发问；流式回答、图表、轨迹正常；窗口高度 640px
- Footer：邮箱 / 仓库链接 / 技术栈署名
- 桌面端左右 SideRails 显示，移动端隐藏
- 全屏按钮跳转 `/chat`（此时仍是旧版全屏页，Task 9 重写）

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/portfolio/ChatSection.vue frontend/src/views/PortfolioView.vue frontend/src/router/index.ts
git commit -m 'feat(frontend): 组装单页作品集，首页路由切换为 PortfolioView'
```

---

## Task 9: 全屏 `/chat` 页重写 + 清理删除

**Files:**
- Rewrite: `frontend/src/views/ChatView.vue`
- Modify: `frontend/src/router/index.ts`（删 `/login` 路由）
- Delete: `frontend/src/views/LoginView.vue`、`frontend/src/components/chat/ChatSidebar.vue`、`frontend/src/components/chat/ChatMain.vue`、`frontend/src/components/chat/WelcomeCard.vue`、`frontend/src/stores/auth.ts`、`frontend/src/api/client.ts`

**Interfaces:**
- Consumes: Task 7 的 `<ChatWidget>`（`empty` 插槽）、`WELCOME_QUESTIONS`、`composables/useMediaQuery.ts` 中已存在的 `useEscapeKey(handler: () => void)`
- Produces: `/chat` 全屏页（返回栏 + ChatWidget 铺满）；路由表最终态：`/`、`/chat`、`*` → `/`

- [ ] **Step 1: 重写 `frontend/src/views/ChatView.vue`**

```vue
<template>
  <div class="chat-fullscreen">
    <header class="cf-bar">
      <button class="cf-back" aria-label="返回作品集" @click="goBack">
        <svg viewBox="0 0 24 24" width="16" height="16"><path fill="currentColor" d="M20 11H7.83l5.59-5.59L12 4l-8 8 8 8 1.41-1.41L7.83 13H20v-2z"/></svg>
        <span>返回</span>
      </button>
      <span class="cf-title">和我聊聊</span>
      <span class="cf-spacer" />
    </header>
    <div class="cf-body">
      <ChatWidget>
        <template #empty>
          <div class="cf-empty">
            <AnimatedAvatar state="idle" :size="88" />
            <p class="cf-welcome">
              你好，我是 Xiehaoyu-Agent。可以问我个人经历与技术栈，
              或让我查数据、画图表。
            </p>
            <div class="chips">
              <button
                v-for="q in WELCOME_QUESTIONS"
                :key="q.question"
                class="chip"
                :disabled="chat.isStreaming"
                @click="ask(q.question)"
              >
                {{ q.question }}
              </button>
            </div>
          </div>
        </template>
      </ChatWidget>
    </div>
  </div>
</template>

<script setup lang="ts">
import { useRouter } from 'vue-router'
import ChatWidget from '@/components/chat/ChatWidget.vue'
import AnimatedAvatar from '@/components/shared/AnimatedAvatar.vue'
import { useChatStore } from '@/stores/chat'
import { useEscapeKey } from '@/composables/useMediaQuery'
import { WELCOME_QUESTIONS } from '@/utils/quick-questions'

const router = useRouter()
const chat = useChatStore()

function goBack() {
  if (window.history.length > 1) {
    router.back()
  } else {
    router.push({ path: '/', hash: '#chat' })
  }
}
useEscapeKey(goBack)

function ask(q: string) {
  if (chat.isStreaming) return
  chat.sendMessage(q)
}
</script>

<style scoped>
.chat-fullscreen {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: var(--bg-base);
}
.cf-bar {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.75rem 1rem;
  border-bottom: 1px solid var(--border);
  background: rgba(10, 25, 47, 0.85);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}
.cf-back {
  display: flex;
  align-items: center;
  gap: 0.35rem;
  border: 1px solid var(--border);
  border-radius: 6px;
  background: transparent;
  color: var(--text-2);
  font-size: 0.85rem;
  padding: 0.4rem 0.8rem;
  cursor: pointer;
  transition: color 0.2s, border-color 0.2s;
}
.cf-back:hover {
  color: var(--accent-strong);
  border-color: var(--accent-border);
}
.cf-title {
  font-size: 0.95rem;
  font-weight: 600;
  color: var(--text-1);
}
.cf-spacer { flex: 1; }
.cf-body {
  flex: 1;
  min-height: 0;
  max-width: 900px;
  width: 100%;
  margin: 0 auto;
}
.cf-empty {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 1rem;
  gap: 1rem;
}
.cf-welcome {
  color: var(--text-2);
  font-size: 0.95rem;
  line-height: 1.7;
  max-width: 420px;
  margin: 0;
}
.chips {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 0.5rem;
  max-width: 520px;
}
.chip {
  border: 1px solid var(--border);
  border-radius: var(--radius-pill);
  background: transparent;
  color: var(--text-2);
  font-size: 0.8rem;
  padding: 0.4rem 0.9rem;
  cursor: pointer;
  transition: color 0.2s, border-color 0.2s, background 0.2s;
}
.chip:hover:not(:disabled) {
  color: var(--accent-strong);
  border-color: var(--accent-border);
  background: rgba(100, 255, 218, 0.06);
}
.chip:disabled {
  opacity: 0.45;
  cursor: not-allowed;
}
</style>
```

- [ ] **Step 2: 修改 `frontend/src/router/index.ts` —— 删除 `/login` 路由**

```ts
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/',
      name: 'Portfolio',
      component: () => import('@/views/PortfolioView.vue'),
    },
    {
      path: '/chat',
      name: 'Chat',
      component: () => import('@/views/ChatView.vue'),
    },
    {
      path: '/:pathMatch(.*)*',
      redirect: '/',
    },
  ],
})

export default router
```

- [ ] **Step 3: 删除废弃文件**

```bash
cd xiehaoyu-agent/frontend
rm src/views/LoginView.vue src/components/chat/ChatSidebar.vue src/components/chat/ChatMain.vue src/components/chat/WelcomeCard.vue src/stores/auth.ts src/api/client.ts
```

- [ ] **Step 4: 构建 + 回归验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功（vue-tsc 零错误；若有残留引用报错，逐一清除）

手动验证：
- `/` 作品集正常；聊天区块"全屏"按钮进入 `/chat`
- `/chat`：发问、流式、图表、轨迹正常；返回按钮与 ESC 均回到作品集聊天区块
- 消息状态跨路由保留：嵌入窗口发一条消息 → 进 `/chat` → 消息仍在；流式中切换路由不中断
- 直接访问 `/login` → 重定向到 `/`

- [ ] **Step 5: Commit**

```bash
git add frontend/
git commit -m 'feat(frontend): 全屏聊天页重写为 /chat，删除登录页与废弃组件'
```

---

## Task 10: 聊天组件深色适配（ChatMessage / ChatInput / ChartRenderer / AnimatedAvatar）

**Files:**
- Modify: `frontend/src/components/chat/ChatMessage.vue`（样式区）
- Modify: `frontend/src/components/chat/ChatInput.vue`（样式 + 发送图标色）
- Modify: `frontend/src/components/result/ChartRenderer.vue`（render 函数 layout 合并逻辑）
- Modify: `frontend/src/components/shared/AnimatedAvatar.vue`（样式区）

**Interfaces:**
- Consumes: Task 2 的 CSS 变量（`--bg-card`、`--accent-border` 等）
- Produces: 深色下完整可用的聊天展示链（消息气泡 / inline-result / 数据表 / 图表）

- [ ] **Step 1: `ChatMessage.vue` 样式修改（`<style scoped>` 内逐条替换）**

```css
.chat-message.user .msg-bubble {
  background: var(--bg-bubble-user);
  border: 1px solid var(--accent-border);
  padding: 0.7rem 1rem;
  max-width: 70%;
  border-radius: 18px;
  margin-left: auto;
}
.inline-result {
  margin-top: 0.6rem;
  border: 1px solid var(--border);
  background: var(--bg-card);
  border-radius: 10px;
  overflow: hidden;
}
.ir-section {
  border-bottom: 1px solid var(--border);
}
.action-btn:hover { background: rgba(255, 255, 255, 0.06); color: var(--text-1); }
.irt-connector { width: 1px; flex: 1; background: var(--border); margin: 3px 0; }
.irt-connector.flow {
  background: linear-gradient(180deg, var(--accent-strong) 0%, rgba(136,146,176,0.25) 50%, var(--accent-strong) 100%);
  background-size: 100% 250%;
  animation: flowDown 1.2s linear infinite;
}
```

（模板中两个 SVG 图标的 `color="#0b7a55"` / `color="#4f83e0"` / `color="#d97706"` 分别改为 `#64ffda` / `#64b5f6` / `#ffb86c`。）

- [ ] **Step 2: `ChatInput.vue` 样式修改**

```css
.input-container {
  display: flex;
  align-items: flex-end;
  gap: 0.5rem;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 28px;
  box-shadow: var(--shadow-pop);
  padding: 0.4rem 0.5rem 0.4rem 1.2rem;
  transition: border-color 0.2s;
}
.input-container:focus-within {
  border-color: var(--accent-strong);
  background: var(--bg-card);
}
.send-btn {
  background: var(--accent-strong) !important;
  transition: background 0.2s ease;
}
.send-btn:hover:not(:disabled) { background: var(--accent-strong-hover) !important; }
.send-btn:disabled { background: #3a4a63 !important; }
```

模板中发送/停止两个 `n-icon` 的 `color="#fff"` 改为 `color="#0a192f"`。

- [ ] **Step 3: `ChartRenderer.vue` 的 `render()` 中 layout 合并逻辑替换为**

```ts
    const fig = JSON.parse(props.figureJson)
    const layout = { ...(fig.layout || {}) }
    layout.paper_bgcolor = 'rgba(0,0,0,0)'
    layout.plot_bgcolor = 'rgba(0,0,0,0)'
    layout.font = { ...(layout.font || {}), color: '#8892b0', size: 11 }
    layout.margin = { t: 40, r: 20, b: 50, l: 50, ...(layout.margin || {}) }
    for (const key of Object.keys(layout)) {
      if (/^[xy]axis/.test(key)) {
        layout[key] = {
          gridcolor: 'rgba(136,146,176,0.15)',
          zerolinecolor: 'rgba(136,146,176,0.25)',
          ...layout[key],
        }
      }
    }
    Plotly.newPlot(el, fig.data, layout, {
      responsive: true,
      displaylogo: false,
      displayModeBar: false,
    }).then((plot: any) => { chartInstance = plot })
```

- [ ] **Step 4: `AnimatedAvatar.vue` 样式修改**

```css
.animated-avatar {
  display: inline-block;
  border-radius: 50%;
  overflow: hidden;
  flex-shrink: 0;
  background: rgba(100, 255, 218, 0.06);
}
```

- [ ] **Step 5: 构建 + 深色聊天全链路视觉验证**

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

`npm run dev` 验证：
- 发送"2018 年每月订单数，帮我画个图"：图表深色可读（字体/网格线）、数据表深色、SQL 折叠面板正常
- 发送"介绍一下你自己"：RAG 回答气泡、引用、轨迹时间线在深色下清晰
- 用户气泡薄荷描边、流式光标、流光轨迹可见
- 输入框深色、发送按钮薄荷底深色图标

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/chat/ChatMessage.vue frontend/src/components/chat/ChatInput.vue frontend/src/components/result/ChartRenderer.vue frontend/src/components/shared/AnimatedAvatar.vue
git commit -m 'feat(frontend): 聊天组件全链路深色适配'
```

---

## Task 11: 收尾（meta / README / deploy / 全量验收）

**Files:**
- Modify: `frontend/index.html`
- Modify: `README.md`（技术栈表「鉴权/限流」行 + 架构章节路由描述）
- Modify: `deploy/xiehaoyu-agent.service:10`
- Modify: `.env`（用户本地，gitignored —— 在最终回复中提醒用户手动同步，不入库）

**Interfaces:**
- Consumes: 前序全部任务
- Produces: 可部署的最终态

- [ ] **Step 1: 修改 `frontend/index.html`**

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="谢浩宇的数字分身 —— 基于个人知识库的 RAG 问答 + ChatBI 数据问答的个人智能体" />
    <title>谢浩宇 · 个人智能体</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

- [ ] **Step 2: 修改 `deploy/xiehaoyu-agent.service` 第 10 行（uvicorn 加 `--proxy-headers`，配合 Nginx 已有 `X-Forwarded-For` 获取真实 IP）**

```
ExecStart=/srv/xiehaoyu-agent/.venv/bin/uvicorn backend.app.main:app --host 127.0.0.1 --port 8000 --workers 1 --proxy-headers
```

- [ ] **Step 3: 修改 `README.md`**

技术栈表「鉴权/限流」行替换为：

```
| **限流**       | 公开访问 + 按 IP 小时限流 + 全局每日上限           | 防刷 API                        |
```

架构章节中「JWT 鉴权」相关表述改为「公开访问（按 IP 限流）」；登录/访问码相关段落删除。

- [ ] **Step 4: 全量测试 + 构建**

Run: `cd xiehaoyu-agent && .venv/Scripts/python -m pytest tests/ -v`
Expected: 全部 PASS

Run: `cd xiehaoyu-agent/frontend && npm run build`
Expected: 构建成功

- [ ] **Step 5: 全量验收清单（手动，对照设计文档 §10.2）**

桌面 + 移动端（DevTools 375px）各过一遍：
- [ ] Hero / 关于我 / 技能项目 / 聊天 / Footer 五张截图检查点均达标
- [ ] RAG 问答（"介绍一下你自己"）：引用展示正常
- [ ] ChatBI（"2018 年每月订单数，帮我画个图"）：图表 + 数据表 + SQL 折叠 + 执行轨迹
- [ ] 停止生成 / 重试按钮
- [ ] 全屏 `/chat`：进入 / 返回 / ESC / 流式中切换路由 / 刷新直达
- [ ] 锚点平滑滚动；移动端汉堡菜单；SideRails 移动端隐藏
- [ ] reduced-motion（DevTools 模拟）下无动效
- [ ] 429 文案：用 `for` 循环快速打满 IP 配额验证提示（或临时把 `IP_HOURLY_QUOTA` 调小）

- [ ] **Step 6: Commit**

```bash
git add frontend/index.html README.md deploy/xiehaoyu-agent.service
git commit -m 'chore: 更新站点 meta、README 鉴权描述与 uvicorn proxy-headers 配置'
```

---

## Self-Review 记录

- **Spec 覆盖**：设计文档 §3-§10 各节均有对应 Task（§3 路由→T6/T8/T9；§4 视觉→T2/T10；§5 组件→T4/T5/T7/T8/T9；§6 后端→T1；§7 内容→T3；§8 错误处理→T1 429 文案 + T9 边界 + T11 验收；§9 性能/无障碍→T2 reduced-motion + T11 验收；§10 测试→T1 pytest + T11 清单）
- **类型一致性**：`sseChatStream(question, callbacks, signal)` 新签名在 T6 定义、T8/T9 沿用；`ChatWidget` 的 `empty` 插槽在 T7 定义、T8/T9 使用；`profile/skillGroups/projects/experience` 字段名在 T3 定义、T4/T5 使用一致
- **已知中间态**：T2-T8 期间旧页面视觉异常（预期，T9 后消失）；T6 后 `/login` 页仍可访问但无入口（T9 删除）
